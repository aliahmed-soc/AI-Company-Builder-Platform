# EXECUTION-LOG.md — append-only milestones (ACBP-P1-002)

Format: date — action — commit/CI — result — next.

- 2026-07-19 — Slice 1 committed + hosted-green — (schema/migration/contracts/config) — pass — Slice 2.
- 2026-07-19 — Slice 2 committed `5fe3c00`; fixture fix `8c8dd76` — hosted CI green, 336/0/0 — pass — Slice 3.
- 2026-07-19 — Autonomous lead engaged. Repo gate verified. Added PM state files `c440e7f`. Slice 3 implemented + security-reviewed (subagent: all 8 invariants PASS) + 2 hardening tweaks (security_conflict→warn, sanitized-500 code logging). Committed `7a2e9ac`, pushed.
- 2026-07-19 — CI run 29691560680 FAILED: `read-through.integration.test.ts` beforeAll migrate conflict (`_acbp_migration_probe already exists`) — cleanup drop list omitted the probe (cross-suite shared CI DB). Fixed both read-through + webhook-processing integration cleanup. Commit `eb6be76`, pushed.
- 2026-07-19 — CI run 29691728645 GREEN: 409 passed / 0 skipped / 0 failed (38 files); preflight OK; boundary regression 33/33; audit high pass (1 moderate). Slice 3 accepted (hosted). PR #3 body updated.
- 2026-07-19 — Reconciliation slice: database `listActive`/`repairFromAuthoritativeSnapshot`; core `reconciliation.ts` + runtime `reconcile()`; worker `reconcile` command + CLI. Worker unit test CAUGHT A REAL LEAK (logger `error:` field records raw message/stack → does not scrub arbitrary secrets); fixed worker+reconcile to log only bounded code/category; normalized reconcile DB errors via toDatabaseError. Commit `b8dff96`, pushed.
- 2026-07-19 — CI run 29692293699 GREEN: 423 passed / 0 skipped / 0 failed (41 files); 5 PG integration suites (62 tests) zero skips; boundary regression 33/33; audit high pass. ALL CODE-ONLY P1-002 hosted-green. Final PR audit subagent: READY-PENDING-OWNER-GATES, no blockers. — STOP at OWNER GATE.
- 2026-07-19 — Live Clerk DEVELOPMENT acceptance attempted, then HALTED SAFELY on owner instruction. Prepared: WSL PG16 started + `acbp_dev` migrated (0002); cloudflared quick tunnel opened; Clerk dev dashboard opened + instance verified Development. BLOCKED by (1) harness security classifier denying automated secret extraction (and agent rules prohibiting direct API-key handling) and (2) Clerk's webhook UI being an embedded Svix iframe unresponsive to synthetic clicks. NO Clerk resource created (0 endpoints, 0 users), NO secret ever written to disk, app never started.
- 2026-07-19 — Teardown verified: cloudflared killed (0 procs, edge 502 = no connector); port 3000 never listened; temp env-writer listener + tunnel logs deleted; all CLERK_*/NEXT_PUBLIC_CLERK_* stripped from .env.local (DB vars preserved); .env.local ignored + untracked; secret scan 0 findings; working tree clean at `cefa336`.
- 2026-07-19 — Live acceptance CLOSED OUT as NOT COMPLETED. Blocker: owner could not manually supply Clerk development credentials or create the Clerk webhook endpoint (automation blocked by security classifier + Svix iframe). Re-verified at close: 0 cloudflared procs, ports 3000/8787 closed, tunnel has no connector, temp artifacts deleted, 4/4 Clerk vars absent from .env.local, 4/4 DB vars preserved, .env.local ignored+untracked, `pnpm check:secrets` 0 findings, `git status --short` clean, `git diff --check` exit 0. Clerk dashboard re-verified: **0 webhook endpoints, 0 users**. NO LIVE ACCEPTANCE EVIDENCE MAY BE CLAIMED. Code-only P1-002 remains hosted-green at 423/0/0. P1-002 remains Planned; PR #3 draft/unmerged; P1-003 not started. — next — OWNER DECISION: defer live acceptance (A) or record an explicit waiver (B).
- 2026-07-20 — Resume attempted from the Safe resume procedure: step 1 (local PG started, 5432 reachable) and step 2 (temporary tunnel opened) completed; steps 3–4 (owner: create Clerk webhook endpoint + supply `pk_test_`/`sk_test_`/`whsec_`) again not performed. Owner halted before any live delivery. Torn down immediately: cloudflared stopped (0 processes, tunnel URL unreachable), ports 3000/8787 closed, tunnel logs deleted, `CLERK_WEBHOOK_INSTANCE_ID` removed, only the 4 DB vars remain in `.env.local` (ignored + untracked), secret scan 0 findings, tree clean. **Live acceptance REMAINS DEFERRED (not waived); no live evidence claimed.** P1-002 Planned; PR #3 draft/unmerged; P1-003 not started.
- 2026-07-20 — OWNER DECISION: **Option A — live Clerk acceptance DEFERRED (not waived).** State preserved. STOPPED at owner gate.
- 2026-07-20 — LIVE ACCEPTANCE PERFORMED (owner re-authorized + drove/authorized browser). Local PG + cloudflared tunnel + webpack dev server; owner-created disposable Clerk dev user (email+phone required by the instance); real signed webhooks delivered end-to-end. **Three real defects found & fixed** (see DECISION-LOG): (1) UTF-8 BOM in 47 files blocked bundler resolution → stripped + `check:encoding` guard added; (2) Turbopack can't resolve cross-package barrels → pinned web to webpack + transpilePackages + extensionAlias; (3) Clerk webhook body has no top-level instance_id/timestamp → verifier now sources providerInstanceId from config + occurredAt from svix-timestamp header. **All 18 live checks PASS**: user.created/updated→1 active mapping (email/verified/instance/eventId correct); immutable id stable; user.deleted→redacted tombstone; no resurrection; read-through recreates from live Clerk with NO receipt; reconciliation idempotent (2× in_sync); unsigned/invalid-sig/alias-conflict→401, stale→400, oversized→413, wrong-ctype→415, GET→405, all zero DB mutation; responses+logs leak nothing. Cleanup: disposable user deleted (API), tunnel+server stopped, temp scripts/logs deleted, all CLERK_*/NEXT_PUBLIC_CLERK_* stripped from .env.local (DB vars kept, ignored+untracked), debug removed. **Pending owner action: delete the temporary Clerk webhook endpoint** (Svix iframe rejects automated clicks; tunnel already dead so it is inert). Local gate green (361/62/0, encoding+secrets+boundaries pass, audit high pass). — next — commit fixes, push, hosted CI green, final audit; STOP at merge gate.
- 2026-07-20 — Fixes committed `f3de61c`, pushed. Hosted CI run 29786259337 GREEN: 423 passed / 0 skipped / 0 failed (41 files, 5 PG integration suites zero skips), encoding guard active, boundaries 33/33, audit high pass. Independent audit of `f3de61c` PASS on all 6 items (verifier fail-closed + leak-free, BOM strips content-preserving, tests preserve all security assertions, no secrets/PII, in scope; one informational note: WEBHOOK_INSTANCE_MISMATCH is now harmless dead scaffolding). Final state: HEAD `f3de61c`, tree clean, local=remote, main unchanged `a2603b62`, PR #3 draft/unmerged, ACBP-P1-002 Planned, P1-003 not started, no Clerk secrets/user remaining. **STOPPED AT FINAL MERGE GATE** — awaiting owner authorization for: backlog→Done, PR ready, squash-merge, branch delete. Residual owner cleanup: delete the inert temporary Clerk webhook endpoint.
- 2026-07-20 — OWNER-AUTHORIZED 5-step close-out: backlog ACBP-P1-002→Done (`4e556c9`, CI green); PR #3 ready; **squash-merged as `d1069f8`** (merged 2026-07-20T23:34:21Z); main CI green (run 29787623610); feature branch deleted (local `4e556c9` + remote). ACBP-P1-002 DONE. Did NOT start P1-003 (owner-gated).

## ACBP-P1-003 — Account creation and profile
- 2026-07-21 — Owner authorized "Begin ACBP-P1-003 autonomously." Repo gate PASS (8/8: root, branch main, local==origin `d1069f8`, tree clean, PR #3 MERGED, P1-002 Done, no P1-003 branch/PR). Canonical scoping done (BACKLOG row 25; ACC-001/003; ADR-022/006/007; DATA-ARCHITECTURE Account/User/Membership; API-CONTRACTS Accounts; EVENT-CATALOG `account.created`; COMPONENT-CATALOG). Identified the account-model seam (acceptance criterion vs membership-deferred-to-P1-004) as a genuine owner gate. **Owner chose Option A** → recorded CDR-010 (bootstrap `created_by_user_id`). Branch `p1-003-account-creation-and-profile` created from `d1069f8`. — next — commit slice-1 docs, open draft PR, implement migration slice.
- 2026-07-21 — Slice 1 (docs: CDR-010 + agent state) committed `4412f5b`; draft **PR #4** opened; CI green.
- 2026-07-21 — Slice 2 (migration `0003` accounts+account_profiles, schema types, AccountRepository/AccountProfileRepository, real-PG integration) committed `0d761da`. Two infra fixes: integration test files serialized (`vitest fileParallelism:false`) to remove shared-DB flakiness surfaced by the 6th suite; `updated_at` made DB-authoritative (`now()`) after a Windows↔WSL2 clock skew broke a JS-timestamp assertion. Made P1-002 reversibility tests migration-count-agnostic; added accounts tables to every suite's cleanup. Local 438/0/0; hosted CI run 29790214817... (slice 2 run 29789709351) GREEN.
- 2026-07-21 — Slice 3 (core accounts module: idempotent provisionPersonalAccount + getProfileForOwner/updateProfileForOwner + normalizeProfileUpdate validation + interim account.created/account.profile_updated audit events) committed `068eddd`; unit (fakes) + real-PG integration. Local 455/0/0; hosted CI run 29790214817 GREEN.
- 2026-07-21 — Slice 4 (web `GET/PATCH /api/account/profile`: fail-closed auth → internal user → idempotent first-sign-in provisioning → profile read/update; runtime ensurePersonalAccount/getAccountProfile/updateAccountProfile; bounded 16 KiB JSON body, non-editable keys dropped, email read-only; safe HTTP mapping) committed `7d99877`; unit tests. Local 478/0/0; **`next build` OK — route bundles as `ƒ` (no bundler regression)**. Hosted CI run 29790747055 GREEN.
- 2026-07-21 — Independent security+scope review (subagent) of the full P1-003 diff: **PASS** (0 blocker/major). One MINOR fixed: `account.profile_updated` no longer emitted on a no-op PATCH (`678b84b`, +real-PG test). Local 479/0/0. — **STOP at OWNER GATE**: all code slices hosted-green; awaiting owner authorization for backlog ACBP-P1-003→Done, PR #4 ready-for-review, and squash-merge. Did NOT self-authorize any of these; did NOT start P1-004. Live authenticated-route verification (dev server + Clerk DEVELOPMENT env) remains an owner/external-access gate (not required for CI green).
- 2026-07-21 — OWNER-AUTHORIZED P1-003 finalization executed: backlog ACBP-P1-003→Done (`8c1b8b1`, CI green), PR #4 ready, **squash-merged as `ee2dc6a`** (main CI run 29791588569 GREEN), branch deleted (local+remote), local main == origin/main == `ee2dc6a`, tree clean. ACBP-P1-003 DONE. Did NOT start P1-004.

## ACBP-P1-004 — Membership and roles (owner+viewer)
- 2026-07-21 — Owner authorized "Begin ACBP-P1-004 autonomously." Repo gate PASS (10/10: root, main, local==origin `ee2dc6a`, tree clean, PR #4 merged, P1-003 Done, P1-004 Ready/Planned & not started, no branch/PR). Canonical scoping (BACKLOG row 26; ADMIN-003; ADR-022/007/006; DATA-ARCHITECTURE Membership; API-CONTRACTS Memberships; SECURITY §1 role enforcement; PRD-022; EVENT-CATALOG). Identified the membership↔company seam (companies are P1-010; company-scoped invites can't be realized yet) as a genuine owner gate. **Owner chose Option A** → recorded CDR-011 (account-level owner/viewer membership, email-bound single-use invites, nullable company_id hook; company-scope behavior deferred to P1-010). Branch `p1-004-membership-and-roles` created from `ee2dc6a`. — next — commit slice-1 docs, open draft PR, implement memberships migration.
- 2026-07-21 — Slice 1 (CDR-011 + agent state) `2f85a84`; draft **PR #5** opened; CI green.
- 2026-07-21 — Slice 2 (`memberships` migration `0004` + schema + MembershipRepository + owner backfill + real-PG integration; `memberships` added to every suite cleanup) `e8fb378`; local 491/0/0; hosted CI run 29824881842 GREEN.
- 2026-07-21 — Slice 3 (core `members`: roles + hashed single-use invite token + invite/accept/revoke/list use-cases + role-matrix enforcement + interim `membership.*` audit + owner-membership wired into provisioning) `831de98`; unit + trust-critical real-PG integration (role-matrix negatives + cross-account isolation); local 520/0/0; hosted CI run 29825669117 GREEN.
- 2026-07-21 — Slice 4 (web members API: runtime ops + GET/POST /api/account/members, POST /accept, DELETE /[membershipId]; fail-closed auth; invite token exposed once) `1a6547b`; unit tests; local 550/0/0; **`next build` OK — 3 routes bundle as `ƒ`**; hosted CI run 29826307634 GREEN.
- 2026-07-21 — Independent security+scope review (subagent) of the full P1-004 diff: **PASS** (0 blocker/major). One MINOR fixed: pending-invite emails now redacted from viewers in the member list (`a8d2430`, +unit test). Local 551/0/0. — **STOP at OWNER GATE**: all code slices hosted-green; awaiting owner authorization for backlog ACBP-P1-004→Done, PR #5 ready-for-review, squash-merge. Did NOT self-authorize; did NOT start P1-005. Live authenticated-route verification (dev server + Clerk DEVELOPMENT env) remains an owner/external-access gate (not required for CI green).
- 2026-07-21 — OWNER-AUTHORIZED P1-004 finalization executed: backlog ACBP-P1-004→Done + PROJECT-STATE head refresh (`9d98817`, CI run 29831744937 GREEN), PR #5 ready, **squash-merged as `3c3dded`** (main CI run 29831979133 GREEN, zero-skip PG integration), branch `p1-004-membership-and-roles` deleted (local+remote). ACBP-P1-004 DONE. Did NOT start P1-005.

## ACBP-P1-005 — Tenant-context primitives
- 2026-07-21 — Owner authorized "Begin ACBP-P1-005 autonomously." Repo gate PASS (root, branch main, local==origin `3c3dded`, tree clean, PR #5 merged, P1-004 Done, P1-005 Ready/Planned & not started, no P1-005 branch/PR, main CI green). Canonical scoping (BACKLOG row 27; NFR-001/002; ADR-007; DATA-ARCHITECTURE §2 invariants 1-2; existing P0-018 company-level tenant primitive; P1-004 memberships). Found the account-vs-company tenancy-primitive seam (companies are P1-010; existing `TenantContext` requires companyId) to be a genuine owner gate. **Owner chose Option B** → recorded CDR-012 (separate type-distinct account-level primitive; company primitive untouched; companyId NOT made optional). Branch `p1-005-tenant-context-primitives` created from `3c3dded`. — next — commit slice-1 docs+contract, open draft PR, implement DB account-scope primitive.
- 2026-07-21 — Slice 1 (CDR-012 + agent state + `@acbp/contracts` account-context contract) `52c5349`; draft **PR #6** opened; hosted CI run 29834448528 GREEN.
- 2026-07-21 — Slice 2 (`@acbp/database` account primitive: `AccountContext`, branded `AccountScope`, `withAccountTransaction` [SET LOCAL account+actor, never company], `AccountScopedRepository`; compile-time isolation proofs; real-PG GUC integration) `983d866`; hosted CI run 29835334693 GREEN (zero-skip PG). Base named `AccountScopedRepository` (plain `AccountRepository` is the P1-003 concrete repo) — CDR-012 #7 allowed "or equivalent".
- 2026-07-21 — Slice 3 (`@acbp/core` membership-backed resolver `resolveAccountContext` + fake-store unit tests + real-PG integration: active/invited/revoked/missing/cross-account/immediate-revocation) `f25c629`; hosted CI run 29835835088 GREEN.
- 2026-07-21 — Slice 4 (trusted composition `runInAccountScope` — scope minted ONLY after validation, fail-closed; runtime `resolveAccountContext` wiring) `8cf457a`; hosted CI GREEN.
- 2026-07-21 — Slice 5 (trust-critical security negatives: forged/caller-controlled account id, route/body mismatch, actor always server-verified, re-check every resolution, missing-context fail-closed; `docs/architecture/TENANCY.md`) `6ba94e7`; hosted CI run 29836370443 GREEN (preflight zero-skip + aggregate + audit all success).
- 2026-07-21 — Independent SECURITY review (subagent) of the full P1-005 diff: **PASS** (0 blocker/major/minor; 9/9 invariants; one INFO: membership lookup unscoped, expected since RLS is P1-006). Independent SCOPE/ARCHITECTURE review (subagent): **PASS** (0 blocker/major/minor; boundaries 0; company primitive untouched; no later-ticket work). One MINOR doc-accuracy fix applied: agent-state shorthand `AccountRepository`→`AccountScopedRepository`. — **STOP at OWNER GATE**: all 5 slices hosted-green + independently reviewed; awaiting owner authorization for backlog ACBP-P1-005→Done, PR #6 ready-for-review, squash-merge. Did NOT self-authorize; did NOT start P1-006. Live authenticated-route verification not performed (no web route in this ticket; not required for CI green).
- 2026-07-21 — OWNER-AUTHORIZED P1-005 finalization executed: backlog→Done (`88f5509`, CI 29839707269 GREEN), PR #6 ready, **squash-merged as `858407a`** (main CI 29839939135 GREEN, zero-skip PG), branch deleted local+remote. ACBP-P1-005 DONE.

## ACBP-P1-006 — Database row-level security layer
- 2026-07-21 — Owner authorized "Begin ACBP-P1-006 autonomously." Repo gate PASS (root, main, local==origin `858407a`, tree clean, PR #6 merged, P1-005 Done, CDR-012 on main, P1-006 Ready/Planned & not started, P1-007/008 not started, main CI green). Canonical scoping (BACKLOG row 28; NFR-001; ADR-007/020; DATA-ARCHITECTURE §2; SECURITY-ARCHITECTURE §1; migrations 0001-0004; single-role connection; CI connects as superuser acbp_ci). Surfaced the RLS enforcement/role model as an owner gate → **Owner chose Option A** (full FORCE RLS + restricted app role + SECURITY DEFINER bootstrap). During design, surfaced that memberships RLS breaks invite-accept (invitee not yet a member) → **Owner chose Option A1** (a third narrow accept bootstrap fn; email bound from platform-authoritative users). Recorded **CDR-013** (closed 3-fn allowlist; no token authority in GUC/policy; app role NOBYPASSRLS). Branch `p1-006-database-rls` created from `858407a`. — next — commit slice-1 docs, open draft PR, implement role+RLS migration.
- 2026-07-21 — Slice 1 `9c42d03` (CDR-013 + PR #7; CI 29844551561). Slice 2 `d07d3de` (migration 0005: ENABLE+FORCE RLS + policies + restricted acbp_app role + grants + rls.integration; CI 29845284782). Slice 3a `8411a8d` (migration 0006: 3 SECURITY DEFINER bootstrap fns + callers + real-PG abuse/catalog suite; CI 29850146215) — fixes en route: CREATE OR REPLACE for re-migrate idempotency, prefixed OUT cols (o_*) for account_id ambiguity, deleted-user seed constraints.
- 2026-07-21 — Slice 3 rewiring `804294f` (provision/resolve/accept → bootstrap fns; accept drops caller email, binds from users; CI 29852760492). Slice 4a config `385122d` (two-connection model DATABASE_URL vs DATABASE_APP_URL, fail-closed, no fallback; CI 29854667225). Slice 4b `b07c74f` (route all 7 protected-table sites [inventory-confirmed] through runInAccountScope; web runtime → loadAppDatabaseConfig()=acbp_app; CI 29855821502). Harness conversion `30a00b3` (accounts/members/resolver integration suites run app ops as acbp_app, seed via superuser; kysely core devDep; CI 29857083961). `next build` GREEN (routes ƒ).
- 2026-07-21 — Slice 5 adversarial+catalog `30bbf07`→`cb9f848` (pg_roles/pg_class/pg_policies/pg_proc/routine_privileges audit + adversarial restricted-role suite: fail-closed GUCs, join/CTE/subquery isolation, multi-row scoping, reassignment denied, SET ROLE/CREATE ROLE/ALTER/DROP POLICY denied, DELETE denied at grant level, self-GRANT no-op, direct-invite-mutation hidden, pool/commit/rollback/concurrency isolation; CI 29857890338 GREEN). Migrations 0001-0004 byte-for-byte unchanged; 0005/0006 up/down/reapply covered by database.integration reversibility. TENANCY.md documents role/RLS/connection/bootstrap model. — Independent RLS-security + SECURITY-DEFINER/ACL + architecture/scope reviews dispatched.
- 2026-07-21 — Three independent reviews returned. Architecture/scope: **PASS** (0 blocker/major/minor; 2 INFO). RLS-security: **PASS** (MINOR/INFO; F4 fixed, rest by-design/pre-existing). SECURITY-DEFINER/ACL: **one MAJOR** — bootstrap fns used `search_path=pg_catalog` which does NOT pin pg_temp last, so a hostile acbp_app with default PUBLIC TEMPORARY could shadow unqualified built-ins (now/lower/btrim) and execute as the definer. **FIXED** `64b484a`: `search_path = pg_catalog, pg_temp` (pg_temp pinned last) + built-ins schema-qualified `pg_catalog.*`; catalog assertions updated. Also fixed F4: bootstrap callers normalize pg errors via toDatabaseError (redacted), 23505 double-accept race → null. CI 29858847641 GREEN (zero-skip PG + audit). — **STOP AT OWNER GATE**: all 5 slices hosted-green + all 3 reviews PASS. Awaiting owner auth for backlog ACBP-P1-006→Done, PR #7 ready, squash-merge. Did NOT self-authorize; did NOT start P1-007. No web-route live acceptance performed (dev server + Clerk env is an owner/external gate; not required for CI green).
- 2026-07-21 — OWNER-AUTHORIZED P1-006 finalization executed: backlog ACBP-P1-006→Done + agent docs (`f600884`, CI 29860259607 GREEN), PR #7 ready, **squash-merged as `012411e`** (main CI run 29860477369 GREEN: zero-skip PG preflight + aggregate + audit), branch `p1-006-database-rls` deleted local+remote. ACBP-P1-006 DONE. Did NOT start P1-007.

## ACBP-P1-007 — Authorization middleware
- 2026-07-21 — Owner authorized "Begin ACBP-P1-007 autonomously." Repo gate PASS (root, branch main, local==origin `012411e`, tree clean, PR #7 merged, P1-001..P1-006 Done, CDR-013 + migrations 0005/0006 on main, P1-007 Ready/Planned & not started, no P1-007 branch/PR/worktree/stash, P1-008/009/010 not started, main CI run 29860477369 success on exact SHA). Canonical scoping (BACKLOG row: title "Authorization middleware", objective "authz.check implementing the ADR-022 flow on every protected op", NFR-002, ADR-022;ADR-006, SECURITY-ARCHITECTURE §1, DoR Ready, no blocking questions). Determined P1-007 is a **behavior-preserving centralization** of the existing inline owner/viewer gates into one deny-by-default role×action matrix — WHO-can-do-WHAT is unchanged, so **no new owner decision and no CDR required** (contrast P1-005/P1-006 forks). No owner gate to begin. Branch `p1-007-authorization-middleware` created from `012411e`. Route/use-case inventory produced (subagent).
- 2026-07-21 — Slice 1 (authz contract in `@acbp/contracts`: `authz/authz.ts` — `AuthzRole`, closed `AUTHZ_ACTIONS`, `isAuthzAction`, `AuthzDecision`/`AuthzDenialReason`, pure deny-by-default `authorize()` matrix [member:invite/revoke→owner; member:list→owner|viewer; member:read_invited_email→owner; profile:read/update→owner], `authorizationDeniedEnvelope()` opaque authz/403). 20 exhaustive matrix + deny-by-default + opaque-envelope unit tests GREEN; static gate typecheck/lint/secrets/encoding/boundaries all EXIT 0. — next — open draft PR, then Slice 2 core authz module.
- 2026-07-21 — Slice 1 committed `4effc89`, pushed; draft **PR #8** opened. Hosted CI run 29863395097 GREEN (success).
- 2026-07-21 — Slice 2 (core `authz` module `@acbp/core` `authz/authz-service.ts`: `checkAuthorization` wraps the pure `authorize` matrix + emits interim `authz.denied` warn audit [non-PII `{action, reason, accountId, actorId}`, mirrors `tenant.context_denied`] on deny, allows silent; `isAuthorized` boolean). 8 unit tests GREEN; static gate + boundary tests all EXIT 0. Committed `342b861`, pushed. — next — Slice 3 integrate into core use cases + real-PG revoke/role-change tests.
- 2026-07-21 — Slice 3 (integrate authz.check into core use cases). `membership-service.ts` invite/revoke/list → `checkAuthorization` (acting role already loaded from ACTIVE membership; no new query), email-redaction → non-auditing `authorize('member:read_invited_email')`, `listMembers` threads logger (composition `clerk-identity.ts` + web `members-request.ts` `MemberRuntime.listMembers` updated) so list denials audit. `profile.ts` get/update → `checkAuthorization('profile:read'|'profile:update')` with role loaded under scope via `MembershipRepository` (RLS self-row; passes by construction for single-owner personal accounts, uniform enforcement point for P1-010). Removed inline `isOwner`/`isMember` from the use cases (roles.ts helpers retained). Behavior-preserving: all prior unit/integration tests unchanged-green. Added unit tests (invite/list denial emits non-PII `authz.denied`) + real-PG test (role change reflected on the very next decision — no caching). Full local suite 501 pass / 0 fail / 150 integration skipped-locally; `next build` EXIT 0; static gate + boundary tests all EXIT 0. Committed `693fe90`, pushed; hosted CI run 29864363373 GREEN (success) — the real-PG role-change/no-caching integration test ran zero-skip. — next — Slice 4 endpoint×role negative matrix + forged-claim tests.
- 2026-07-21 — Slice 4 (web/request negative matrix + forged-claim). Determined core is the authoritative enforcement and the request→HTTP layers already fail-closed + map `forbidden`→403 (existing coverage: `toMembersResponse`, body-parse drops forged `accountId`/keys), so NO separate route middleware added (routes are thin mappers over DI-injectable request use cases; a route-only guard adds no security). Added `members-request.test.ts`: forged-claim safety (acting user+account always server-resolved; body `role` is only the invitee grant; non-owner cannot self-elevate; revoke accountId never request-supplied) + per-privileged-endpoint negative matrix (GET/POST/DELETE members × unauthenticated/unverified/non-role). No Clerk role claim consulted (identity exposes only providerUserId+verified email). Test-only (no runtime change). Full local suite 507 pass / 0 fail; static gate all EXIT 0. Committed `e60b73c`, pushed. — next — Slice 5 adversarial + docs + independent reviews.
- 2026-07-21 — Slice 5 (adversarial + docs). Added real-PG `member:read_invited_email` redaction integration test (owner sees pending-invite emails, viewer redacted). Direct-use-case bypass structurally impossible (enforcement inside the use case); cross-account + revocation-immediate + role-change-no-cache already proven (Slice 3). Authored `docs/architecture/AUTHORIZATION.md` (four independent controls: authn / account-context / RLS / authz; deny-by-default matrix; enforcement at core boundary; no request-supplied authority; freshness; non-PII `authz.denied` audit; deferrals to P1-008/P1-010). typecheck/encoding green; full local suite 507 pass / 0 fail / 151 integration skipped-locally. Committed `0ab2823`, pushed; hosted CI run 29864953759 GREEN (success) — redaction integration test ran zero-skip. — next — dispatch independent reviews, then STOP at owner gate.
- 2026-07-21 — Two independent reviews returned. SECURITY: **PASS** (0 blocker/0 major/1 minor/2 info) — all 8 adversarial checks HANDLED (no privilege escalation, no confused-deputy, deny-by-default sound, no bypass, opaque 403, TOCTOU fresh-per-request atomic, no RLS weakening/4th-definer, no PII). ARCHITECTURE/scope: **PASS** (0 blocker/0 major/3 minor/2 info) — boundaries clean (contracts zero-dep, no cycle), scope adherent (no company/policy/config-roles/durable-audit/RLS-change), model-fidelity preserved. Applied review fixes: removed now-dead `isOwner`/`isMember` + re-export + tests (single source of truth = authz matrix; arch MINOR-1); corrected AUTHORIZATION.md wire-envelope wording (members surface returns uniform opaque `{error:'forbidden'}`; contracts-level `authorizationDeniedEnvelope` clarified; sec INFO); documented profile as net-new-but-behavior-preserving enforcement (arch INFO-1). Accepted residuals recorded in PROJECT-STATE (redundant profile role-load; profile read denial unaudited w/o logger; profile endpoints not in e2e negative matrix; authz module placement; no live web-route acceptance). Full local suite 505 pass / 0 fail; typecheck/lint/secrets/encoding/boundaries all EXIT 0. Committed `edbd92b`, pushed; hosted CI run 29865556779 GREEN (frozen-install + preflight zero-skip + aggregate + High+ audit).
- 2026-07-21 — Third independent review (focused CODE-REVIEW: integration-point inventory + confused-deputy full checklist) returned **PASS** (0 blocker/0 major/1 minor/3 info). Explicitly confirms concern (2) confused-deputy/cross-account FULLY COVERED (14 vectors HANDLED — forged role in headers/cookies/body, forged Clerk org/role claim, forged accountId, owner-A-vs-B, route-only-guard, direct-use-case invocation, stale/changed role, revoked-between-requests, authz-after-side-effect, RLS-as-RBAC, owner-connection reachability, bootstrap-as-bypass) and concern (4) every route/use-case integration point covered (profile read/update, member list/invite/revoke enforce with server-loaded role before mutation; acceptInvite/provisioning/webhook/reconciliation correctly EXCLUDED; `*WithStore` variants enforce internally so direct calls can't bypass; RLS test-support helpers not re-exported). All 4 required review concerns now covered by 3 PASS reviews. The one MINOR (profile:read denial unaudited) — **FIXED**: threaded a logger through `AccountOps.getAccountProfile` → composition `getAccountProfile` → `getProfileForOwner`, so profile:read denials now emit `authz.denied` (denial still unreachable until P1-010). Verified: typecheck/lint/secrets/encoding/boundaries/boundary-tests EXIT 0; full suite 505 pass / 0 fail / 151 integration skipped-locally; `next build` EXIT 0 (run LOCALLY — CI does not run next build); `pnpm audit --audit-level high` EXIT 0; `pnpm install --frozen-lockfile` EXIT 0; `git diff --check` clean. — **STOP AT OWNER GATE**: all 5 slices hosted-green + 3 reviews PASS covering all 4 concerns. Awaiting owner auth for backlog ACBP-P1-007→Done, PR #8 ready, squash-merge. Did NOT self-authorize; did NOT start P1-008. No live authenticated web-route acceptance performed (owner/external gate).

## ACBP-P1-007 finalization (owner-authorized, conditional)
- 2026-07-21 — Owner authorized conditional finalization. Gate PASS (root, branch, local==origin `fc296b7`, tree clean, PR #8 OPEN/DRAFT/MERGEABLE/CLEAN head==HEAD base main, CI 29866466962 on exact HEAD success, migrations-vs-main diff empty, exactly 3 SECURITY DEFINER fns, P1-007 Planned, P1-008 Planned, 3 reviews PASS). **Step 2 — profile-endpoint acceptance closed:** added the smallest focused tests (test-only; no production change): profile-request.test.ts endpoint negatives (read missing-internal-user→not_found; update deleted→forbidden + missing-internal-user→not_found, no write) and a real-PG CORE role-denial test (accounts.integration.test.ts) — a NON-owner active member (role forced to viewer via superuser seed) is denied `profile:read` AND `profile:update`, opaquely (undefined) + audited (non-PII authz.denied), no write; direct core call (not via route) still enforces. HTTP owner-vs-viewer is unreachable by product construction (profile routes resolve the caller's OWN single-owner account) → role negative proven at the core seam, NOT from the pure matrix test alone. Existing coverage cited: profile-http.test.ts drops forged body role/account keys + maps forbidden→403/not_found→404. Verified: typecheck/lint/secrets/encoding/boundaries/boundary-tests EXIT 0; full suite 508 pass / 0 fail / 152 integration skipped-locally; `pnpm audit --audit-level high` EXIT 0; `next build` EXIT 0 (run LOCALLY — CI does not run next build); `git diff --check` clean. Committed `d6bab32`, pushed; hosted CI run 29867326118 GREEN (frozen-install + preflight zero-skip + aggregate + High+ audit) — the new core role-denial test ran zero-skip.
- 2026-07-21 — Step 4: backlog ACBP-P1-007 flipped **Planned→Done** (that row only; P1-008/009/010 remain Planned). PROJECT-STATE status refreshed. Proceeding to PR #8 ready → squash-merge → main CI → branch cleanup.
- 2026-07-21 — P1-007 FINALIZED & MERGED. Squash `340f9c2` (PR #8 MERGED), main CI 29867863044 GREEN (frozen-install + preflight zero-skip + aggregate + High+ audit). Feature branch deleted local+remote. Local main==origin/main==340f9c2, tree clean.

## ACBP-P1-008 — Audit event foundation
- 2026-07-22 — Owner authorized "Begin ACBP-P1-008 autonomously." Repo gate PASS (root, branch main, local==origin `340f9c2`, tree clean, PR #8 merged, P1-007 Done, main CI 29867863044 success, no P1-008 branch/PR/worktree/stash, P1-008 Ready/Planned & not started, P1-009/010 Planned, migrations 0005/0006 + exactly 3 SECURITY DEFINER fns intact, runtime uses loadAppDatabaseConfig()=DATABASE_APP_URL). Canonical scoping (BACKLOG row: "Append-only audit store; in-tx write pattern for high-risk ops"; ACT-002/NFR-008; ADR-015; EVENT-CATALOG envelope; invariant 11; audit.write contract). Two discovery subagents produced a complete event inventory (no durable store exists; Logger is stdout-only; ~20 interim events, all "durable store is P1-008") + canonical doc synthesis. Surfaced the audit-store scope/shape as a genuine owner gate (ADR-015 one-store-C/A/G vs account-only RLS; global/denial/bootstrap handling would need a prohibited privileged path or unresolved isolation model). **Owner chose Option A** (account-scoped first cut) → recorded **CDR-014**. Branch `p1-008-audit-event-foundation` created from `340f9c2`. — next — commit slice-1 contract, open draft PR.
- 2026-07-22 — Slice 1 (audit contract in `@acbp/contracts` `audit/`: server ULID `generateEventId`/`isUlid`; `AuditActorType`(user|worker|system|admin), `AuditOutcome`, closed `AUDIT_EVENTS` registry [membership.invited/revoked, schema v1] + `isAuditEventName` deny-unregistered; `boundedMetadata` flat-scalars-only validator [rejects nesting/arrays/Error/null/bigint/symbol/non-finite/over-long/too-many/over-large; key-name-only errors, never the value]; typed factories `membershipInvited`/`membershipRevoked` — no free-form events). 17 unit tests GREEN; static gate typecheck/lint/secrets/encoding/boundaries all EXIT 0. Committed `d585c7f`, pushed; draft **PR #9** opened; hosted CI run 29872934993 GREEN (success). — next — Slice 2 migration + AuditWriter.
- 2026-07-22 — Slice 2 (migration `0007_audit_events`: append-only table [ULID text PK, jsonb payload, actor_type/outcome/schema_version checks, account+time index, partial-unique idempotency_key], ENABLE+FORCE RLS, INSERT+SELECT policies keyed to app.current_account fail-closed, grant INSERT+SELECT only to acbp_app [no UPDATE/DELETE/TRUNCATE], no FK on account_id [redacted trace survives deletion], no SECURITY DEFINER fn added, BYPASSRLS to no one; `AuditEventsTable` schema type Updateable=never; `writeAuditEvent` appends under the caller's AccountScope in-tx with account/actor/event_id/occurred_at bound server-side). Real-PG suite `audit.integration.test.ts` (server-bound fields + cross-account isolation; UPDATE/DELETE/TRUNCATE denied [invariant 11]; fail-closed no-GUC insert+select; forged-account WITH CHECK; catalog/ACL INSERT+SELECT-only + FORCE RLS + exactly 2 policies + acbp_app no-BYPASSRLS/no-superuser; idempotency-key unique). typecheck/lint/secrets/encoding/boundaries + boundary tests EXIT 0; full local suite 525 pass / 0 fail / 158 integration skipped-locally. — next — commit/push Slice 2, verify hosted CI (runs the audit suite zero-skip), then Slice 3 in-tx integration + completeness.
- 2026-07-22 — Slice 2 CI surfaced 3 orthogonal issues, all fixed: (a) `aeb9956` — 14 integration suites that reset migration history now drop `audit_events` (was `relation already exists` on re-apply) + `database.integration` table guard updated; (b) `9a56c08` — catalog test used non-existent `pg_roles.rolsuperuser` -> `rolsuper`; (c) `2a424ea` — newly-published High CVE in transitive `sharp` (<0.35.0 via Next.js; GHSA-f88m-g3jw-g9cj) failed the repo-wide High+ audit gate -> `pnpm-workspace.yaml` override to >=0.35.0 (sharp is an UNUSED optional dep; apps/web has no next/image; `next build` verified LOCALLY — CROSS-CUTTING supply-chain remediation, NOT a P1-008 feature). Exact-head CI 29874548459 GREEN.
- 2026-07-22 — Slice 3 (same-tx producer integration). `inviteMember`/`revokeMember` write `membership.invited`/`membership.revoked` via `writeAuditEvent` INSIDE `runInAccountScope` (SAME tx + AccountScope; account/actor/event_id/occurred_at bound server-side; payload = role only, never token/email). Revoke concurrency-safe: `MembershipRepository.revokeIfActive` (conditional `WHERE status='active'`), `RevokeResult.changed` gates the audit so idempotent no-op / last-owner / missing / cross-account / rolled-back mutation write NO success audit. Test seam `auditWriter?` (production never sets it). Completeness registry `audit/audit-operations.ts` (`AUDITED_OPERATIONS` op->event, compile-exhaustive `factoryFor`, no-orphan test). Real-PG tests: exactly-one-row producers (server-bound, PII-free); audit-write failure rolls back mutation (fail-closed); write-then-throw rolls BOTH back (atomicity); denials/no-op/cross-account write no success audit; concurrent revoke -> exactly one audit. Operational logger retained (separate best-effort; not redirected to audit_events). Static gate + boundary tests EXIT 0; full local suite 528 pass / 0 fail / 167 integration skipped-locally. Committed `26197f9`, pushed; hosted CI 29875453979 GREEN (frozen install + preflight zero-skip + aggregate incl. audit atomicity + High+ audit). — next — Slice 4.
- 2026-07-22 — Slice 4 (adversarial + docs). Extended `audit.integration.test.ts`: ALTER/DROP/TRUNCATE/DISABLE-RLS/NO-FORCE-RLS denied for acbp_app; DROP/ALTER audit policies + permissive `FOR ALL` policy denied; self-`GRANT UPDATE`/`SET ROLE`/`CREATE ROLE` denied + UPDATE still denied after; pooled-context isolation (sequential A→B no leak; concurrent A/B each see only own rows); catalog (audit_events owner != acbp_app; acbp_app NOCREATEROLE/NOINHERIT/NOCREATEDB + member of no role; exactly 2 policies INSERT+SELECT; exactly 3 SECURITY DEFINER fns unchanged). Docs: `docs/architecture/AUDIT.md` (logging-vs-durable-audit table, data model + RLS/immutability, unforgeable same-tx write path, implemented [membership.invited/revoked] + deferred [denials/bootstrap/global/company/outbox/read-API/retention] lists, recovery, supply-chain sharp note); EVENT-CATALOG implementation-status note; TENANCY + AUTHORIZATION P1-008 pointers updated. typecheck/lint/secrets/encoding/boundaries/boundary-tests EXIT 0; full local suite 528 pass / 0 fail / 173 integration skipped-locally. Committed `ea4fa31`; escalation test fixed `cfe6755` (a non-owner self-GRANT is a no-op warning not an error); CI 29876173730 GREEN.
- 2026-07-22 — THREE independent reviews returned. (1) audit-integrity/RLS-ACL/append-only: **PASS** (0 blk/0 maj/2 min/1 info) — immutability/RLS/forgery-resistance/grants/SECURITY-DEFINER(=3, 0005-0006 byte-unchanged)/PII/ULID all correct. (2) tx-atomicity/producer-completeness: **PASS** (0/0/1/4) — same-tx fail-closed writes; success audit only on real transitions; structural completeness. (3) architecture/scope+sharp: **PASS** (0/0/0/5) — boundaries clean, only the account-scoped first cut, sharp override correctly isolated + accurate claims, no owner-gate violation. Applied fixes: per-account idempotency unique index `(account_id, idempotency_key)`; bounded correlation/causation/idempotency ctx ids (≤200) in the writer; automated compile-exhaustive per-operation producer completeness test (`test.each` over `AUDITED_OPERATION_IDS` with a `Record<AuditedOperation,...>` driver → a future op missing a driver fails compile, a use case that loses its write fails CI); corrected ULID entropy comment. Documented accepted residuals in AUDIT.md (name/subject_type app-layer-only registry; actor_type caller-supplied but DB-CHECK-bound + actor_id server-bound; log-before-commit best-effort; no live web-route acceptance). Out-of-scope P1-004 last-owner concurrency race flagged as a SEPARATE task (not fixed on this branch). typecheck/lint/encoding/boundaries EXIT 0. — **STOP AT OWNER GATE**: awaiting owner auth for backlog ACBP-P1-008→Done, PR #9 ready, squash-merge. Did NOT self-authorize; did NOT start P1-009.
- 2026-07-22 — Final review-fix commit `fdc442e` pushed; exact-final-HEAD CI 29876769730 GREEN (frozen install + preflight zero-skip + aggregate incl. audit atomicity/adversarial/catalog/migration-lifecycle + High+ audit). PR #9 body refreshed. (Local lint had a spurious failure from a stray `.claude/worktrees` copy — de-registered via `git worktree prune`; lint clean; not in git / absent on CI.)
- 2026-07-22 — OWNER-AUTHORIZED P1-008 finalization: backlog ACBP-P1-008 flipped **Planned→Done** (that row only; P1-009/010 remain Planned). Completion boundary reconfirmed (durable set = membership.invited/revoked only; no audit update/delete method; no deferred event registered; no 4th SECURITY DEFINER; 0005/0006 byte-unchanged; runtime on DATABASE_APP_URL; acbp_app NOBYPASSRLS). Sharp override preserved. P1-004 last-owner race remains flagged as task_6619240e (running in a separate session; NOT fixed here). Proceeding to PR #9 ready → squash-merge → main CI → branch cleanup.
- 2026-07-22 — P1-008 FINALIZED & MERGED (squash `8afb8f0`, PR #9; main CI 29877371936 GREEN; branch deleted). P1-009 discovery surfaced a company-vs-account scope conflict (feed is company-scoped per ACT-001/ADR-015/diagram-11 but audit is account-scoped + companies are P1-010) → owner chose Option A (reorder P1-010 before P1-009).

## ACBP-P1-010 — Company lifecycle
- 2026-07-22 — P1-010 discovery + owner decisions (CDR-015): separate `company_memberships` table (account membership foundation untouched); company creation under the existing AccountScope with an account-keyed policy (NO 4th SECURITY DEFINER function); deactivate deferred; 4 durable company.* events (created/updated/paused/resumed); audit_events nullable `company_id` expand; immutable-revision company profile. Owner authorized "Begin ACBP-P1-010 implementation." Gate PASS (root, branch main, local==origin `8afb8f0`, tree clean, main CI 29877371936 success, PR #10 [P1-004 race fix] OPEN/unmerged base main — untouched, no P1-010/P1-009 branch, P1-010 Planned, 3 SECURITY DEFINER fns, runtime DATABASE_APP_URL). Branch `p1-010-company-lifecycle` created from `8afb8f0`. Planning: CDR-015 + P1-009 dependency correction (Dependencies → ACBP-P1-008;ACBP-P1-010; P1-009 stays Planned, acceptance unchanged) + agent records. Committed `f0d6892`, pushed; draft **PR #11** opened. — next — Slice 1.
- 2026-07-22 — Slice 1 (company contracts in `@acbp/contracts`). `company/company.ts`: `CompanyStatus` (draft/onboarding/active/paused — deactivate deferred) + guard; `toDisplayStatus` (draft/onboarding→provisioning, active→active, paused→paused, unknown→'unknown' per COMP-008 "never a fabricated healthy state"); `CompanyCreationMode` (own_idea/platform_suggested/existing_business, COMP-001) + guard; `INITIAL_COMPANY_STATUS='draft'`; `isLegalCompanyTransition` (WORKFLOW §1 subset draft→onboarding→active⇄paused). Extended authz: 6 company actions in AUTHZ_ACTIONS + POLICY (company:create→owner [account role]; read/status→owner|viewer; rename/pause/resume→owner [company role]) + authz.test EXPECTED matrix. Note: the typed company.* audit factories + registry + completeness drivers land in Slice 3 with their in-tx producers (keeps the no-orphan + exhaustive-driver completeness invariant green at every commit). 41 unit tests; static gate all EXIT 0; full local suite 549 pass / 0 fail. Committed `1ba19cc`, pushed; hosted CI green on that SHA. — next — Slice 2.
- 2026-07-22 — Slice 2 (migration `0008_companies.ts` + schema types + RLS). Additive expand: `companies` (C-root: id/account_id immutable, status draft/onboarding/active/paused, creation_mode; account-keyed INSERT, account-scoped SELECT, dual-keyed UPDATE), `company_profiles` (immutable revisions PK(company_id,version); dual-keyed; INSERT+SELECT only), `company_memberships` (SEPARATE table; dual-keyed INSERT + self-branch SELECT for the no-SECURITY-DEFINER resolver; active-unique `(company_id,member_user_id) WHERE active`; INSERT+SELECT only), and `audit_events.company_id` nullable + `audit_events_insert/select` ALTERed to DUAL-SCOPE (account event company_id NULL → account-visible; company event → BOTH match). FORCE RLS on all three; grants least-privilege; NO 4th SECURITY DEFINER; `memberships` UNTOUCHED (company_id stays FK-less hook). schema.ts: CompaniesTable/CompanyProfilesTable/CompanyMembershipsTable + audit company_id + row types. Added `company.integration.test.ts` (real-PG: account-keyed create, account-scoped read, dual-keyed update + immutable id/account_id, append-only profiles, self-branch membership resolution, active-unique, audit dual-scope insert+read, FORCE-RLS/grant/allowlist catalog). Added the 3 new tables (child-first) to all 14 integration drop-lists. Static gate all EXIT 0; full local 549 pass, +9 company integration tests skip locally (run zero-skip on CI). Committed `3ee90a0`, pushed. CI on `3ee90a0` FAILED on ONE stale cross-ticket guard (`database.integration.test.ts` "P1-002..P1-008 tables" asserted `companies` must NOT exist) — the company RLS suite itself PASSED 9/9 (design correct). Fixed the guard (now P1-002..P1-010, asserts the 3 company tables exist), commit `04094af`; CI on `04094af` GREEN (company RLS ran zero-skip). — Slice 2 hosted-green.
- 2026-07-22 — Slice 3 (resolver + CompanyScope mint + same-tx creation bootstrap + 3 modes). @acbp/database: `elevateToCompanyScope(accountScope, companyId)` — verifies the company is visible under the account (account-scoped SELECT, fail-closed), sets `app.current_company` SET LOCAL on the SAME tx, mints a branded TenantScope (no 2nd tx/connection; not a public scope-minter); `company-repositories.ts` (CompanyRepository insert/findById/updateStatus, CompanyProfileRepository insert/currentRevision, CompanyMembershipRepository insert/findActiveForUser); `writeAuditEvent` widened to `AccountScope | TenantScope` (`AuditScope`) stamping `company_id` from a company scope (null for account scope). @acbp/contracts: 4 `company.*` audit events + factories (companyCreated/Updated/Paused/Resumed; changed-field NAMES only; no PII). @acbp/core: audit registry PARTITIONED (membership vs company subsets + compile guard that the partition covers exactly AuditedOperation; factoryFor now `(subjectId)=>AuditEvent`, exhaustive over all 6; members producer driver retyped to the membership subset); `company-context-resolver.ts` `runInCompanyScope` (account resolve → active company-membership self-lookup → elevate; deny-by-default coarse `company_not_specified`/`company_access_denied`, interim `company.context_denied` non-PII audit); `company-service.ts` `createCompany` (validate → owner-gated `company:create` from ACCOUNT role → ONE tx: insert company [account-keyed] → elevate → profile v1 → owner company membership → `company.created` in-tx audit → all-or-nothing). Tests: contracts audit (+4 factory tests), core company-service unit (6, validation), real-PG company create/resolve integration (10: atomic create, 3 modes, viewer/non-member forbidden, validation, audit-fail rollback, resolver ran/deny paths). Static gate + `pnpm run check` all EXIT 0; full local 559 pass, +10 integration skip locally (run zero-skip on CI). NOTE: company.update/pause/resume are registered+mapped now (no-orphan invariant green) but their LIVE producer drivers land in Slice 4 with the lifecycle use cases; `company.created` is proven by this bootstrap test. Committed `5dbacdd`, pushed; hosted CI GREEN (company create/resolve suite ran zero-skip). — Slice 3 hosted-green.
- 2026-07-22 — Slice 4 (lifecycle: read/rename/pause/resume + pause-pickup rig + completeness closure). @acbp/contracts: `canPickUpAutonomousWork(status)` = status==='active' (COMP-006/invariant-16 groundwork: only active companies pick up NEW autonomous work; paused/draft/onboarding block). @acbp/core `company-lifecycle.ts`: `getCompany` (owner|viewer read → truthful `toDisplayStatus`, COMP-008), `renameCompany` (owner-only `company:rename` → new immutable profile revision version+1 + `company.updated` [changed-field NAMES]; identical values = idempotent no-op, no version/audit), `pauseCompany`/`resumeCompany` (owner-only; `isLegalCompanyTransition`-guarded active⇄paused; `company.paused`/`company.resumed` in-tx; illegal transition → `invalid_transition`, not applied). All run under `runInCompanyScope` (company role → ADR-022 role check; RLS-confined; audit same-tx). Tests: contracts (+canPickUpAutonomousWork), core `validateProfileEdit` unit (3), real-PG lifecycle integration (+8: read/displayStatus, owner-rename+company-viewer-forbidden, rename no-op, pause⇄resume+audits, illegal transitions rejected, viewer-cannot-pause, pause-blocks-pickup rig) + the EXHAUSTIVE `Record<CompanyAuditedOperation, ()=>Promise<void>>` producer driver (4) — all 4 company producers now LIVE, closing the completeness loop (create/update/pause/resume each write exactly their mapped event in-tx). Note: onboarding→active provisioning is P1-012; pause/resume tests seed the `active` state directly (superuser) since owner-driven pause/resume are the in-scope transitions. Static gate + `pnpm run check` all EXIT 0; full local 564 pass, +11 integration skip locally (run zero-skip on CI). Committed `921c8db`, pushed; hosted CI GREEN (lifecycle + company producer drivers ran zero-skip). — Slice 4 hosted-green.
- 2026-07-22 — Slice 5 (HTTP API boundary — canonical: backlog Interface "companies API"; P1-004 precedent). @acbp/core composition `clerk-identity.ts`: `ClerkIdentityRuntime` gains createCompany/getCompany/renameCompany/pauseCompany/resumeCompany (bound to the shared restricted-role client). apps/web: `server/companies/companies-request.ts` (ADR-022 flow — resolve SERVER-VERIFIED identity → internal user → caller's OWN account; companyId is a request SELECTOR validated by @acbp/core membership; acting user + account server-resolved, never request-supplied; owner-only enforced in core) + `companies-http.ts` (bounded 16KB JSON parse, keys allowlisted, opaque 403, `toCompaniesResponse`); routes `POST /api/companies`, `GET|PATCH /api/companies/[companyId]`, `POST /api/companies/[companyId]/pause|resume` (Next nodejs/force-dynamic, params awaited). Tests: companies-request unit (server-resolved authority + endpoint×principal negative matrix: unauth/unverified/deleted/core-denied → refused) + companies-http (parse caps/415/413/400, key allowlist drops `status`/`accountId`, response-code table, opaque-denial). Boundary check clean (apps/web imports domain only via @acbp/core; no @acbp/database/@acbp/adapters). Static gate + `pnpm run check` all EXIT 0; full local 591 pass. Committed `b200985`, pushed; hosted CI GREEN (Next build of the new routes + apps/web company tests). — Slice 5 hosted-green.
- 2026-07-22 — Slice 6 (adversarial RLS + docs). Appended a cross-tenant/tamper adversarial block to the database `company.integration.test.ts`: forged `current_company` at another account's company reveals/mutates nothing (companies account-scoped; profiles dual-key + EXISTS(account) → invisible; cross-account profile insert rejected); joins/CTEs/subqueries cannot leak another company's rows; no-company-context + empty/malformed company GUC fail closed (0 rows, no cast error); audit company-event forgery (company_id ≠ current_company) rejected; restricted role cannot ALTER/DISABLE/UN-FORCE RLS / DROP the 3 company tables / drop-or-create company policies; DELETE denied at grant level on all 3. Docs: updated `docs/architecture/AUDIT.md` (audit README) with the "Extended in P1-010" section (nullable company_id, dual-scope policies, the 4 durable company.* events, partitioned completeness) + factory list + flipped the deferred "company-scoped audit" line to implemented (CDR-015 §Profile-versioning already documents the versioned-profile "Docs" deliverable). Static gate (typecheck/lint/encoding/secrets) all EXIT 0. Committed `cb5ed15`, pushed; hosted CI GREEN (**801 passed / 0 skipped**, preflight zero-skip). — Slice 6 hosted-green.
- 2026-07-22 — Review pass (3 independent reviews over main..HEAD; coverage matrix confirmed all 16 required areas covered → no extra reviewer needed). Scope/charter: FULLY COMPLIANT, zero deviations. Security: no CRITICAL/HIGH. Correctness: no High defect. Findings fixed: (MEDIUM) concurrent profile rename now bounded-retries the (company_id,version) race → true last-write-wins, coarse `conflict`→409 after N attempts instead of 500; (LOW) pause/resume assert the SPECIFIC expected prior status (active→pause, paused→resume) so onboarding→active can't be forced via resume + audit never mislabeled; (LOW/security) removed caller free-text `reason` from pause/resume path (no body; nothing user-supplied persisted to the immutable audit — data minimization); (LOW) rename accepts explicit `null` description as clear (parity with create); (LOW) transition checks updateStatus affected-row count before auditing (no phantom-transition audit); (coverage) added forced-audit-failure rollback tests for rename+pause + null-desc unit. Accepted residuals: getCompany status/displayStatus divergence (unreachable behind DB CHECK, fail-closed) + thrown transient DB error → bare 500 (pre-existing non-leaking pattern shared with membership ops). Independent re-verification (from code/PostgreSQL catalogs + CI): create bootstrap = one restricted acbp_app tx under AccountScope, CompanyScope minted only from the authoritative inserted row, app.current_company transaction-local, all rows same-tx, no 4th SECURITY DEFINER, no owner client reachable; resolver = selector-only companyId, active account+company membership required, cross-account company opaque via the self-branch; company_memberships separate + company-scoped active-unique + account foundation empty-diff. Docs updated: CDR-015 (review outcomes + residuals), AUDIT.md (reason removal + specific-from), TENANCY.md (P1-010 company RLS now implemented, was stale "reserved/never-set"). Committed `7689fa0` (review fixes+docs) + `23eba7c` (test-isolation fix for the rollback assertions); hosted CI on `23eba7c` GREEN (**803 passed / 0 skipped**). — Owner gate reached.
- 2026-07-22 — Owner-authorized controlled finalization. Gate reconfirmed (branch/HEAD `23eba7c`/tree-clean/PR#11 CLEAN/exact-head CI success/migrations 0001-0007 unchanged/CDR-015 present/P1-009 Planned/no P1-009 artifacts/exactly-3 SECURITY DEFINER via real-PG catalog test). PR #10 rechecked: OPEN/unmerged, origin/main still `8afb8f0` (= merge base) → no reconciliation. Completion boundary reconfirmed (no deferred item implemented). Set ACBP-P1-010 backlog Planned→Done (scoped line edit; P1-009 stays Planned, deps unchanged). — next — commit backlog+state, exact finalization-SHA CI green, mark PR #11 ready, final PR#10/main recheck, squash-merge "ACBP-P1-010: Company lifecycle", verify exact-main CI, delete branch. Then STOP (no P1-009).

## ACBP-P1-009 — Activity event foundation (started 2026-07-22)
- Gate: main @ 093ec3f (P1-010 squash, exact-main CI 29935591570 green 803/0-skip); tree clean; exclusive worktree; PR #10 OPEN/untouched. Canonical discovery reconciled (ADR-015, diagram-11, DATA-ARCHITECTURE, EVENT-CATALOG, API-CONTRACTS, AUDIT.md, CDR-014/015); greenfield (no outbox/activity/projector/worker; apps/worker = scaffold + reconcile CLI). Owner decisions received -> CDR-016. Branch p1-009-activity-event-foundation from 093ec3f. --- next --- planning commit (CDR-016 + records), draft PR, then Slice 1 (activity contracts + activity:read authz) under TDD; each slice hosted-green before continuing. Stop at a new owner decision or the final owner gate.
- 2026-07-22 — Slice 1 (activity contracts + activity:read authz). @acbp/contracts activity module: ACTIVITY_TYPES (the 4 company events; isProjectableActivity gates projection — account/Logger/unknown names excluded), ExecutionState (executed for all four; ACT-003), activityDisplayPayload (re-bounds audit metadata; no correlation/causation/raw), ActivityEventDTO/ActivityPage (redacted; honest as_of), keyset cursor (opaque URL-safe JSON via encodeURIComponent — neutral, no Buffer; versioned + company-bound; fail-closed decode, never throws), clampActivityPageSize (default 25/max 100). authz: added activity:read (owner|viewer) + EXPECTED matrix. Unit tests. Static gate all EXIT 0; full local 603 pass. --- next --- commit/push Slice 1, verify hosted CI, then Slice 2 (migration 0009 activity_events + dual-keyed FORCE RLS).
- 2026-07-22 — Slice 2 (migration 0009 activity_events + dual-keyed RLS). Append-only projection table: event_id PK = source audit event_id (idempotent/traceable/rebuildable), account_id/company_id NOT NULL (no FK, trace survives), activity_type CHECK (4 company events only), occurred_at/actor/subject/payload(redacted jsonb)/schema_version/projected_at. FORCE RLS dual-keyed (current_account AND current_company, fail-closed); grants INSERT+SELECT only (no UPDATE/DELETE); keyset index (company_id, occurred_at DESC, event_id DESC). schema.ts ActivityEventsTable (never-on-update) + row types + index export. Added activity_events to all 16 integration drop-lists + the P1-002..P1-010 table guard. Real-PG activity.integration.test.ts: dual-key isolation, forged/absent-context reject, fail-closed read, append-only UPDATE/DELETE/TRUNCATE denied, idempotent-by-event_id, type CHECK, keyset ordering, FORCE-RLS/grant/allowlist catalog. Static gate all EXIT 0; full local 603 pass, +8 activity integration skip locally (run zero-skip CI). --- next --- commit/push Slice 2, verify CI, then Slice 3 (synchronous in-tx projection writer wired into the 4 lifecycle use cases).
- 2026-07-22 — Slice 3 (synchronous in-tx activity projection). @acbp/database projectCompanyActivity(scope, event, auditEventId): only the 4 company events project (others safe no-op); reads the AUTHORITATIVE audit row in-tx for occurred_at/actor (== a rebuild-from-audit; deterministic), inserts the redacted activity row keyed by the source audit event_id under the CompanyScope; throws on failure -> caller tx rolls back. Wired into all 4 lifecycle use cases (createCompany + rename/pause/resume via the transition helper): capture the audit event id, project in the SAME CompanyScope transaction after the audit write. Added activityWriter test seam (mirrors auditWriter). Slice 2 CI (activity_events RLS) hosted-green. Real-PG tests: each op projects exactly one activity row (traceable to its audit id, redacted payload, occurred_at == audit); activity-projection failure rolls back the whole op (mutation + audit + activity) for create + pause. Static gate all EXIT 0; full local 603 pass. --- next --- commit/push Slice 3, verify CI, then Slice 4 (getCompanyActivity read: CompanyScope + activity:read + keyset pagination + DTO + honest as_of).
- 2026-07-22 — Slice 4 (activity feed READ use case). @acbp/database ActivityFeedRepository.listByCompany (company-scoped keyset over activity_events, occurred_at DESC/event_id DESC matching activity_events_feed_idx; no OFFSET). @acbp/core getCompanyActivity: runInCompanyScope -> activity:read (owner|viewer) -> decode+validate opaque company-bound cursor BEFORE scope (invalid -> invalid_cursor, no DB hit) -> fetch limit+1 keyset -> redacted ActivityEventDTO map -> nextCursor + honest asOf (newest returned occurred_at; null when empty). Note: occurred_at is millisecond-precise (round-trips through a JS Date in the projector), so cursor round-trips are exact (no keyset skip/dup). Slice 3 CI hosted-green. Real-PG tests: newest-first + as_of, empty feed shape, stable keyset pagination (3 pages of 5; no dup/omission; order == authoritative), page-size clamp, viewer-ok/non-member-forbidden authz, cross-company isolation (no membership -> forbidden; A feed excludes B rows), cursor validation (malformed + another-company cursor -> invalid_cursor). Static gate all EXIT 0; full local 603 pass. --- next --- commit/push Slice 4, verify CI, then Slice 5 (API GET /api/companies/[companyId]/activity + request/http + runtime wiring + web build).
- 2026-07-22 — Slice 5 (activity feed API + runtime wiring). @acbp/core composition getCompanyActivity. apps/web: companies-request getCompanyActivityForRequest (server-resolved account+actor; companyId membership-validated selector; raw cursor/limit forwarded, domain validates/clamps) + CompaniesRequestResult activity/invalid_cursor; companies-http activity->200 {items,nextCursor,asOf} / invalid_cursor->400; route GET /api/companies/[companyId]/activity (reads cursor/limit from query string; nodejs/force-dynamic). Slice 4 CI had 1 test-only failure (viewer lacked an account membership) — fixed. Tests: companies-request activity (server-resolved authority + cursor/limit forwarding + forbidden/invalid_cursor/unauth) + companies-http mapping. Web build EXIT 0 (all 5 company routes incl. /activity compiled). Static gate all EXIT 0; full local 608 pass. --- next --- commit/push Slice 5, verify CI, then Slice 6 (adversarial cursor/cross-company + docs + independent reviews).
- 2026-07-22 — Slice 6 (adversarial scope test + docs). Added a real-PG scope/adversarial test: the feed renders company events ONLY (an account-level membership.invited written via inviteMember never appears; activity_events globally contains only company.* rows with non-null company_id); DTO redaction (details has only the whitelisted keys; no correlation/payload). Slice 5 CI hosted-green. Docs: new docs/architecture/ACTIVITY.md (activity README — synchronous projection model, activity_events schema, taxonomy, DTO/redaction, activity:read authz, dual-keyed RLS, keyset cursor, as-of/lag honesty, rebuild, API scope, deferred SSE/outbox); AUTHORIZATION.md (activity:read owner|viewer); AUDIT.md (activity feed now implemented in P1-009, outbox still deferred). Static gate all EXIT 0; full local 608 pass. --- next --- commit/push Slice 6, verify CI, run 3 independent reviews (tenant/authz, projection/atomicity, pagination/cursor), address findings, then STOP at the owner gate.
- 2026-07-23 — Owner-mandated review-checkpoint corrections (pre-review). (1) CURSOR: replaced the URL-safe-JSON cursor with a genuine unpadded base64url codec (pure-ECMAScript, ASCII-payload; URL-safe alphabet, no +/=; strict decode rejects bad alphabet/length/non-ASCII/non-JSON/unknown-version/missing-fields/over-long/invalid-ISO/invalid-ULID); cursor now bound to ACCOUNT+COMPANY and carries the exclusive keyset-after position PLUS the immutable first-page traversal upper bound — later pages apply both predicates, so events inserted after page 1 are excluded from that traversal (fresh traversal includes them; sourceThrough constant). (2) DTO tightened: items expose only id/type/occurredAt/state:executed/actorType/per-type allowlisted summary (created->creation_mode; updated->changed_fields; paused/resumed EMPTY — stored reason-like keys never emitted); no actor internal id/account_id/company_id/raw payload/correlation; allowlist applied at projection AND re-applied at DTO mapping; out-of-taxonomy stored type dropped, never emitted raw. (3) Honest metadata: projectionMode:synchronous; asOf = PostgreSQL now() of the feed query tx (never app clock); sourceThrough; lagSeconds:0. (4) BACKFILL added to migration 0009 (pre-RLS, migration connection): the 4 company events with company_id NOT NULL; event_id preserved; occurred_at date_trunc millis (== runtime JS-Date grid; live==backfill==rebuild); per-type redacted payload; account/Logger/unknown excluded; ON CONFLICT DO NOTHING idempotent; down/up deterministic. (5) API: cursor+limit are the ONLY query params (others -> 400). Tests: full base64url/tamper matrix (contracts), backfill 7-point real-PG protocol (through-0008 seed -> 0009 -> redaction/ids/millis -> rerun idempotent -> down/up deterministic), EXPLAIN query-plan proof (activity_events_feed_idx usable, no seq scan, bounded), traversal-stability integration, updated DTO/page assertions. Static gates + check + web build all EXIT 0; full local 614 pass. --- next --- commit/push, exact-head CI, re-dispatch the 3 independent reviews on the final diff.
- 2026-07-23 — Review collection + fixes. Rework CI on 99594ec failed ONLY on the repo-wide High+ dependency gate: 4 newly published High advisories in next <16.2.11 (middleware/proxy bypass; transitive via @clerk/nextjs + direct) — fixed by locking next@16.2.11 (in-range patch; audit High+ now EXIT 0; web build re-verified). All 3 re-dispatched independent reviews returned: NO CRITICAL/HIGH/MEDIUM. Fixed all Low/coverage items: (L1/F2) live projector rewritten as a single INSERT...SELECT from the authoritative audit row with date_trunc(milliseconds) — identical SQL to the 0009 backfill, eliminating the JS float/date path (live==backfill==rebuild byte-identical); live path stays loud on duplicates by design (rebuild mapping = the 0009 SQL with ON CONFLICT). (F3) 0009 now asserts loudly that the migration role has BYPASSRLS/superuser before the backfill (audit_events FORCE RLS would otherwise silently blind it). (F1) added rename projection-failure rollback proof (distinct inline path). (tie-break) added equal-timestamp page-boundary repository test. (LOW-2) asOf unreachable wall-clock fallback now throws. Docs: ACTIVITY.md residuals (cursor opaque-by-convention w/ caller-own ids; all-dropped-page fail-safe; OR-filter keyset shape; asOf=transaction read timestamp conservative; backfill precondition) + CDR-016 review-outcomes section + no-filters wording. Full gates + check + audit + web build all EXIT 0; local 614 pass. --- next --- commit/push, exact-head CI, coverage matrix, owner-gate report.
- 2026-07-23 — Owner-mandated TEMPORAL IDENTITY tightening (Step 2 of the final-gate instructions; supersedes the millisecond-grid design). activity_events.occurred_at is now copied BIT-EXACTLY from audit_events.occurred_at (sub-millisecond microseconds preserved): removed date_trunc from BOTH the 0009 backfill and the runtime projector (both are now the identical exact-copy SQL); the reader selects an exact microsecond epoch ((extract(epoch)*1000000)::bigint — exact numeric math in PG16) and serializes the canonical microsecond ISO via integer-only math (microsecondEpochToIso; no JS Date/float in any ordering path); DTO occurredAt, sourceThrough, and the cursor all carry the exact instant; keyset predicates bind the ISO via ::timestamptz casts (PG parses its own precision exactly — sub-millisecond ordering round-trips with no skip/dup); cursor timestamp validation tightened to a STRICT microsecond-ISO shape (also closes the Date.parse-permissiveness nit: 2026/Jan-1-2026 now rejected); asOf likewise exact-epoch-sourced. New tests: real-PG sub-millisecond identity (two rows in one millisecond stored exactly + keyset-ordered exactly + exact-epoch projection), backfill exact-preservation asserted IN SQL (act.occurred_at = au.occurred_at, .123456 preserved), core all-rows SQL identity join (no IS DISTINCT FROM row), 0009 BYPASSRLS-guard negative test (the DO-block raises for acbp_app), strict-timestamp + microsecondEpochToIso contract tests. Docs (ACTIVITY.md/CDR-016) flipped from millisecond-grid to exact-preservation language. Prior head da95f31 CI was SUCCESS (recorded); this rework produces the new final candidate head. Local 617 pass.
- 2026-07-23 — Owner-authorized controlled finalization of ACBP-P1-009. Gate reconfirmed live (branch/HEAD be9e9f1 == origin/tree clean/PR#12 DRAFT CLEAN head==HEAD/exact-head CI 29970704493 success 852-0skip/only 0009 changed in migrations/CDR-016 present/P1-011 unstarted/reviews 3-of-3 complete all findings resolved). PR#10 rechecked: OPEN/unmerged, origin/main 093ec3f == merge base -> untouched, no reconciliation. Completion boundary + temporal identity + 0009 security + cursor/honesty + privacy/authz + dependency remediation all affirmed against this exact HEAD (verified this session; code unchanged). Set ACBP-P1-009 backlog Planned->Done (scoped line edit; deps unchanged; P1-010 Done; P1-011 Planned). --- next --- commit backlog+state, exact finalization-SHA CI green, mark PR#12 ready, final main/PR#10 recheck, squash-merge, exact-main CI, branch cleanup, STOP (no P1-011).

## ACBP-P1-011 — Company switching and portfolio (started 2026-07-23)
- Gate: main @ e99b0b3 (P1-009 squash; exact-main CI 29971233314 green 852/0-skip); tree clean; exclusive worktree; PR #10 OPEN/untouched; P1-011/P1-012 Planned, no artifacts; migrations 0001-0009 intact. Discovery brief delivered; owner returned 19 explicit decisions -> CDR-017. Branch p1-011-company-switching-portfolio from e99b0b3. --- next --- planning commit (CDR-017 + records), draft PR, then Slice 1 (portfolio contracts + portfolio:read) under TDD; each slice hosted-green. Stop at a new owner decision or the final owner gate.
- 2026-07-23 - Slices 1-5 implemented + hosted-green on exact pushed commits: Slice 1 contracts/codec/portfolio:read 3e0834a (CI 29972673530); Slice 2 PortfolioRepository + real-PG isolation/keyset c936ba1 (CI 29973029939); Slice 3 getCompanyPortfolio sequential fresh-scope enrichment + stale-drop 398cbdc (CI 29973431573); Slice 4 GET /api/companies + runtime wiring + next build 872edce (CI 29973681871); Slice 5 switch-isolation/concurrency/GUC-cleanup 6ce59b2 (CI 29973847984, 897 passed / 0 skipped). Two independent reviews (security+tenant-isolation; scope+correctness) CLEAN - 0 Critical/High/Medium; 3 Low/Info notes dispositioned (see DECISION-LOG + P1-011-REVIEW-COVERAGE.md).
- 2026-07-23 - Slice 6 evidence: real-PG EXPLAIN query-plan suite (realistic ANALYZEd population; semantic plan assertions; natural plan recorded) + paused-company truthful-status test committed 650c424; targeted follow-up review dispatched (revoked-selection / paused / plan-suite areas). PR #10 rechecked: OPEN/unmerged; origin/main unchanged at e99b0b3 (the P1-011 base) - no reconciliation needed.
- 2026-07-23 - Targeted review (R3) returned: areas A/B/C all SOUND; no Critical/High on product code; 2 Medium + 4 Low confined to the evidence suite. All fixed or dispositioned: M-2 fixed STRUCTURALLY (mirror deleted; PortfolioRepository.buildListQuery extracted so the plan suite EXPLAINs the exact production builder - drift impossible); M-1 companies-join assertion made bitmap-tolerant; L-2 Sort Key assertions tightened to created_at DESC / .id DESC (direction per CDR-017 s8); L-3 index-name tripwire documented in-test; L-4 verbatim revoked-selection test added (succeed -> revoke -> SAME request 403 -> portfolio excludes); L-1 zero-Seq-Scan kept (load-bearing; judged stable); L-5 paused-seed-by-UPDATE accepted (read-path test; transition proven in P1-010). Register: P1-011-REVIEW-COVERAGE.md T-rows. Slice 6 coherent commit (docs + review fixes) follows; then final local gate + exact-head hosted CI.
- 2026-07-23 - Slice 6 final: docs/review-matrix/plan-evidence + targeted-review fixes committed 3010d21; full local gate green on that HEAD (frozen install; check EXIT 0; 648 passed / 254 PG-dependent skips; focused portfolio suites 61/61; audit high pass; diff --check clean; next build EXIT 0). Exact-head hosted CI 30005684391 SUCCESS: 84 files / 902 tests / 0 failed / 0 skipped (zero-skip preflight); all 4 portfolio real-PG suites executed; natural plan re-derived from the PRODUCTION builder (identical shape). PR #13 body updated; MERGEABLE/CLEAN at head 3010d21. PR #10 re-verified OPEN/unmerged; origin/main unchanged at e99b0b3. STOPPED AT OWNER GATE.
- 2026-07-23 - OWNER-AUTHORIZED P1-011 finalization begun: finalization gate revalidated live (root/branch/HEAD==origin==3010d21/tree clean/PR #13 OPEN+DRAFT+CLEAN head==HEAD/CI 30005684391 success on exact head); PR #10 OPEN/unmerged, main==merge-base e99b0b3; backlog ACBP-P1-011 Planned->Done (BACKLOG.csv row only; P1-012 stays Planned). Sequence: finalization commit -> exact-commit CI -> PR ready -> final main recheck -> squash-merge (no Co-Authored-By) -> exact-main CI -> branch delete.

## ACBP-P1-012 - Workspace provisioning (started 2026-07-23)
- Gate: main @ e7f9a53 (P1-011 squash; exact-main CI 30006648643 green 84f/902/0-skip); tree clean; exclusive worktree; PR #10 OPEN/untouched; P1-012/P1-013+ Planned, no artifacts; migrations 0001-0009 intact; 3 SECURITY DEFINER. Discovery brief delivered; owner returned 25 explicit decisions -> CDR-018. Branch p1-012-workspace-provisioning from e7f9a53. --- next --- planning commit (CDR-018 + records), draft PR, then Slice 1 (contracts + authz + audit registry) under TDD; each slice hosted-green. Stop at a new owner decision or the final owner gate.
- 2026-07-23 - P1-012 slices 1-5 implemented + hosted-green on exact pushed commits: planning 09d143d; Slice 1 contracts/authz/audit-registry 69d15fa (CI 30009862235 FAILED on orphan registered events -> fixed d0dbe2f: provisioning operations added to the completeness registry with partition + factories); Slice 2 migration 0010 + real-PG RLS/privilege/backfill suite bcd12a2 (CI 30010682316 green, incl. the d0dbe2f fix); Slice 3 atomic creation-tx integration 7e0a5d4 (CI 30011303006 green); Slice 4 executor/resume/completion + 12-test real-PG suite ae4fd5c (CI 30012231249 green); Slice 5 GET/POST provisioning APIs + web tests + prod build 5933fe3. Draft PR #14. Slice 6 in progress: architecture docs written (PROVISIONING.md new; TENANCY/AUTHORIZATION/EVENT-CATALOG/AUDIT/DATA-ARCHITECTURE/API-CONTRACTS updated); three independent reviews dispatched (security/RLS/audit, correctness/concurrency/state-machine, scope/migration/taxonomy).
- 2026-07-23 - Three independent reviews returned: NO Critical/High. R2 (correctness/concurrency) 2 Medium - concurrent duplicate resumes could (a) retry a just-failed step with no retry_requested audit and (b) double-write retry_requested with stale next_attempt. FIXED STRUCTURALLY: Phase A now returns an exact (step, fromAttempt) RetryAuthorization; a failed row executes ONLY under a matching authorization and the retry_requested audit (user actor, exact next_attempt, causation source) is written IN the executing step transaction - unauthorized failed rows halt with no mutation. Also fixed (Lows): provisioning.started system actor on the creation path; create returns active from the DTO companyStatus (not the steps flag); both routes catch -> bounded generic 500 envelope; DTO unknown-status doc; draft-pause invalid-transition coverage restored; 2 new tests (concurrent retry race proving 1-attempt/0-retry then 1-executed-retry/1-event; crash-before-activation recovery activating exactly once). Accepted (documented in PROVISIONING.md + P1-012-REVIEW-COVERAGE.md register): completion-gate narrow 500 on concurrent pause; deploy-order backfill runbook note; non-backfilled active/paused read shape; audit-invisible crashed attempts (CDR-mandated). Docs: PROVISIONING.md (new), TENANCY/AUTHORIZATION/EVENT-CATALOG/AUDIT/DATA-ARCHITECTURE/API-CONTRACTS updated. Local gate on the Slice 6 candidate: check EXIT 0; 674 passed / 277 PG-dependent skips; build EXIT 0; audit high pass; diff-check clean.
- 2026-07-23 - Slice 6 committed ea42621; exact-head hosted CI 30013972809 SUCCESS: 87 files / 951 tests / 0 failed / 0 skipped (zero-skip preflight; all provisioning suites executed). PR #14 body updated; MERGEABLE/CLEAN at head ea42621. STOPPED AT OWNER GATE.
- 2026-07-23 - OWNER-AUTHORIZED P1-012 finalization begun: gate revalidated live (root/branch/HEAD==origin==ea42621/tree clean/PR #14 OPEN+DRAFT+CLEAN head==HEAD/CI 30013972809 success on exact head/10 migrations/no p1-013 artifacts); PR #10 OPEN/unmerged, main==merge-base e7f9a53; backlog ACBP-P1-012 Planned->Done (row only; P1-013 stays Planned). Sequence: finalization commit -> exact-commit CI -> PR ready -> final main recheck -> squash-merge (no Co-Authored-By) -> exact-main CI -> branch delete.

## ACBP-P1-013 - Administrative-access foundation (started 2026-07-23)
- Gate: main @ 795227b (P1-012 squash; exact-main CI 30014863811 green 87f/951/0-skip); tree clean; exclusive worktree; PR #10 OPEN/untouched; P1-013/P1-014+ Planned, no artifacts; migrations 0001-0010 intact; 3 SECURITY DEFINER. Discovery brief delivered; owner returned 21 explicit decisions -> CDR-019. Branch p1-013-administrative-access-foundation from 795227b. --- next --- planning commit (CDR-019 + records), draft PR, then Slice 1 (contracts + registrations) under TDD; each slice hosted-green. Stop at a new owner decision or the final owner gate.
- 2026-07-23 - P1-013 slices 1-4 implemented + hosted-green on exact pushed commits: planning c48734d (CDR-019); Slice 1 contracts/authz/audit-registry 15d5adb (CI 30017194994 green; admin:tenant_read EMPTY allow-list; admin.tenant_read registered; META_MAX_VALUE_LEN 512->1024 for astral verbatim reasons). Slice 2 migration 0011 platform_admins + real-PG RLS/catalog suite + operational runbook d49e33b; its CI 30017530296 FAILED - root cause a LATENT head-pinned one-step migrateDown in the P1-012 backfill suite (popped 0011 instead of 0010; the 0009 reapply assertion had already gone vacuous when 0010 landed) -> repaired b014e4e pinning rollback targets BY NAME (Migrator.migrateTo). Slice 3 1b28db6: executeAdminCompanyRead (@acbp/database; one restricted tx: actor GUC -> fresh platform_admins self-check -> target GUCs -> relationship-verified read -> target-tenant admin.tenant_read audit -> commit; audit failure rolls back) + adminReadCompanyOverview (@acbp/core; reason-first; ONE coarse forbidden) + 8-test real-PG trust suite; a86cf92 moved the no-impersonation source guard to an always-run boundary test; CI 30018642111 SUCCESS 91 files / 980 / 0 skipped. Slice 4 0db555c: POST /api/admin/accounts/[accountId]/companies/[companyId]/read (body EXACTLY {reason}, unknown properties + all query params rejected, reason validated before any identity/DB work, one generic 400 / one opaque 403 / bounded 500, four-field DTO); ClerkIdentityRuntime.adminReadCompanyOverview; 38 web tests incl. ordering + leak canaries; prod build emits route dynamic; CI 30019840829 SUCCESS 1018/1018/0-skip.
- 2026-07-23 - P1-013 Slice 5 begun: malformed-selector UUID-shape guard added to adminReadCompanyOverview (non-UUID ids -> the SAME coarse forbidden without touching the DB; kills the uuid-cast 500 path; integration cases added incl. SQL-ish junk). Docs: ADMINISTRATIVE-ACCESS.md + BREAK-GLASS-DESIGN.md (10 mandated statements; design-only) NEW; SECURITY-ARCHITECTURE Sec.3 delivered-foundation note; AUTHORIZATION (admin:tenant_read = separate DB-backed gate, empty allow-list); TENANCY (P1-013 entry: per-transaction target GUCs, no RLS weakening); API-CONTRACTS (Admin domain row); EVENT-CATALOG (admin.tenant_read row + header); AUDIT (admin event section + partition + metadata-bound rationale); DATA-ARCHITECTURE (Platform admin object row). Documented deviation: no reified AdminCapability value - the capability is the verified in-transaction position (nothing to cache/serialize/forge); boundary tests pin the absence of any generic scope primitive instead.
- 2026-07-23 - P1-013 Slice 5 continued: postcss GHSA-6g55-p6wh-862q (new HIGH advisory, transitive via next) broke the exact-head High+ audit gate on run 30020713762 -> remediated with a pnpm-workspace.yaml override postcss>=8.5.12 (audit clean, web build green). Three independent reviews (8 owner lenses) returned: NO Critical/High; 1 Medium (flagged twice independently) + 6 Lows + 1 informational. ALL FIXED: (M) the auditWriter test seam - which handed the live target-scoped transaction to injectable code through the public core API - REMOVED from AdminOpOptions and the primitive signature; replaced by a no-argument post-audit failpoint that cannot skip/replace/observe the unconditional writeAuditEvent call. (L) soft-deleted users now lose admin authority at the DB layer (self-check joins users.status='active'; DB + core tests; runbook offboarding step). (L) lone surrogates (JSON-escape smuggle) rejected by validateAdminReason isWellFormed gate (contract + route tests) - kills the jsonb-persist 500 path. (L) actorType pinned LAST in the audit-context spread (caller ctx can never re-label the admin actor). (L) boundary guard now scans EVERY production file of the admin path (core+web+route+primitive, auto-discovered), comment-stripped, with quoted-table matching (memberships itself banned), raw-SQL INSERT forms, onBehalfOf, and tenant use-case/scope-runner deny-list + a self-coverage assertion. (L) catalog pins hardened: relrowsecurity AND relforcerowsecurity; ALL public-namespace SECURITY DEFINER functions by exact name (no acbp_% filter); no grants to any role beyond acbp_app/owner. (L-doc) core log canary added (capturing logger; reason absent from every emitted line). (Info) cache-control: no-store on every admin response.
- 2026-07-24 - OWNER-AUTHORIZED P1-013 finalization begun. Gate revalidated LIVE: root E:/AI-Company-Builder-Platform; branch p1-013-administrative-access-foundation; HEAD == origin == 966e44d; tree clean; no stash; no unpushed commit; PR #15 OPEN/DRAFT/unmerged/MERGEABLE/CLEAN with head == HEAD; merge base == origin/main == 795227b. Exact-head CI run for 966e44d discovered and recorded: 30021770562 = completed/success (frozen install; preflight OK; 93 files / 1038 passed / 0 failed / 0 SKIPPED; migration 0011 + admin RLS/catalog suite 6; core admin trust suite 10; boundary guard 20; reason contracts 10; API/privacy 33+6; activity taxonomy 18; provisioning 8; secret/encoding/boundary checks pass; High+ audit "No known vulnerabilities"). Local final-feature production web build EXIT 0 (route emitted dynamic). Reviews: 3 independent reviewers over the 8 owner lenses - no Critical/High; 1 Medium + 6 Lows + 1 info, all fixed in 966e44d; ledger P1-013-REVIEW-COVERAGE.md. Structural reconfirmation (live greps): zero runtime platform_admins writes; 13 API routes with exactly ONE under /api/admin; no UI/SSE/queue/outbox in the admin path; active-users JOIN + active-admin self-check; relationship check on id AND account_id; unconditional writeAuditEvent with actorType pinned last; ADMIN_REASON_MAX_CODE_POINTS still 512; admin:tenant_read allow-list empty; activity taxonomy still the four lifecycle events; migrations 0001-0010 byte-identical to base (only 0011 added); exactly 3 SECURITY DEFINER definitions (all 0006); no DATABASE_URL anywhere in apps/web. PR #10 re-inspected via GitHub state only: OPEN/unmerged/draft, untouched. Backlog ACBP-P1-013 Planned -> Done (that row only; P1-014+ unchanged). Sequence: finalization commit -> exact-commit CI -> PR ready -> final main/PR#10 recheck -> squash-merge (no Co-Authored-By) -> exact-main CI -> branch delete.

## ACBP-P1-014 - Tenant-isolation adversarial test suite (started 2026-07-24)
- Gate: main @ c1990ad (P1-013 squash PR #15; exact-main CI 30049788001 green 93f/1038/0-skip); tree clean; no stash/unpushed; exclusive worktree; PR #10 OPEN/unmerged/untouched; stale ancestor branch claude/affectionate-northcutt-f33c98 (340f9c2, ancestor of main) left untouched; P1-013 Done, P1-014/P1-015 Planned; no P1-014 artifacts; migrations 0001-0011 intact; exactly 3 SECURITY DEFINER. Discovery brief delivered; owner returned 20 explicit decisions -> CDR-020. Branch p1-014-tenant-isolation-adversarial-suite from c1990ad. --- next --- planning commit (CDR-020 + records), draft PR, then Slice 1 (two-tenant harness + restricted-role guard + threat inventory + consolidated catalog suite + README skeleton). Each slice hosted-green before the next. Stop at any Class O/M/A defect, a new owner decision, or the final owner gate.

## ACBP-P1-014 - Tenant-isolation adversarial test suite (2026-07-24)
- Gate: main @ c1990ad (P1-013 squash PR #15; exact-main CI 30049788001 green 93f/1038/0-skip). Branch p1-014-tenant-isolation-adversarial-suite; draft PR #16; CDR-020 records 20 owner decisions. Six slices, each exact-head hosted-green: f0f8532 harness+threat inventory+catalog (CI 30052709713); 917375b scope/revocation/GUC/concurrency (30056114225); b7f2fcc seam-free predicate-removal (30057324995); b84ce16 core product-path matrix (30057881277); 15a78b5 HTTP routes vs real DB + Class R bounded envelopes (30059425807); 2b87120 README+ledger+45/45 threat traceability (30059847486).
- Ten-lens independent review (3 reviewers) returned 3 HIGH: (1) tools/check-boundaries.mjs stripped block comments BEFORE line comments, so a // comment containing /* (a documented api/** glob) erased a file's entire import list and the gate reported clean on the one file violating it - fixed, and with the gate working the harness moved to packages/test-support, its canonical scaffold-spec home, with core ops INJECTED to avoid a core->adapters->test-support->core cycle; (2) trust-critical #20 forged placeholder ids so a claim-trusting regression would still have denied - claims now name the real target tenant, plus a source guard that no production file reads provider metadata, plus the sharp real-member-with-forged-owner-role case; (3) CURSOR-CROSS-COMPANY returned before its only assertion on every run - now generates a second activity row and requires the cursor. Also fixed: vacuous audit_events predicate-free read; UPDATE test tolerating 0 rows and a same-account cross-company write; column-privilege denials that were uuid cast errors (now SQLSTATE 42501); missing non-emptiness preconditions; seam guard breadth; runtime-role proof for the route path; mutating-verb oracle equality (B2 now genuinely paused); taxonomy negative; per-process test password instead of a scanner-evading literal. Final review head b5aa9da (CI 30061302385).
- CLASS M OWNER GATE raised and resolved: activity_events.event_id global uniqueness. Analysis proved the oracle is NOT production-reachable - event_id is a server-generated ULID, the sole live projector receives the id returned by the same-transaction audit write, and its source SELECT is FORCE-RLS-confined so a foreign id yields zero rows exactly like an unknown one, failing with a sanitized validationError BEFORE the unique constraint. Owner confirmed OPTION C (accept + document; no migration 0012; no schema/index/RLS/grant/API/behavior change). Recorded in the defect ledger (R-A1), the suite README and a new review-disposition doc, with four accepted principles (server-generated opaque global identities may stay global absent any supply path; caller-influenceable idempotency keys stay tenant-scoped as audit_events already does; a raw DB program bypassing the sole projector is outside the app threat path; users/identity_webhook_receipts are intentional global identity substrate). Regressions added: ACTIVITY-SOURCE-SWAP now deterministic, plus a projectCompanyActivity test proving foreign and unknown source ids give the same sanitized failure, insert nothing and roll back cleanly. Head d02254d, CI 30062049849 green: 103 files / 1154 passed / 0 failed / 0 skipped, verify 2m55s (5m ceiling), High+ audit clean, local web build EXIT 0.
- Defects: 10 Class T (test/fixture only), 1 Class R (bounded HTTP error envelopes on the company/account routes, restoring the accepted all-errors-bounded invariant; no success/denial semantics changed; source guard added), 1 Class M (owner-decided above). No test was weakened to pass.

## ACBP-P1-015 - Slice A integration: secure company creation (2026-07-24)
- Gate: main @ b559d37 (P1-014 squash PR #16; exact-main CI green). Branch p1-015-slice-a-secure-company-creation; draft PR #17; CDR-021. Only Planned ticket left in Phase 1.
- Discovery resolved P1-015's one apparent ambiguity WITHOUT an owner gate: browser-level E2E is not merely inconvenient, it is currently impossible - the slice-A flows are API-only by owner decision (P1-010..P1-013 each shipped with no UI and no SSE) so there are no screens to drive, and driving Clerk's hosted sign-in would require live provider credentials the operating constraints forbid. CDR-021 records the deferral to staging and defines "demo script" as a runnable pnpm demo:slice-a sharing ONE journey implementation with the CI suite.
- Slice 1 (2f03a70): runSliceAJourney in @acbp/test-support (10 steps, each carrying its requirement id and human-readable evidence, never throwing so a failure names the step); slice-a.e2e.integration.test.ts (full journey + negative set + three creation modes); tools/demo/ (script + ESM resolution hook + Clerk stub); SLICE-A-DEMO.md. Exact-head CI 30063164730 SUCCESS: 104 files / 1157 passed / 0 failed / 0 skipped, 3m18s (5m ceiling).
- Two independent reviews (security; architecture/scope) found NO production-security defect and no boundary/scope violation, but proved the DEMO SCRIPT - the backlog row's entire acceptance criterion and verification procedure - could not run at all: (1) register(pathToFileURL(new URL(...).pathname)) doubles the drive letter on Windows (file:///E:/E:/...) and threw before the try block, so none of the documented exit-code semantics could ever occur; (2) even past that, the real route modules import through the @/ alias, declared only in apps/web/tsconfig.json and vitest.config.ts, so a root-level tsx process failed with ERR_MODULE_NOT_FOUND. Both defects are direct evidence the script had never been executed before commit. Repaired (register(URL); alias resolution added to the hook with deterministic .ts/.tsx/index.ts probing), then ACTUALLY EXECUTED against real PostgreSQL: 10/10 steps + restricted-role proof, exit 0. Wired pnpm demo:slice-a into ci.yml so the acceptance criterion has hosted evidence rather than a claim.
- Further review repairs, all about assertions that could not fail: ACC-001 was nominal (the stub always presented a verified email, so deleting the production rule would have broken nothing) - the verification status is now mutable and a negative test proves an unverified primary email is refused on both routes with a bounded envelope and writes nothing. PORT-003 was a single GET of the caller's only company - now a real A->B->A switch inside one process, which is what "context switch discards prior tenant state" actually names. The "internal user mapping" step read the row the fixture itself inserted - relabelled a precondition, with the real proof moved to the actor_id the ROUTE stamps on the audit trail. The "no cross-attributed trail" step filtered own-company AND foreign-account, structurally unsatisfiable - now asks the falsifiable question (did this caller leave any audit row INSIDE the other tenant, including from the denials?). The restricted-role claim was only an environment precondition - now proven positively via runtimeConnectionRoles in both the suite and the script.
- Consolidation: the runtime env wiring existed in three hand-copied places (P1-014 HTTP suite, Slice A suite, demo script) differing only in placeholder strings, so a new variable in the composition root would have silently broken some callers and left others passing against a stale configuration - now one harness helper configureRouteRuntimeEnv (which also owns the DATABASE_URL deletion). The fixture's company names are exported as SEEDED_COMPANY_NAMES / world.companyNames so leak assertions search for the string actually written; restating them at call sites made a rename silently vacuous.
- Docs corrected from the reviews: CDR-021 no longer claims the read-through CREATE-on-miss branch is exercised (it needs the unseamed @clerk/backend reader and a live Clerk; creation-on-miss stays covered by the core read-through tests); TEST-AND-VERIFICATION-STRATEGY.md's Browser/E2E entry-gate row amended from "M1 (slice A)" to staging-once-screens-exist, with a new full-stack-journey row for what actually landed.
- Local integration environment: real-PostgreSQL runs ARE possible locally (contrary to the older note) once the dedicated WSL distro is held open by a background process and the local owner role is granted superuser/createrole to match CI (migration 0005 creates the acbp_app role and failed with "permission denied to create role"). Hosted CI remains the authoritative zero-skip gate.
- 2026-07-24 - Review-repair slice 8b8471b pushed; exact-head CI 30064683125 SUCCESS: 104 files / 1159 passed / 0 failed / 0 skipped, 3m06s (5m ceiling), preflight OK, High+ audit clean, and the NEW hosted "Slice A demo" step green (11/11 steps, live cross-tenant denial). Local production web build EXIT 0; git diff --check clean.
- 2026-07-24 - AUTONOMOUS P1-015 finalization begun under the standing Phase 1 authorization. Gate revalidated LIVE: root E:/AI-Company-Builder-Platform; branch p1-015-slice-a-secure-company-creation; HEAD == origin == 8b8471b; tree clean; no stash; merge base == origin/main == b559d37; PR #17 OPEN/DRAFT/unmerged/MERGEABLE/CLEAN with head == HEAD; PR #10 re-inspected through GitHub state only - still OPEN/unmerged/draft, untouched; stale ancestor branch claude/affectionate-northcutt-f33c98 untouched. Structural reconfirmation: migrations 0001-0011 unchanged (no 0012); exactly 3 SECURITY DEFINER function definitions, all in 0006 (acbp_provision_account, acbp_resolve_own_membership, acbp_accept_invite); no schema, RLS, grant, route, API or production-behavior change in this ticket - P1-015 adds tests, a demo harness and docs only. Backlog ACBP-P1-015 Planned -> Done (that row only); ALL 15 Phase 1 rows now Done. Sequence: finalization commit -> exact-commit CI -> PR ready -> final main/PR#10 recheck -> squash-merge "ACBP-P1-015: Slice A integration: secure company creation" (no Co-Authored-By) -> exact-main CI -> branch delete -> Phase 1 completion report.
- 2026-07-24 - P1-015 FINALIZED. Loader clamp (LOW from repair-delta review) fixed at 984be27, exact-head CI 30065408185 green (104f/1159/0-skip; demo 11/11). Independent repair-delta review returned CLEAN (repairs real, no regressions; single LOW resolved). PR #17 squash-merged as "ACBP-P1-015: Slice A integration: secure company creation" (no Co-Authored-By) -> main 85fcb8f. Exact-main CI 30065559857 SUCCESS: 104 files / 1159 passed / 0 failed / 0 skipped, hosted demo:slice-a 11/11, High+ audit clean. Local main fast-forwarded to 85fcb8f == origin/main; tree clean; p1-015 branch deleted local + remote. PR #10 re-inspected via GitHub state only - OPEN/draft/untouched.
- 2026-07-24 - PHASE 1 COMPLETE: all 15 P1 backlog rows Done. Post-completion read-only audit: P0 = 20 Done + P0-005 Blocked (known); P1 = 15 Done; remote branches = main + external p1-004 (PR #10) only, no abandoned P1 branches; migrations 0001-0011 ordered/intact (no 0012); exactly 3 SECURITY DEFINER (all 0006); acbp_app NOLOGIN/NOSUPERUSER/NOBYPASSRLS/non-owner, BYPASSRLS granted to none; no DATABASE_URL in apps/web runtime; secret/encoding/boundary checks 0; no temp/scratch/secret artifacts tracked (only .env.example); CDR-021 on main; no DECISION-LOG.md (CDRs are standalone files, consistent with prior tickets). One records-only staleness fixed: PROJECT-STATE Active section refreshed to Phase 1 complete on branch records-phase1-complete.

## ACBP-P2-001 - Interview session persistence and state machine (2026-07-24, Phase 2 / M2)
- Gate: main @ 21d8029 (Phase 1 complete). Branch p2-001-interview-session-state-machine; draft PR #20; CDR-022. First Phase 2 ticket, selected as the root of the M2 dependency tree (P2-002/005/008 trace to it) over P2-006 (downstream) and P2-003 (gated by open question IOQ-13). No owner gate: canon (WORKFLOW-STATE-MACHINES §2, ADR-008, EVENT-CATALOG, DATA-ARCHITECTURE, API-CONTRACTS) pins the state set, resume semantics, tenancy keying and events.
- Five slices, each exact-head hosted-green: 873405e contracts (six-state machine + legal-transition guard + honest display projection + redacted DTO; authz interview:read/participate owner|viewer; interview.started registration + factory + core audit-completeness partition), CI 30067448595. 1ea1eac migration 0012 interview_sessions (dual-keyed FORCE RLS, column-immutable identity, one-open-session-per-company partial unique index) + real-PG suite; its first CI failed on two points fixed in 3ebcd14 - my test seeded two accounts with the same owner user (accounts_owner_unique) and the P1-014 catalog test TENANT_TABLES lacked interview_sessions; also caught latent: role_table_grants shows only INSERT/SELECT for a column-level-UPDATE table, and a bound string 'now()' does not cast to timestamptz. 3ebcd14 core ops (start/suspend/resume/get under runInCompanyScope; server-enforced transitions with an optimistic from-state backstop; interview.started emitted in-tx) + InterviewSessionRepository + kill-and-resume real-PG suite, CI 30068857443. 4b893877 API routes (GET/POST interview, POST interview/suspend, interview/resume; no body/query; bounded errors) + web tests + production build (all four routes dynamic), CI 30069462478. edaa9ad real-DB HTTP adversarial suite (forged owner/admin claims never authorize; bounded cross-tenant denial; malformed companyId bounded not framework-500; paused company coarse 409) + INTERVIEW.md + TENANCY/AUTHORIZATION entries, CI 30069980632 (108 files, audit clean).
- Two independent reviews (security; architecture/scope) over the full diff: security CLEAN (no HIGH/MEDIUM/LOW production-security finding across all seven axes, verified against real-PG evidence). Architecture substantially clean. All observations FIXED (none accepted as-is) at 28020b6: (MEDIUM doc) CDR-022 §2 wording corrected to the atomic mint-and-enter the impl performs; (LOW code) concurrent first-start made graceful via ON CONFLICT DO NOTHING on the partial index + re-read (new real-PG concurrent-start test: one session, one audit, both ok); (LOW code) removed dead lockById; (LOW doc) EVENT-CATALOG interview.started annotated activity-deferred + Notes; (LOW doc) CDR-022 §5 started_at CHECK stated as the biconditional the migration enforces. Ledger: P2-001-REVIEW-COVERAGE.md. Review-fix CI 30070692829 green.
- Scope honored (CDR-022): the ready_for_review/confirmed/superseded transitions are defined + guarded, their effects deferred to later M2/M3; interview.started audit-only (activity projection deferred so P1-009's closed taxonomy is not expanded); no questions/answers/generation/memory/understanding. No new SECURITY DEFINER (still 3, all 0006); no BYPASSRLS; no owner runtime; migrations 0001-0012.
- AUTONOMOUS finalization (standing Phase 2 authorization). Gate revalidated LIVE: root E:/AI-Company-Builder-Platform; branch p2-001-...; HEAD == origin == 28020b6; tree clean; merge base == origin/main == 21d8029; PR #20 draft; PR #10 OPEN/untouched (GitHub state only). Backlog ACBP-P2-001 Planned -> Done (that row only); 11 P2 rows remain Planned. Sequence: finalization commit -> exact-commit CI -> PR ready -> recheck main/PR#10 -> squash-merge (no Co-Authored-By) -> exact-main CI -> delete branch -> next Ready P2 ticket.

## ACBP-P2-002 - Question and answer persistence (2026-07-24, Phase 2 / M2)
- Gate: main @ 6cf537e (P2-001 squash). Branch p2-002-question-answer-persistence; draft PR #21; CDR-023. Selected after P2-001 (natural continuation of the M2 interview flow) over P2-006 (also unblocked). No owner gate: the Q&A data model (DATA-ARCHITECTURE:18-19), append-only revision semantics, and dual-keyed tenancy are pinned by canon. The one interpretation - whether an answer revision is audited - was resolved autonomously toward persistence-only and flagged for owner visibility (see below).
- Five slices, each exact-head hosted-green: 176e6a2 contracts (answer status, bounded submission validation, redacted DTOs, current/next-revision + lifecycle + idempotent-no-op derivations), CI 30074... . f9450fe migration 0013 (interview_questions immutable + interview_answers append-only, PK (question_id, revision), content-shape CHECK, dual-keyed FORCE RLS, select+insert only) + real-PG suite; its first CI failed because I had hardcoded the P2-001 interview-sessions catalog test to assert last-migration=0012/count=12 - fixed cf47b08 (both interview catalog tests now assert a migration is PRESENT, not last/only). c7545b6 core ops (add question / record-revise-skip answer / get QA under CompanyScope; append-only; graceful concurrent revision) + InterviewQaRepository + real-PG suite. 26a949c API (GET .../interview/qa, POST .../interview/questions/[questionId]/answer; open session resolved server-side; client never supplies a session id) + web tests + build (both routes dynamic). 0cc26bd real-DB Q&A HTTP adversarial (forged owner claims denied; bounded errors) + INTERVIEW.md Q&A section, CI 30074267969 (audit clean).
- Two independent reviews (security; architecture/scope) over the full diff: both CLEAN, no HIGH/MEDIUM, no must-fix. BOTH rendered an explicit verdict that the CDR-023 §4 audit-deferral is ACCEPTABLE and NOT a mandatory owner gate - accountability is tamper-resistant at the privilege level (no UPDATE/DELETE grant on the append-only authored rows); interview.question_answered has no consumer and the audited correction (understanding.corrected) is M3; a correction event is additive/reversible. All observations FIXED (none accepted as-is) at 71657ae: (LOW schema) created_by_user_id -> NOT NULL so accountability is structural (+ real-PG null-author-rejected test); (LOW correctness) recordInterviewAnswer now does a BOUNDED RETRY on ON CONFLICT so concurrent DISTINCT answers both persist (no lost content) + two new real-PG tests (distinct->revisions 1,2,3; identical->one row); (LOW doc) CDR-023 §4 rewritten to lead with the acceptance-criteria/DISC-008-cross-milestone/ADR-015-high-risk-scope argument (not doc-precedence); (LOW doc) EVENT-CATALOG interview.question_answered annotated deferred + Notes; (LOW dead code) removed unused nextRevision helper. Ledger: P2-002-REVIEW-COVERAGE.md. Review-fix CI 30075033944 green.
- Scope honored (CDR-023): no question generation/adaptivity (P2-005), no understanding re-evaluation/stale flagging (M3), no typed memory (P2-006), no gateway (P2-003); no audit/domain event emitted (persistence-only). No new SECURITY DEFINER (still 3, all 0006); no BYPASSRLS; no owner runtime; migrations 0001-0013.
- AUTONOMOUS finalization (standing Phase 2 authorization). Gate revalidated LIVE: root E:/AI-Company-Builder-Platform; HEAD == origin == 71657ae; tree clean; merge base == origin/main == 6cf537e; PR #21 draft; PR #10 OPEN/untouched (GitHub state only). Backlog ACBP-P2-002 Planned -> Done (that row only); P2 now 2 Done / 10 Planned. Sequence: finalization commit -> exact-commit CI -> PR ready -> recheck main/PR#10 -> squash-merge (no Co-Authored-By) -> exact-main CI -> delete branch -> next Ready P2 ticket (P2-006 typed memory is unblocked; P2-003/P2-005 gated by IOQ-13).

## ACBP-P2-006 - Typed memory items with provenance (2026-07-24, Phase 2 / M2)
- Gate: main @ 1c49c55 (P2-002 squash). Branch p2-006-typed-memory-items; draft PR #22; CDR-024. The only unblocked Ready P2 ticket (deps P1-005 Done; P2-003/P2-005 gated by IOQ-13). No owner gate: the memory data model (DATA-ARCHITECTURE §3), closed 8-type enum, provenance, dual-keyed tenancy are pinned by canon. One interpretation - that a memory creation IS audited (contrast P2-002 persistence-only, because MEM-003/backlog say "All changes audited") - resolved toward auditing (memory.item_created in-tx) and flagged for owner visibility; both reviews confirmed it is not an owner gate.
- MIGRATION-CYCLE BLOCKER root-caused this window (Class T): the prior window's hosted 42P01 "relation public.memory_items does not exist" in multi-step migrateDown/migrateTo(earlier) was caused by a window-1 BULK DROP-LIST EDIT that also matched and edited migration 0013's down loop, inserting 'memory_items'. So 0013.down ran drop-policy/revoke on memory_items; during a down PAST 0013, 0014.down had already dropped it -> 42P01 at the 0013 rollback step. The new single-step memory-items test passed and masked it. Diagnosed by stepping the migrateDown loop on a DISPOSABLE PostgreSQL: Docker daemon unresponsive; the WSL 5433 cluster unreachable from Windows; used the Windows-native postgres on 127.0.0.1:5433 (isolated acbp_p2006_test db + acbp_owner role, command-local ACBP_TEST_DATABASE_URL only, .env.local untouched). Fix cb43315: 0013.down reverted to ['interview_answers','interview_questions'] (parity with main); the two window-1 speculative changes (0014.down cascade; self-FK removal) reverted to the standard pattern + restored self-FK.
- Five slices (Slice 1 pre-existing from prior window, 4 this window), each exact-head hosted-green: 0cc3d8f contracts (closed 8-type + 6-source enums, type-by-source-path maySourceProduceType, validation, DTO, memory:read/write authz, memory.item_created audit registration + factory + core partition). 67f2e3c migration 0014 memory_items (dual-keyed FORCE RLS, SELECT+INSERT only, type/source/type-by-source/source_ref/confidence/confirmation CHECKs, self-FK superseded_by) + real-PG suite; cycle-fix cb43315. c0fe138 core createMemoryItem (audited-in-tx; type-by-source; untyped rejected) + listMemoryItems (newest-first, type filter, bounded) + MemoryItemRepository + real-PG suite (audit atomicity, matrix, no-activity-projection, concurrent append). bc752f4 API GET/POST /api/companies/[companyId]/memory + request/http (server-resolved account/company/actor; no query param) + web tests + build. 8bfc668 real-DB memory HTTP adversarial (forged owner/admin claims denied; body-field forgery ignored; type/source negatives; no supersede/delete verb) + docs (MEMORY.md, EVENT-CATALOG memory.item_created, AUDIT, AUTHORIZATION, TENANCY).
- Two independent reviews (security; architecture/scope) over the full diff: BOTH CLEAN, no HIGH/MEDIUM, no must-fix. Explicit CORRECT verdicts on the migration root-cause fix and the audit atomicity/decision (reversible documented interpretation, NOT an owner gate). All findings FIXED at a5fe97c: (LOW security fail-safe) assertType/assertSourceType now THROW on an out-of-set value instead of coercing corruption toward the most-trusted user_fact/user_edit; (MEDIUM doc) source_ref "resolvable/validated" wording tempered to non-empty+bounded (encoding is convention; deep resolvability is P2-007); (LOW YAGNI) removed unused MemoryItemRepository.findById. Retained documented judgment calls: member-write grant (memory:write owner|viewer), confirmation_state without confirmed_by (P2-008), the labeled auditWriter test seam. Ledger: P2-006-REVIEW-COVERAGE.md. Review-fix CI 30090738122 green.
- Scope honored (CDR-024 §7): create + list only; NO supersede/confirm/delete OPERATION (grants SELECT+INSERT; superseded_by column present but null, ready for P2-010), no context assembly (P2-007), no understanding/confidence-scoring (P2-008), no browser (P2-010), no generation (P2-005). No new SECURITY DEFINER (still 3, all 0006); no BYPASSRLS; no owner runtime; migrations 0001-0014; no 0015.
- AUTONOMOUS finalization (standing Phase 2 authorization). Gate revalidated LIVE: root E:/AI-Company-Builder-Platform; HEAD == origin == a5fe97c; tree clean; merge base == origin/main == 1c49c55; PR #22 draft; PR #10 OPEN/untouched (GitHub state only). Backlog ACBP-P2-006 Planned -> Done (that row only); P2 now 3 Done / 9 Planned. Sequence: finalization commit -> exact-commit CI -> PR ready -> recheck main/PR#10 -> squash-merge (no Co-Authored-By) -> exact-main CI -> delete branch -> next Ready P2 ticket (inspect canon; P2-003/P2-005 gated by IOQ-13).

## ACBP-P2-010 - Memory browser (2026-07-24, Phase 2 / M2)
- Gate: main @ 942cc1d (P2-006 squash). Branch p2-010-memory-browser; PR #23; CDR-025. The only unblocked Ready P2 ticket (deps P2-006 Done; P2-003/P2-005 gated by IOQ-13). The list/filter/edit/delete browser on memory_items.
- OWNER GATE reached + RATIFIED: memory DELETION SEMANTICS (CLAUDE.md names "deletion semantics" a mandatory gate; P2-010 operationalizes memory delete first time). Surfaced with a recommendation; the owner ratified all three: (A) soft delete via deleted_at + deleted_by_user_id; (B) memory.item_deleted written in the same transaction as every deletion; (C) deletion->understanding/plan propagation deferred to M3/M4 (dependents don't exist; P2-010's only dep is P2-006). The edit + read half (canon-resolved, delete-independent) was built while the gate was pending; the delete half was built after ratification.
- Slices, each exact-head hosted-green: f82f5b2 contracts (memory:edit owner-only + memory.item_superseded audit). 49e899c migration 0015 (column-level UPDATE(superseded_by) grant + dual-keyed UPDATE policy - the interview_sessions 0012 precedent) + core editMemoryItem (versioned supersede, version-guarded) + getMemoryItem + currentOnly list. 3f03492 API (GET filtered list, GET single, PATCH edit) + web tests + build. dc24e9f DELETE core: contracts (memory:delete owner-only + memory.item_deleted audit {item_type,source_type,transition}), migration 0016 (deleted_at + deleted_by_user_id + PAIR check + mutual-exclusion check + narrow UPDATE(deleted_at,deleted_by_user_id) grant; 0015 untouched), core deleteMemoryItem (soft delete, server clock, guarded so exactly one transition + one audit; list/get omit deleted; edit/delete of deleted/superseded -> conflict). 330ba8c DELETE route + real-DB adversarial. 78f7635 docs (MEMORY.md browser section, EVENT-CATALOG, AUDIT, AUTHORIZATION; CDR-025 §0 ratified). b9441f1 review fixes.
- Two independent reviews (security; architecture/scope) over the full diff: BOTH CLEAN. Security no HIGH/MEDIUM, explicit CORRECT verdicts on soft-delete concurrency determinism, audit atomicity, grant narrowness. Architecture PASS, owner decision faithfully implemented. Findings FIXED at b9441f1: (MEDIUM) editMemoryItem inserted the new version before the guarded supersede -> a lost concurrent-edit race committed an orphan active version; fixed with findByIdForUpdate (SELECT FOR UPDATE) so editors serialize and the loser conflicts before inserting + a real-PG concurrent-edit test (one ok/one conflict, exactly two rows, one active version); (LOW doc) four dangling CDR-025 §8 refs -> added CDR-025 §7 propagation section + repointed; (LOW security, retained) malformed non-UUID id -> bounded 500 (established Class-R cross-route behavior, not an oracle). Ledger: P2-010-REVIEW-COVERAGE.md. Review-fix CI 30102561583 green.
- Scope honored (CDR-025): list/filter/get + owner edit (supersede) + owner soft delete; NO restore/undelete/purge/physical delete, no includeDeleted, no hard delete, no in-place content overwrite, no dependent-staleness propagation placeholder (CDR-025 §7 deferral), no context assembly (P2-007), no understanding (P2-008), no generation (P2-005), no demo (P2-012). No new SECURITY DEFINER (still 3, all 0006); no BYPASSRLS; no owner runtime; migrations 0001-0016; no 0017. Column-level UPDATE confined to superseded_by + deleted_at + deleted_by_user_id.
- AUTONOMOUS finalization (post-ratification; the owner said do not stop again for the resolved deletion decision). Gate revalidated LIVE: root E:/AI-Company-Builder-Platform; HEAD == origin == b9441f1; tree clean; merge base == origin/main == 942cc1d; PR #23; PR #10 OPEN/untouched (GitHub state only). Backlog ACBP-P2-010 Planned -> Done (that row only); P2 now 4 Done / 8 Planned. Sequence: finalization commit -> exact-commit CI -> PR ready -> recheck main/PR#10 -> squash-merge (no Co-Authored-By) -> exact-main CI -> delete branch -> next Ready P2 ticket (inspect canon; P2-003/P2-005 gated by IOQ-13).
- 2026-07-24 — ACBP-P2-003 model gateway STARTED. IOQ-13 owner-RATIFIED ("Adopt proposed defaults": interactive 30s / generation 120s / retries <=2) -> CDR-026 recorded + IOQ-13 marked Resolved. Branch p2-003-model-gateway from main 10b4e2e. Canon-confirmed scope: the provider-neutral gateway ABSTRACTION + a deterministic FAKE provider is autonomous; the LIVE provider path (real key + gpt-5.1 snapshot pin [CDR-001 A8] + ADR-019 A13 eval gate) is a DEFERRED owner gate (CDR-026 A0), NOT built.
- Slice 1 (contracts) 7f82721: @acbp/contracts/model gateway.ts + usage.ts — ModelGatewayRequest/Result (ADR-011 A5 field-for-field), TaskClass/TimeoutClass + IOQ-13 config constants, the seven-value normalized taxonomy + retryable/public-envelope mapping, the append-only usage-event contract. + CDR-026. Unit tests pin config + taxonomy. Static gate green.
- Slice 2 (migration 0017) ce7122d: usage_events — company-owned dual-keyed FORCE RLS, APPEND-ONLY (SELECT+INSERT grants only, NO update/delete grant, NO update policy — invariant 9), integer micro-units, outcome<->error_category pairing CHECK, closed enums, bounded lengths + UsageEventRepository + schema. Real-PG suite 8/8 (0 skips): dual-keyed isolation, append-only refusal, CHECKs, FK cascade, down/up/reapply by name, catalog (3 SECURITY DEFINER unchanged, acbp_app NOBYPASSRLS). Table-existence guard updated.
- Slice 3 (gateway+fake) 607cbbd: core callModel (policy pre-check, per-class timeout, bounded retry <=2 + re-ask <=1, fallback eligibility [generation ineligible — no silent fallback], normalized taxonomy, redacted logging, FAIL-CLOSED metering) — imports no provider SDK, no DB (injected). FakeModelProvider (@acbp/adapters/model), the only wired adapter. 18 deterministic tests (contract/metering, fault injection, redaction canary, fail-closed). Secret-scanner-safe synthetic markers.
- Slice 4 (composition) 2d63260: createModelGateway wires recordUsage -> UsageEventRepository under the request's company scope in a short tenant tx AFTER the external call; fail-closed preserved end-to-end. Real-PG composition suite 5/5 (0 skips): one ok usage row per call (correct company/account/provider/model/cost), error metering, fallback attribution, cross-company RLS isolation, DB-level fail-closed (write failure -> throw, no row).
- Slice 5 (docs): CDR-026; IOQ-13 Resolved; AI-AND-WORKER A3 implementation-status note; EVENT-CATALOG + DATA-ARCHITECTURE (usage_events/model.call_completed realized append-only, audit-mechanism = the usage row, not a second audit_events entry); PROJECT-STATE + this log. Evidence: static gate green; unit 850/850; real-PG usage_events 8/8 + composition 5/5. — next — independent review, then STOP at the finalization OWNER GATE (backlog Done / push+draft PR / exact-head CI / mark ready / squash-merge / exact-main CI / delete branch are all owner-gated; P2-003 finalization was not pre-authorized).
