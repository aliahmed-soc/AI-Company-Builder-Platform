
- 2026-07-24 (window start 23:23 +03:00) — NEW 6h autonomous window. State validated: main==origin/main==c916d81 (P2-005 merged), P2-008 branch p2-008-understanding-generation at a65a9a9 (CDR-029 + contracts slices done from prior window), tree clean, migrations end 0018, 3 SECURITY DEFINER. Continued P2-008 from the pushed state (no restart).
- P2-008 slices, TDD, each focus-verified: 29adc86 migration 0019 (understanding_documents + understanding_items — company-owned dual-keyed FORCE RLS, versioned append-only SELECT+INSERT only, unique (company_id,version), FK cascade, status/class/confidence/content/source_ref CHECKs; UnderstandingRepository; real-PG 6/6; both tables added to 31 reset lists + test-support ALL_TABLES + database.integration existence guard + P1-014 catalog TENANT_TABLES/EXPECTED_GRANTS — the P2-003 lesson). CI 30124416764 green. 16546dc core generateUnderstanding (memory->gateway->classify->persist version+items+audit in one tx, audit-or-nothing; failure/malformed persists nothing; model-flagged partial; understanding:generate/read authz; composition validator) + real-PG integration 8/8. 448b83d docs (EVENT-CATALOG/AUTHORIZATION/AUDIT/DATA-ARCHITECTURE). CI 30125418699 GREEN (full zero-skip).
- Independent 10-dimension security/scope review (subagent): CLEAN — no Critical/High/Medium. 3 LOW fixed at 85808e7: (L1) concurrent generation could throw uncaught duplicate-version -> race-safe insertDocument ON CONFLICT DO NOTHING + bounded retry + a real-PG concurrency test (two concurrent -> versions [1,2]); (L2) CDR-029 §3 wording -> failure/malformed persists NOTHING (only model-flagged partial), matches the safer code; (L3) CDR-029 §1 wording -> classification is model-based, source_ref null (linkage deferred), UNDER-002 preserved (understanding_items never writes to memory). Ledger P2-008-REVIEW-COVERAGE.md. Full unit 906/0; real-PG understanding 15/15 (0 skips). Checkpoint h1 23:41.
- Scope (CDR-029): the understanding-generation ENGINE + composition seam. HTTP routes DEFERRED with the live provider (CDR-026 §0 — every endpoint invokes gated generation; mirrors P2-003/P2-005). No live provider/key/real call; fake provider only. No new SECURITY DEFINER (still 3), no BYPASSRLS, no owner runtime; migrations 0001-0019. No P2-009 review/confirm, no P2-007 context assembly, no P3-001 strategy leakage.
- Finalization (pre-authorized this window): backlog ACBP-P2-008 Planned->Done; records commit; push; exact-head hosted CI (zero-skip real-PG); PR #27 ready; squash-merge "ACBP-P2-008: Understanding generation" (no Co-Authored-By); exact-main CI; delete branch; continue to next Ready ticket (P2-009 / P2-007). Local full-serial suite exceeds the 10-min shell cap; hosted CI is the authoritative zero-skip run.
- P2-008 MERGED squash 9e11466 (PR #27); exact-main CI 30126453545 green; branch deleted local+remote; main==origin/main==9e11466. Phase 2 8/12.
- ACBP-P2-009 selected via canon discovery (subagent over P2-007/P2-009/P2-011/P2-012): P2-009 highest leverage (unblocks P2-012 + P3-001 critical spine), no owner gate (Usage "—" so no live model; additive migration in existing dual-keyed FORCE-RLS pattern; new closed authz actions != new authz model). P2-011 correctly avoided (live-model eval gate CDR-026 §0). Branch p2-009-understanding-review from 9e11466; draft PR #28; CDR-030.
- P2-009 scope (CDR-030): the owner REVIEW + CONFIRMATION gate over the understanding VERSION (not the immutable session — P2-008 decoupled it; session-state sync deferred to P2-012). Five per-item controls; owner-only confirm unlocks strategy; DISC-008 correction supersedes a confirmation + flags dependents. NO live model (evidence/research recorded, not executed); NO HTTP route; no P2-011/P3-001 leakage; no owner-gate crossing.
- P2-009 slices, TDD, focus-verified: contracts 7455e66 (5 review controls, confirmation-event kinds, expected_version staleness, isVersionConfirmed gate; understanding:review/:confirm owner-only; 3 audit events item_reviewed/confirmed/corrected + exhaustive AUDITED_OPERATIONS partition; exact-set enumeration tests). Migration 0020 594b044 (understanding_item_reviews + understanding_confirmation_events — append-only dual-keyed FORCE RLS, SELECT+INSERT only, UNIQUE(document_id,kind) idempotency, decision/kind/shape CHECKs, FK cascade; both tables in EVERY reset list (33) + ALL_TABLES + database.integration existence + P1-014 catalog; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Core 3c349f1 (recordUnderstandingReview/confirmUnderstanding/correctUnderstanding/isCurrentUnderstandingConfirmed gate; audit-or-nothing; STRATEGY_DEPENDENT_COUNT=1). Docs 2f2d80d. Local real-PG all green zero-skip: 0020+0019 migration 14, catalog 48, database 10, core review 8, audit producer 12; full unit 927; typecheck/lint/secrets/boundaries/encoding clean; boundary 33. Code-slice exact-head CI 30128716043 GREEN.
- Independent 10-dimension adversarial review (subagent): no Blocker/Critical/High. 4 Low/Medium fixed: (D1) understanding.confirmed metadata doc {version,confirmed_by}->{version} in CDR §3/§6 + EVENT-CATALOG (impl already {version}, actor is server-stamped); (D2) review/confirmation repo listing order id->created_at,id (chronological "latest row" semantics correct for future consumers); (P1) CDR §3 confirm precondition softened to the enforced set (per-item review not a hard gate — canon does not quantify "review done"); (P2) correction_ref "never content" -> "bounded reference, not content-validated". P2-009-REVIEW-COVERAGE.md.
- P2-009 MERGED squash 40548cf (PR #28); exact-main CI on 40548cf FAILED only on the `pnpm audit` step — a new dev-only transitive advisory brace-expansion <5.0.8 (GHSA-mh99-v99m-4gvg) landed 2026-07-24 and blocks the repo-wide High+ gate for ANY commit (135/135 tests passed zero-skip). CI hotfix: pnpm-workspace.yaml overrides `brace-expansion: ">=5.0.8"` (the established sharp/postcss remediation pattern), PR #29 squash 875a00c; exact-main on 875a00c GREEN. Both branches deleted. Phase 2 9/12; migrations end 0020.
- ACBP-P2-012 selected via canon discovery (subagent over P2-007/P2-011/P2-012): P2-012 highest leverage (M2/M3 milestone-exit E2E on the critical spine), deps P2-009+P2-010 both Done, fully buildable on the deterministic FakeModelProvider, no owner gate. P2-011 EXCLUDED (live-model eval owner gate CDR-026 §0); P2-007 = Phase-3 kickoff. Branch p2-012-slice-b from 875a00c; draft PR #30; CDR-031.
- P2-012 (Testing/vertical-slice): the founder-discovery E2E interview→follow-ups→classification→understanding→edit→confirm→correction→fallback-flag. runSliceBJourney (@acbp/test-support) implemented ONCE, shared by the CI suite (packages/core/src/discovery/slice-b.e2e.integration.test.ts) + the runnable demo (tools/demo/slice-b.mjs, `pnpm demo:slice-b`) so they can't drift (Slice A/CDR-021 precedent). Core use cases + gateway factory INJECTED (structural SliceBOps method sigs + arrow makeGateway) — test-support must NOT import @acbp/core (workspace-graph cycle: adapters tests import test-support). Drives the REAL use cases through restricted acbp_app under FORCE RLS; only the model provider edge is the FakeModelProvider (no live model/key). Proves resumability (suspend/resume round-trip), fact/assumption distinctness, understanding generation, confirm gate (blocked pre-confirm, unlocked post-confirm), correction re-blocks planning (dependents flagged), usage events verified, audit trail (interview.started/memory.item_created/understanding.generated/.confirmed/.corrected) tenant+actor stamped, and the fallback-flag NEGATIVE (follow-up static_fallback honest + understanding generation persists nothing on failure — ADR-019 non-silent). No migration, no new authz/audit, no owner gate. Commit 51cb256. Local: E2E 1/1, demo 13/13, unit 927; boundaries(no cycle)/typecheck/lint/secrets/encoding/boundary-tests clean.
- P2-012 review hardening 2bb8f24 (Low-1 step-3 falsifiability: assert user_fact carries the answer; Low-4 step-12 scoped to journey's own audits; demo truncation guard; CDR wording), finalization records e752955 (backlog Done). Finalization-head + exact-main CI both GREEN 136/136 zero-skip. MERGED squash a6dff28 (PR #30); branch deleted. Phase 2 10/12.
- ACBP-P2-007 (context assembly, trust-critical) STARTED — the last non-gated Phase-2 ticket (P2-011 owner-gated on the live-model eval, CDR-026 §0). Deliberately sliced: this window shipped only the PURE security-critical CONTRACTS slice (CDR-032) — rankMemoryForContext/provenanceTier (confirmed user > accepted assumptions > research; invalidated excluded; MEM-004 ordering) + SECRET_PATTERNS/redactSecrets/containsSecret (fail-closed blocklist; seeded secret never reaches the prompt — invariant 12/NFR-018). Commit 8b01212; branch p2-007-context-assembly from a6dff28; draft PR #31. 20 unit tests (11 synthetic secret shapes redacted + benign no-FP + ranking); synthetic fixtures allowlisted for the secret scanner; recursive typecheck/lint/secrets/encoding/boundaries clean. NOT finalized — the core assembleContext use case + MEM-004 conflict-detection semantics + real-PG integration + security review + finalization are the follow-up core slice (next window). Handoff clean.
- P2-007 contracts security review (subagent): 2 HIGH redactor defects FIXED (H1 quadratic ReDoS in the PEM pattern -> bounded; H2 fail-open on truncated private key -> BEGIN-anchored fail-closed fallback); added JWT/key=value/SendGrid/npm patterns; documented residual gaps. Commit 8113608; 24 unit tests; P2-007-REVIEW-COVERAGE.md. Exact-head CI 30137480384 GREEN.
- P2-007 CORE slice (owner resumed the window; ratified the MEM-004 conflict semantics: genuine contradiction -> open question, never silent rank-resolve; reuse P2-005). Stated the exact gap: P2-005's contradiction detection is MODEL-based + answer-time + persists no signal; context assembly is model-free (builds the model's prompt) -> cannot reuse it at assembly time; semantic detection of arbitrary stored texts deferred (needs a model-based write-time signal). Built the DETERMINISTIC same-source_ref detector: detectMemoryConflicts flags a confirmed-user item + an ai_assumption sharing one source_ref. assembleContext (commit 381c2bd): read current memory (memory:read) -> detect conflicts -> WITHHOLD both conflicting items + audit context.conflict_flagged in-tx (bounded {confirmed_count,assumption_count}; no content/source_ref; outcome blocked) -> provenance-rank -> redact secrets -> return contextParts+conflicts. NO model call; no migration; no new authz. Real-PG integration 6/6 (provenance order + redaction; conflict flagged/withheld/audited; empty; non-member forbidden; cross-company isolation; audit-or-nothing rollback). Docs: CDR-032 §3 final design, EVENT-CATALOG, DATA-ARCHITECTURE, AUTHORIZATION. Full unit 958; all gates clean. MERGED squash 68f99e4 (PR #31); exact-main CI green zero-skip; branch deleted. Phase 2 11/12 (only P2-011 owner-gated on the live-model eval).
- 2026-07-25 (window start ~00:00 +03:00) — NEW 8-hour autonomous window. State validated: main==origin/main==68f99e4 (P2-007 merged), tree clean, migrations end 0020, 3 SECURITY DEFINER; only other open PR is p1-004-last-owner-race-fix (#10, left untouched). Read the FULL backlog (all phases). Phase 2 complete except P2-011 (owner-gated). Deferred P3-001 (its "16-field option standard" STRAT-002 is referenced 5+ times across canon but the exact 16 fields are NEVER enumerated in the repo — a genuine product-semantics gap, flagged for the owner; all of P3 gates on it). Selected ACBP-P4-002 (task model + state machine): Ready, deps P1-005 Done, no owner gate, no live model, and it is the M-critical spine feeding P4-003/004/005 + P5-002.
- P4-002 slices, TDD, each focus-verified. Contracts+authz+audit 780ce94: the closed 11-state TaskState set + LEGAL_TRANSITIONS map (WORKFLOW §4 verbatim) + pure guards (isLegalTaskTransition/legalTaskSuccessors/isOpen/isTerminal/isTaskHold/toTaskDisplayPhase) + TaskDTO; 100% transition-table conformance test (every (from,to) in STATES×STATES asserted); task.created audit event + factory ({has_milestone} only) + exhaustive AUDITED_OPERATIONS partition; task:create/task:read authz (owner|viewer). Migration 0021 9916ffd (tasks mutable-with-audit M: SELECT+INSERT + column-scoped UPDATE(state,updated_at), 11-state CHECK, title/desc bounds, dual-keyed FORCE RLS; task_dependencies append-only I: SELECT+INSERT, UNIQUE(task_id,depends_on_task_id), CHECK no-self-dep, dual-keyed FORCE RLS; FK cascade; TaskRepository with race-safe guarded updateState; both tables in EVERY reset list + test-support ALL_TABLES + database.integration existence + P1-014 catalog TENANT_TABLES/EXPECTED_GRANTS/column-privilege expectations; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Core eeddf65 (createTask mints draft no-audit; planTask server-enforced draft->planned + task.created audited in-tx audit-or-nothing, illegal transitions rejected with no audit; addTaskDependency immutable same-company edge self/duplicate/unknown refused; getTask/listTasks redacted reads; task:create covers plan+depend). Local real-PG all green zero-skip: tasks migration 7/7, core use cases 10/10, catalog + database existence re-verified; full recursive typecheck/lint/secrets/boundaries clean. Docs: CDR-033, EVENT-CATALOG, AUTHORIZATION, DATA-ARCHITECTURE, PROJECT-STATE, this log.
- P4-002 independent security/scope review (subagent, 9 dimensions): PASS, no Blocker/Critical/High. 2 Low fixed (LOW-1 addTaskDependency duplicate check was TOCTOU -> ON CONFLICT DO NOTHING RETURNING + a real-PG concurrency regression test; LOW-2 trim description consistently with title). milestone_id-no-FK NOTE accepted (milestones=P4-001; deferred). Ledger P4-002-REVIEW-COVERAGE.md. Finalization commit 77f1d09. Exact-head CI 30143086362 GREEN (141 files / 1564 / 0 skipped). MERGED squash 08e7d6a (PR #32); exact-main CI 30143239625 GREEN zero-skip; branch deleted local+remote. Phase 4 1/7; migrations end 0021.
- ACBP-P3-001 selected via canon discovery (subagent over P4-004/P4-005/P5-001/P3-001): P3-001 highest-leverage unblocked (root of the strategy->decision->planning critical spine). CORRECTED an earlier mistaken deferral: the "16-field option standard" (STRAT-002) IS enumerated in canon -- PRD §11.3 line 302 (description, customer, offer, business_model, scope, benefits, risks, cost_range, effort, time_to_validate, time_to_launch, required_resources, key_assumptions, validation_method, success_metrics, confidence = 16), locked by the backlog's "All 16 fields" acceptance -- verified directly. P5-001 correctly EXCLUDED (unperformed mandatory owner DoR split review, IMPLEMENTATION-ROADMAP §Document control). No live model (FakeModelProvider; live deferred CDR-026 §0), no owner gate. Branch p3-001-strategy-option-generation from 08e7d6a; draft PR #33; CDR-034.
- P3-001 slices, TDD, focus-verified. Contracts f85263d: the closed 16-field STRATEGY_OPTION_FIELDS (PRD §11.3) + parseStrategyOptions (deny-by-default; ADR-019 "unknown" labeled sentinel; honest fewer-than-three DERIVED from count) + narrowStrategyOutput (single-parse defensive re-check of the gateway's validated output) + StrategyGeneration/Option DTOs; strategy.options@1 gateway template; strategy.generated audit event+factory + exhaustive AUDITED_OPERATIONS partition; strategy:generate/:read authz (owner|viewer). Migration 0022 932c399 (strategy_generations + strategy_options -- immutable I, dual-keyed FORCE RLS, SELECT+INSERT only, closed status/similarity enums + option_count/version/jsonb-object/UNIQUE(generation_id,ordinal) CHECKs, FK to understanding_documents; StrategyRepository; both tables in EVERY reset list + ALL_TABLES + catalog TENANT_TABLES/EXPECTED_GRANTS/column-privileges + database existence, FK-safe drop order before understanding_documents; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Core 2c9c22d (generateStrategyOptions gated on isCurrentUnderstandingConfirmed -> gateway -> narrow -> persist generation+options+strategy.generated in one tx audit-or-nothing, gateway-failure/malformed persists nothing, metered; getLatestStrategyGeneration read; strategyOutputValidator composition seam). Local real-PG all green zero-skip: strategy migration 6/6, core use cases 8/8, catalog + database existence re-verified; full recursive typecheck/lint/secrets/boundaries clean; full unit 988. Docs: CDR-034, EVENT-CATALOG, AUTHORIZATION, DATA-ARCHITECTURE, PROJECT-STATE, this log.
- P3-001 independent security/scope review (subagent): PASS, no Blocker/Critical/High. 1 Medium + 2 Low fixed (Medium: confirm-gate TOCTOU across the two-transaction window -> persist tx re-verifies the current understanding doc id/version/confirmation, returns stale_understanding persisting nothing, real-PG test via beforePersist seam; Low-1 narrowStrategyOutput rejects a fewerReason on complete; Low-2 migration 0022 status/count + reason-only-when-fewer CHECKs). Ledger P3-001-REVIEW-COVERAGE.md. Finalization commit b324546; exact-head CI 30144628272 GREEN (1599/0 skipped); MERGED squash 450c768 (PR #33); exact-main CI 30144767371 GREEN zero-skip; branch deleted. Phase 3 1/7; migrations end 0022.
- 2026-07-25 (2nd 8-hour window) -- NEW window. State validated: main==origin/main==450c768, tree clean, migrations end 0022, 3 SECURITY DEFINER; only open PR is p1-004-last-owner-race-fix (#10, untouched). Selected ACBP-P3-002 (distinctness check): Ready, deps P3-001 Done, no owner gate. Thorough canon search (per owner directive) CONFIRMS no product-semantics gap: distinct <=> differ on >=1 of {customer, offer, business_model} (PRD J-07 line 373 + REQUIREMENTS STRAT-001 line 32 "3+ options differ on at least one of customer/offer/model; a similarity check rejects near-duplicates; fewer than 3 stated honestly"); the check is DETERMINISTIC domain output validation (AI-AND-WORKER §1), no model/metering. Branch p3-002-distinctness-check from 450c768; draft PR #34; CDR-035.
- P3-002 slices, TDD, focus-verified. Contracts f563d14: DISTINCTNESS_AXES=[customer,offer,business_model] + distinctnessKey (normalized, NUL-separated to avoid axis-boundary collisions) + dedupeByDistinctness (keeps first representative per group; distinct/insufficient_distinct) + adversarial unit tests. Core cba98cc: generateStrategyOptions runs the similarity check after 16-field validation -> persists ONLY the distinct set (near-duplicates rejected, not stored), records the REAL verdict (never 'pending' again), derives status from the distinct count, honest fewer_reason on collapse; updated P3-001 fixtures (identical->genuinely distinct) + adversarial real-PG. NO migration/audit/authz change (existing similarity_check_result column + P3-001 status<->option_count CHECK hold since option_count=distinct count). Source-cleanup faf4c91 (raw NUL byte ->  escape + boundary-collision test). Local: contracts strategy 19 unit + core strategy real-PG 12/12; full recursive typecheck/lint/secrets/boundaries clean; unit 995. Independent review PASS (HIGH axis-boundary collision self-caught + fixed pre-review via faf4c91; 2 Low fixed: stale header comments; fewer_than_three null-reason -> honestFewerReason always factual). Ledger P3-002-REVIEW-COVERAGE.md. Docs: CDR-035, DATA-ARCHITECTURE, EVENT-CATALOG, PROJECT-STATE, this log. MERGED squash a8ace01 (PR #34); exact-head + exact-main CI GREEN zero-skip (144 files / 1610 / 0 skipped); branch deleted. Phase 3 2/7; migrations end 0022.
- ACBP-P3-003 selected (canon-discovery subagent over P3-003/P4-004/P4-005): P3-003 highest-leverage unblocked (sole unblocker of the rest of Phase 3: P3-004->005->007). Deps P3-001 Done; no owner gate. Design derived from canon (no product-semantics gap forced): STRAT-004 optional AI recommendation = a MODEL call (metered; FakeModelProvider, live deferred CDR-026 §0); NEVER auto-selects; no defensible rationale -> no recommendation. New immutable table (columns on the immutable generation impossible). NO new audit event (backlog Audit=—; changes no state; only the gateway usage event). Branch p3-003-comparison-recommendation from a8ace01; draft PR #35; CDR-036.
- P3-003 slices, TDD, focus-verified. Contracts 35b7663: parseStrategyRecommendation (gateway shape-validator; honest abstain) + narrowStrategyRecommendation (single defensive re-entry of the gateway's validated camelCase output) + resolveRecommendation (deny-by-default: one option-in-range + non-blank bounded rationale + sensitivities, else abstain) + StrategyRecommendationDTO + optional recommendation field on StrategyGenerationDTO; strategy.recommend@1 template; strategy:recommend authz (owner|viewer). Migration 0023 eb4274f (strategy_recommendations -- immutable append-only, dual-keyed FORCE RLS, SELECT+INSERT, FK generation+option cascade, rationale/sensitivities CHECKs; StrategyRepository insertRecommendation/latestRecommendation/findOption; table in EVERY reset list + ALL_TABLES + catalog TENANT_TABLES/EXPECTED_GRANTS/column-privileges + database existence, FK-safe drop order; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Core 2875eb2 (recommendStrategy: authz -> load generation options -> gateway -> narrow -> resolve -> persist ONE immutable row OR honest abstain; NEVER writes selection/decision/state; recommendation_failed on gateway/malformed; metered; getLatestStrategyGeneration surfaces the latest recommendation; strategyRecommendationValidator seam). NO audit event (CDR-036). Local real-PG all green: recommendation 7/7, migration 6/6, strategy-generation 12/12; contracts strategy 25 unit; full recursive typecheck/lint/secrets/boundaries clean; unit 1004. Docs: CDR-036, DATA-ARCHITECTURE, AUTHORIZATION, EVENT-CATALOG, PROJECT-STATE, this log.
- P3-003 independent review PASS (no Blocker/Critical/High); a double-parse shape mismatch (gateway validatedOutput is camelCase; core re-parsed raw snake_case -> always abstained) self-caught + fixed via narrowStrategyRecommendation (defensive re-entry, no raw re-parse). MERGED squash 55438de (PR #35); exact-head + exact-main CI GREEN zero-skip; branch deleted. Phase 3 3/7; migrations end 0023.
- 2026-07-25 (3rd 8-hour window) -- NEW window. State validated: main==origin/main==55438de (P3-002/P3-003 merged), tree clean, migrations end 0023, 3 SECURITY DEFINER. One-time housekeeping FIRST (owner-directed): added .gitattributes (* text=auto eol=lf + explicit per-type eol=lf) + git add --renormalize .; ends the recurring Windows CRLF/NUL cleanup (git now normalizes CRLF->LF on add). Verified typecheck/lint/boundaries clean post-renormalize. MERGED squash c645e8e (PR #37); exact-main CI GREEN. Then continued ACBP-P3-004 (owner-authorized finalize path; CDR-037 phase-limited-approval interpretation owner-approved as-is).
- P3-004 slices, TDD, focus-verified. Contracts c4d57c7: SELECTION_MODES {select,edit,combine,reject} + PHASE_SCOPES {first_phase,whole_plan} (STRAT-005 value set) + guards; validateStrategyDecision (deny-by-default per-mode: select -> in-range ordinal; edit/combine -> owner-supplied 16-field object reusing isCompleteOptionFields, NO model call; reject -> non-blank bounded reasons; phaseScope null for reject) + StrategySelectionDTO + optional selection field on StrategyGenerationDTO; strategy.selected audit event+factory (subject=selection id; metadata {mode}+phase_scope when set, never content) + exhaustive AUDITED_OPERATIONS partition entry; strategy:select authz (OWNER-ONLY). Migration 0024 806606b (strategy_selections -- immutable I, dual-keyed FORCE RLS, SELECT+INSERT only; mode/phase_scope enum CHECKs + per-mode shape CHECK + reject-no-phase + bounded reasons + jsonb-object chosen_fields; composite FK (selected_option_id,generation_id)->strategy_options same-generation; FK cascade; StrategyRepository insertSelection/latestSelection; table in EVERY reset list (36) + ALL_TABLES + catalog TENANT_TABLES/EXPECTED_GRANTS/column-privileges + database existence, FK-safe drop order; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Core 5ec73b5 (recordStrategyDecision: OWNER-ONLY strategy:select -> load generation options -> validateStrategyDecision -> resolve option by ordinal -> persist ONE immutable selection + strategy.selected in one tx audit-or-nothing; records a SELECTION ONLY -- NO decision record (P3-005), NO planning unlock (P4 boundary); getLatestStrategyGeneration surfaces the latest selection). Local: contracts strategy unit + core strategy/audit 33 unit; strategy-selection real-PG 11 + strategy_selections migration 10 discovered (local PG down -> skipped; hosted CI is the evidence); full recursive typecheck/lint/secrets/boundaries clean. Docs: CDR-037, DATA-ARCHITECTURE, AUTHORIZATION, EVENT-CATALOG, PROJECT-STATE, this log.
- P3-004 independent review PASS after fix (nine dimensions upheld: owner-only authz, audit privacy {mode}+phase_scope, audit-or-nothing, selection-only scope, deny-by-default validation, DB immutability + composite FK, reset-list hygiene, boundaries, correctness). One HIGH self-inflicted regression caught + fixed at 684cfde: the Slice-2 reset-list pass used PowerShell Get-Content -Raw (PS 5.1 reads Windows-1252), double-encoding every em-dash/box-char in 36 pre-existing integration files into UTF-8 mojibake; restored each from main and re-applied only the one-line insertion via .NET ReadAllText/WriteAllText (zero c3 a2 bytes verified). Ledger P3-004-REVIEW-COVERAGE.md. Exact-head CI 30157609111 GREEN zero-skip (1665/1665, 148 files); MERGED squash 50bbaa8 (PR #36); exact-main CI 30158606094 GREEN zero-skip (1665/1665); branch deleted local+remote. Phase 3 4/7; migrations end 0024.
- ACBP-P3-005 selected (recomputed unblocked set from the full backlog: P0-005 Blocked; P2-011/P7-006 standing owner gates; P3-005, P4-004, P4-005, P5-009, P5-010 available). P3-005 is highest leverage - sole unblocker of P3-007 + P4-001 (and transitively P4-003/006/007), no owner-decision flag. Canon verified FIRST-HAND (not just via the discovery subagent): STRAT-006 REQUIREMENTS.csv verbatim "Every strategy selection/edit/rejection creates a durable decision record linking inputs, options considered, and rationale" / "immutable, timestamped, linked to the understanding version" / "Failed record writes block the transition (decision is not silently unrecorded)"; J-08 MASTER-PRD-v1.md:375 "decision recorded before any planning". Branch p3-005-decision-records from 50bbaa8; draft PR #38; CDR-038.
- P3-005 slices, TDD, focus-verified. Contracts 4896de0: DecisionDTO + optional decision field on StrategyGenerationDTO; normalizeDecisionRationale (optional rationale -> null so a decision is never silently unrecorded for lacking one; present-but-unusable -> deny); decision.recorded audit event + factory (subject = the DECISION id; SCALAR metadata {understanding_version, options_considered_count, mode} - audit metadata forbids arrays, so EVENT-CATALOG options_considered[] is carried as a count recoverable from generation_id; never content/rationale); decision:record authz OWNER-ONLY; new Decision domain in the exhaustive AUDITED_OPERATIONS partition. Migration 0025 bb65087 (decisions -- immutable I, dual-keyed FORCE RLS, SELECT+INSERT only so UPDATE/DELETE both fail = the STRAT-006 "mutation attempts fail" criterion; composite FK (selection_id, generation_id) -> strategy_selections(id, generation_id) via an additive UNIQUE(id, generation_id) [the 0023 trick, dropped by down()]; optional bounded rationale + version CHECKs; FK cascade; repo insertDecision/latestDecision/findSelection; table in EVERY reset list (37) + ALL_TABLES + catalog TENANT_TABLES/EXPECTED_GRANTS/column-privileges + database existence, FK-safe drop order; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Core e1c4a6d (recordDecision: owner-only -> load generation -> resolve the selection [must be in-scope AND belong to THIS generation, turning the composite-FK throw into a clean not_found] -> snapshot understanding_version + options-considered count -> persist ONE immutable decision + decision.recorded in ONE tx = the STRAT-006 "failed writes block the transition" guarantee; unusable rationale denies before any tx work; getLatestStrategyGeneration surfaces the latest decision). Reset-list pass used the UTF-8-safe .NET writer this time (the P3-004 lesson) - zero mojibake. Local: full unit 1018 passed / 0 failed; decisions real-PG 9 + decision-record real-PG 9 discovered (local PG down -> skipped; hosted CI is the evidence); recursive typecheck/lint/secrets/boundaries clean. Docs: CDR-038, DATA-ARCHITECTURE, AUTHORIZATION, EVENT-CATALOG, PROJECT-STATE, this log.
- P3-005 independent review: first pass FAIL (1 Blocker, 1 High, 3 Medium, 7 Low) -> re-review PASS (0 Blocker/Critical/High/Medium). BLOCKER was self-inflicted: the backlog "mark Done" edit used the regex ACBP-P3-005,[^\r\n]*,Ready,Planned which ALSO matched mid-line where ACBP-P3-007 and ACBP-P4-001 name ACBP-P3-005 in their DEPENDENCIES column, silently flipping both unrelated tickets to Done (the next window would have skipped planning + Slice C entirely). Fixed with a line-anchored edit + diff verification; memory rule saved. MEDIUM-2 was a real design gap: CDR-038 claimed the planning gate keys off a NON-reject decision but nothing on the decision recorded the mode, so the obvious P4-001 predicate would have let a REJECTION unlock planning -> added an IMMUTABLE mode snapshot column + DecisionDTO.mode + the later-selection test; CDR/DATA-ARCHITECTURE/AUTHORIZATION now all state the gate as mode <> reject. Fixes f90a1ad; ledger 3f5cbf6; re-review corrections f480ba5. Exact-head CI 30159997800 GREEN zero-skip (1695/1695, 150 files); MERGED squash 766b674 (PR #38); exact-main CI 30160181588 GREEN zero-skip; branch deleted. Phase 3 5/7; migrations end 0025.
- ACBP-P4-001 selected (recomputed unblocked set; P3-005 merge unblocked P4-001 + P3-007). P4-001 is the biggest remaining unblocker (opens P4-003 -> P4-006 -> P4-007) and the direct consumer of the decision record. Canon: ROAD-001 (Must) "Selecting a strategy generates goals, a roadmap, milestones, and initial tasks ... partial roadmaps are labeled"; ROAD-002 (Should) "versioned and editable; changes flag affected tasks and record rationale ... version write failure blocks the edit rather than losing history"; J-09; API-CONTRACTS "Owner (edit)". ADR-016 object storage is BLOCKED (P0-005), so roadmap content stays in Postgres. Branch p4-001-goals-roadmap-milestones from 766b674; draft PR #39; CDR-039.
- P4-001 slices, TDD, focus-verified. Contracts 0cf8a15: parseRoadmapOutput (deny-by-default - ONE malformed member rejects the WHOLE plan; a milestone goal_ordinal must reference a goal present in the same payload so a milestone can never dangle; an empty plan is rejected because empty is not an honest partial; the partial flag is never coerced) + narrowRoadmapOutput + normalizeEditReason (REQUIRED, unlike a decision rationale) + closed value sets + DTOs; planning.roadmap template family + planning.roadmap@1 whose prompt forbids invented dates (ADR-019) and forbids tasks (P4-003); roadmap:generate/:read owner|viewer + roadmap:edit OWNER-ONLY; roadmap.generated + roadmap.edited audit events with SCALAR metadata (EVENT-CATALOG task_ids[] cannot be metadata -> count) + a new Planning domain in the exhaustive AUDITED_OPERATIONS partition. Migration 0026 b517dee (roadmaps versioned append-only + goals + milestones + task_review_flags, all dual-keyed FORCE RLS SELECT+INSERT; supersedes-chain + edit_reason shape CHECKs; composite same-version goal FK; PLUS the additive tasks_milestone_fk ON DELETE SET NULL closing the P4-002 review NOTE - the P4-002 test that passed a synthetic milestone UUID now seeds a real chain, and that breakage IS the fix working; PlanningRepository + latestDecisionForCompany; four tables in every reset list (38) + ALL_TABLES + catalog + existence, REORDERED CHILD-FIRST since tasks must now be deleted before milestones). Core 593051a (generateRoadmap: classifyPlanningGate exported so the persist tx re-applies the SAME rule - no drift; a later rejection re-blocks; partial honesty; version+goals+milestones+audit in one tx; metered by the gateway) and 7c2cbad (editRoadmap: OWNER-ONLY, version-guarded, authz-before-validation, new version + affected-OPEN-task flags + roadmap.edited in one tx so history cannot be lost). Reset-list pass used the UTF-8-safe .NET writer - 0 mojibake. Local: full unit 1036/0; planning real-PG 11 + generation 12 + edit 7 discovered (local PG down); recursive typecheck/lint/secrets/boundaries clean. Docs: CDR-039, DATA-ARCHITECTURE, EVENT-CATALOG, AUTHORIZATION, PROJECT-STATE, this log.
- P4-001 independent review: first pass FAIL (1 High, 6 Medium, 8 Low) -> fixes 199f303 -> re-review found a NEW BLOCKER introduced by one of the fixes, which hosted CI caught simultaneously: the LOW-6 fail-closed guard (refuse to plan when the decided option has no fields) broke all 9 roadmap-edit real-PG tests because that suite seeded an empty {} option. The BEHAVIOUR was right, the FIXTURE was unrealistic, and the branch had ZERO coverage of the new guard - the suite is skipIf-gated and local PG is down, so it was invisible until CI ran it. Fixed at 2220a44 (real decided content + an explicit test pinning the branch) plus MEDIUM-7 (the guard returned no_decision while a decision IS recorded - now generation_failed). H1 was the substantive one: editRoadmap reached a roadmap INSERT with NO planning gate, so a rejection could be side-stepped by revising instead of regenerating; gated identically and ratified as CDR-039 G9. Also fixed: narrow weaker than parse (G10), a milestone-less plan labelable complete (G11), the tasks_milestone_fk NOT tenant-pinned (RI bypasses RLS - now composite with a column-scoped ON DELETE SET NULL, G14), a raw unique-violation instead of stale (G13), re-flagging silently stopping after the first revision (G12), and CLOSED_TASK_STATES restating the contract. Ledger P4-001-REVIEW-COVERAGE.md. Exact-head CI 30162731461 GREEN zero-skip (1755/1755, 154 files); MERGED squash 00a580d (PR #39); exact-main CI 30162937309 GREEN zero-skip; branch cleaned up. Phase 4 2/7; migrations end 0026.
- 2026-07-25 (4th window) -- NEW window, started immediately after the window-3 report with no pause (standing owner directive: relaunch every 8h, never wait). Disk check at window start: E: 10.95 GB free of 244 GB - above the 3 GB threshold, so noted and continued (the owner freed ~10 GB after the window-3 report flagged 0.97 GB). State: main==origin/main==00a580d, tree clean, migrations end 0026. Selected ACBP-P4-003 (task generation + chat steering) - the biggest remaining unblocker (opens P4-006 -> P4-007) and the direct consumer of P4-001 milestones. Canon verified first-hand (PLAN-001/002 in REQUIREMENTS.csv; the seven "Initial task types" at MASTER-PRD-v1:510). Branch p4-003-task-generation from 00a580d; draft PR #40; CDR-040.
- P4-003 slices, TDD, focus-verified. Contracts 7fe3c4b: TASK_TYPES (the seven PRD types, closed); parseTaskPlanOutput (deny-by-default - ONE malformed task rejects the WHOLE plan; a task can NEVER name a milestone that does not exist, enforced before the DB FK; a MALFORMED type rejects while an ABSENT one is null, explicitly missing not guessed; PLAN-001 3+ relaxes ONLY on an honest partial, itself never coerced); parseSteeringOutput (the three PLAN-002 answers behind a CLOSED REQUIRED discriminator - defaulting an absent outcome to "tasks" would be the guessed execution the failure clause forbids; NO 3+ minimum on steering); narrow* re-applying the PERSISTABILITY invariants (the CDR-039 G10 lesson); planning.tasks + planning.task_steering families (two, because the registry asserts slots <-> placeholders exactly); task:generate authz owner|viewer; NO new audit event. Migration 0027 65e83be (ALTER-only: tasks.task_type closed CHECK nullable + tasks.priority integer RANK >= 0, both INSERT-ONLY so the pinned (state, updated_at) column grant is untouched and the catalog forbidden-column assertion now names both; an index on (company_id, priority); real-PG proving all seven types, NULL, an unknown type refused, -1 refused, and both columns un-updatable while state still flips). Core 8ebbb64 (generateTasks + steerTaskPlanning over one gated pipeline: classifyPlanningGate reused + a current roadmap required; STRAT-005 enforced by showing only in-scope milestones AND re-resolving every ordinal server-side at persist; drafts as the preview so no audit; no-phantom-tasks on failure/malformed/sub-3; rank continues across runs; stale_decision + stale_roadmap re-verified in the persist tx; taskPlanOutputValidator carries the in-scope count into the seam). Local: full unit 1055/0; planning real-PG 38 discovered but SKIPPED (local PG down - skipped is not green); recursive typecheck/lint/secrets/boundaries clean; 0 mojibake. Docs: CDR-040, DATA-ARCHITECTURE, AUTHORIZATION, EVENT-CATALOG, PROJECT-STATE, this log.
- ACBP-P5-001a selected (first of the twelve ratified safety-critical sub-scopes; owner approved my own 3-way splits for P5-001/P5-003/P6-001/P6-007 on 2026-07-27). Branch p5-001a-job-store-tenant-stamping from 223f8e5; draft PR #50; CDR-049. THE LOAD-BEARING CALL WAS THAT WE OWN THE JOB TABLE: the Objective's "library per ADR-008" reads naively as "adopt pg-boss and use its job table", which cannot carry a NOT NULL tenant stamp or dual-keyed RLS because those libraries own their DDL - and the owner's ADR-008 amendment already settled it ("job tables remain standard SQL (exit path)"; §13 "library-independent design"), so this needed no gate and P5-001a takes NO library dependency at all. Migration 0031 b4cb180 (jobs - company-owned, dual-keyed FORCE RLS; account_id/company_id NOT NULL and immutable; closed 6-state CHECK including dead_letter declared UP FRONT so P5-001b/c extend rather than reshape; bounded kind/payload/idempotency_key; attempts >= 0; PARTIAL unique index on (company_id, idempotency_key) because a global unique would let one tenant's key reveal another's; pickup index; grants SELECT+INSERT plus a COLUMN-scoped UPDATE(state, updated_at, attempts) and NO DELETE per §4-G5, job history being the run trail; three FKs matching the tasks precedent exactly; jobs added to every reset list + ALL_TABLES + the adversarial catalog; no new SECURITY DEFINER (still 3)/role/BYPASSRLS). Contracts: closed JOB_KINDS + JOB_STATES; validateJobRequest refuses and NEVER repairs - no defaulting, no truncation (truncating two distinct idempotency keys would silently MERGE two jobs), payload bound deliberately HALF the DB CHECK so the contract is the error surface and the CHECK a true backstop; job:enqueue authz OWNER-ONLY (canon does not settle the role, so this took the safer reversible reading - widening later is additive, discovering viewers had been scheduling background execution is not); job.enqueued audit with metadata exactly {kind, deduplicated}, never the payload or the tenant ids. Core enqueueJob: audit-or-nothing, per-company idempotent, conflict scoped to the exact partial index rather than a blanket 23505. Real-PG proof of all THREE refusal layers, each reached the only way it can be. Local gate all exit 0; real-PG suites skip locally so hosted CI on the exact SHA is the evidence.
- P5-001a review pass 1 FAIL (1 High, 2 Medium) -> pass 2 FAIL (1 Medium) -> hosted CI found 2 more, all fixed at 7d1be1d. THE HIGH IS THE ONE WORTH REMEMBERING: the acceptance clause's refusal was UNREACHABLE. runInCompanyScope trims and denies a blank company id ITSELF, before the callback runs, so a context-stripped enqueue returned `forbidden` - indistinguishable from an authorization failure. "Context-stripped job refused" is not satisfied by "no row is written" (already true); it requires the failure be DISTINGUISHABLE so the platform can alarm on it, and the layer meant to expose the problem was hiding it. Neither the unit tests (which call the validator directly, bypassing the scope) nor local runs (real-PG skips) could reach it - reading found it before CI did. Fixed by splitting validateJobTenancy out and running ONLY it before authorization: it reports on the shape of ids the caller supplied and discloses no platform state, whereas invalid_kind/payload_too_large stay behind the authz check where a reason would be a real oracle. Regression test drives five context-stripped shapes through a LEGITIMATE owner. Also pass 1: the row was stamped from caller params rather than scope.tenant (equal on this path, but equal by coincidence, and this is the one ticket whose subject is that tenancy is a grant not a claim); and the unresolvable-conflict branch returned refused:invalid_idempotency_key, a lie that would send a caller to change a correct key and retry forever (now its own conflict_unresolved status). Pass 2: the contract did not mirror the migration's closed state set - added JOB_STATES + a real-PG test asserting every declared state is accepted. CI-only defects, both mine: PostgreSQL will NOT infer a PARTIAL unique index from a bare ON CONFLICT column list (42P10 - the arbiter must restate the predicate), and a COLUMN-level UPDATE grant never appears in role_table_grants (the same reason provisioning_steps/interview_sessions show none), so the catalog's table-level expectation is INSERT/SELECT with the column grant asserted separately. Ledger P5-001a-REVIEW.md.
- 2026-07-28 (window 12, start 02:52 +03:00) — ACBP-P5-002 workflow coordinator, branch `p5-002-workflow-coordinator` from main `9b38d25`, draft PR #55, CDR-053, migration 0035 `task_runs`. THE SEQUENCING QUESTION FIRST: the owner asked how a tool call links back to its run before P5-003b. Canon answers it — DATA-ARCHITECTURE says a tool call "belongs to run" and the backlog says P5-002 owns task runs — so it is a SEQUENCING answer, not a semantic gap: building P5-003b first would need a nullable FK-less `run_id`, the exact hole CDR-049 refused for `jobs.company_id` and worse on the enforcement chokepoint. Order settled as P5-001c → P5-002 → P5-003b (CDR-052 §1). A RUN IS ONE EXECUTION ATTEMPT: the five-state set is small on purpose, because §4's waiting_for_*/paused/blocked_by_policy are TASK states owned by P4-002 and collapsing them would make "which attempt failed, and why?" unanswerable (CDR-053 §2). Slices: 5f0d0f9 contracts (21 tests, pure — the clock is a parameter, `isRunLost` fails closed when a running run has neither heartbeat nor start time); 0035 + TaskRunRepository (tenant-pinned composite FK, guarded additive `tasks_id_company_uq`, UNIQUE(task_id,attempt) so a claim is atomic and the loser is TOLD rather than given a duplicate, column-scoped UPDATE, NO DELETE); c6143ca core + real-PG proof of all three acceptance clauses. Two authz actions — `run:execute` (worker) and `run:cancel` (owner) — because a worker able to cancel its own run could hide work it had been told to stop. Three audit events; `task.completed` deliberately NOT registered (canon requires artifact_refs[], and a run succeeding is not a task completing).
- P5-002 REVIEW PASS 1 FAIL at 91df4af. THE HIGH: `cancelRun` could tell an owner "already terminal" about a RUNNING run — if a queued run is picked up between the read and the guarded write the update misses, and `already_terminal` means "there was nothing left to stop", so the owner walks away believing their cancellation landed while the AI keeps acting. Now re-reads and answers honestly; a run found running becomes a safe-stop. Proven with a ROW LOCK, not a sleep: a held-open transaction moves the run to running without committing, so under READ COMMITTED the read still sees queued while the guarded UPDATE blocks; committing releases it, PostgreSQL re-checks the predicate against the new row version, and the guard misses — deterministically, every time. The helper polls pg_locks/pg_blocking_pids and THROWS if nothing ever blocks, so the test cannot silently degrade into proving the ordinary path. Also: typed `invalid_attempt`/`task_not_found` instead of raw constraint errors reaching a caller as an opaque 500; and an unreachable post-claim branch now throws rather than returning a false `attempt_taken`.
- P5-002 REVIEW PASS 2 FAIL at facca72, and the worse of the two passes. `startRun` would begin executing a task the owner had DELETED: `tasks` has no DELETE grant so the row survives, deletion lives in `task_deletions`, and my probe was a raw selectFrom — findById semantics, which TaskRepository's own docstring says NO product read may use. Owner deletes a queued task, coordinator calls startRun, run goes to running, `task.started` fires. Now reads through `findLive`, and a deleted task answers `task_not_found` — the same answer a foreign task gets, so not an existence oracle. The same probe also accepted a task in ANY state, so a completed/failed/cancelled task could acquire an execution attempt; new contract `canStartRunForTask` DERIVED from canon's own transition table (startable = already running, or can legally reach running) refuses those plus draft/planned with `task_not_startable`. HOW IT HID, worth remembering: every P5-002 test started runs against DRAFT tasks, because createTask mints a draft and nothing in the suite moved it on — the fixtures agreed with the bug, the second time on this ticket family that a convenient fixture accommodated a defect rather than catching it. `newTask` now returns a QUEUED task. Ledger `docs/implementation/P5-002-REVIEW.md`.
- 93cf68e, deliberately outside P5-002 and called out as such: stripped stray control characters from three committed files. A PowerShell backtick inside a double-quoted string is an escape, so prose about code identifiers silently lost the backtick AND the following letter — `retry_state`→CR+"etry_state", `running`→CR+"unning", `attempts`→BEL+"ttempts" (audit.ts), `attempts_exhausted`/`attempts == maxAttempts` (retry.ts), `forbidden`→FF+"orbidden" (EXECUTION-LOG.md). Nothing broke a build (all inside comments/prose), but the BEL made git classify audit.ts as BINARY, silently disabling line-ending normalization for it. Scanned every tracked text file; those were the only ones. The 0x01 in object-key.test.ts is left alone — a deliberate adversarial fixture, not corruption.
- 2026-07-28 (window 12) — ACBP-P5-002 MERGED squash f3452fc (PR #55); exact-head CI on 8822b30 and exact-main CI on f3452fc both green zero-skip (2201/2201); branch deleted local+remote; main==origin/main==f3452fc; migrations end 0035. ACBP-P5-002 done.
- ACBP-P5-003b tool dispatcher, branch `p5-003b-dispatcher-chokepoint` from main `f3452fc`, draft PR #56, CDR-054, migration 0036 `tool_calls`. CANON SETTLED THE HARD QUESTION: diagram 07-worker-execution fixes the order (allowlist -> stop+policy+approval -> "fail closed + audited"), COMPONENT-CATALOG names the component "Trusted - THE enforcement chokepoint" with failure mode "Fail closed on any gate outage", and IMPLEMENTATION-ROADMAP section M5 states the Phase 5 envelope verbatim: "P5 execution is gated by user-initiated runs on informational-class tools only." That last sentence is the answer to what a dispatcher does when the policy engine it must consult does not exist yet - so CLASSES_THAT_PROCEED_WITHOUT_A_GATE is exactly ['informational'], waiving only `unavailable` and never `deny`, and waiving nothing else. Deliberately not a config value: a knob there would be a knob that turns the chokepoint off.
- P5-003b slices: 802754f contracts (26 tests, pure - the gate is exercised as function calls rather than through a database, because a gate reachable only through I/O is a gate that is mostly untested; reasons come in PAIRS because each pair has different fixes - no_allowlist vs not_allowlisted, policy_unavailable vs policy_denied per TOOL-003's separate consequence for unavailability, stop_unavailable vs emergency_stopped, approval_required vs approval_invalid). 7576b23 migration 0036 + repository + the 40-list sweep + catalog. 42899c2 core dispatchToolCall + reportToolCallOutcome + three audit events. 9cb7d78 real-PG suite - 20 tests GREEN ON THE FIRST HOSTED RUN, zero skips (2260/2260).
- P5-003b design decisions worth keeping: `tool_id` has NO FK to tool_definitions, because TOOL-001 requires the ATTEMPT to be audited and the commonest refusal IS an unregistered tool - an FK would make the required record impossible to write. risk_class/tool_version/external_effect are SNAPSHOTS of the gate actually applied, since re-reading the registry later would misreport a past call. external_effect is a BOOLEAN rather than class names inside the receipt CHECK, so the constraint survives a re-shaping of the risk-class set (which CDR-051 section 0.1 now flags as open). Idempotency is checked BEFORE the gates: a duplicate is not a new call to authorize, and re-running the gates could produce a DIFFERENT answer for a call that already happened. Authz reuses run:execute rather than adding tool:dispatch - per-tool least privilege is the ALLOWLIST's job (WORK-005), and a role check cannot express "may read the web but not send mail".
- P5-003b REVIEW PASS 1 FAIL at 1b779f1, two findings of one shape - a value that LOOKS present while carrying no meaning. A blank idempotency key was treated as a real key, so two unrelated calls both passing '' collided on the unique index and the second was reported as a DUPLICATE of the first, returning another call's record as though it were this one's. And a whitespace receipt satisfied the CHECK that TOOL-002 exists to enforce: the use case tested trim(), the DATABASE tested only `is null`, so the layer meant to hold when something skips the use case was the layer that let it through. Both fixed at both layers and proven against real PG.
- P5-003b REVIEW PASS 2 FAIL at 66feb40. THE FINDING: the record named the tool but not its VERSION, and EVENT-CATALOG pairs them ("tool_id+version"). tool_definitions is versioned and the dispatcher resolves the ACTIVE highest version, so without it a re-registration at v2 makes every earlier record ambiguous about which definition - and therefore which risk class - actually applied. On a table whose entire purpose is being the evidence of what the chokepoint decided, that ambiguity IS the defect. Also: outcome and risk_class CHECKs still had one-directional drift guards after pass 1 shipped one for denial_reason only - the THIRD consecutive ticket with that same gap (P5-003a pass 2, P5-002 pass 2, now here), which is itself the lesson: shipping the guard for one constraint does not generalize on its own. Ledger P5-003b-REVIEW.md.
- CORRECTION recorded this window (CDR-051 section 0.1, risk-class.ts header, AUTONOMOUS-RUN-LOG FLAG): canon DOES enumerate the risk classes. APPR-001 says verbatim "informational, internal-reversible, external, or sensitive-irreversible". My P5-003a flag said canon "never enumerates" them and the owner approved the four-class set on that basis. The count matches but the FOURTH CLASS DIFFERS: canon's sensitive-irreversible is about sensitivity and irreversibility wherever they occur, mine ties the top two classes to EXTERNAL effects, so an irreversible INTERNAL action classifies two rungs too low. LEFT UNCHANGED AND FLAGGED under the owner's Phase-6-caution rule - it is their reserved decision and the correction changes the premise of their approval rather than settling it. Nothing is blocked: P5-003b keys off resolveRiskClass and the ordering only, never the names. The search failure is the part worth remembering: "canon is silent" is a claim about ALL of canon, and I asserted it after checking only the architecture layer.
- Guard fix this window: tools/check-reset-lists.mjs had a FALSE NEGATIVE - it asked whether a table name appeared anywhere in the FILE, so a suite that dropped nothing but merely asserted expect(names).toContain('task_runs') satisfied it. database.integration.test.ts was live in exactly that state (its drop list omitted task_runs; only a CASCADE from `tasks` was removing it). The check now scopes to the array literals themselves, strips comments first because the real lists are written one table per line with comments between them, and reports "<no readable table list found>" loudly rather than passing. Three new self-tests, one reproducing the false negative directly.
- 2026-07-28 (window 12) — ACBP-P5-003b MERGED squash c9c4a5e (PR #56); exact-head CI on 2e16af8 and exact-main CI on c9c4a5e both green zero-skip (2265/2265, 24 dispatcher tests); branch deleted local+remote; migrations end 0036. Late CI catch on that ticket worth keeping: `tool_version: null` in audit metadata throws "Audit metadata value type is not allowed" (metadata is scalars only), which broke auditing for exactly the UNREGISTERED-tool refusal - the attempt TOOL-001 most wants recorded. Omit the key rather than nulling it.
- ACBP-P5-003c injection boundary, branch `p5-003c-injection-boundary` from main `c9c4a5e`, draft PR #57, CDR-055, migration 0037 (ALTER-only, adds the `untrusted_context` denial value). THE READING THAT CARRIES THE TICKET: CDR-054 established canon's Phase 5 envelope (no engine -> informational proceeds, everything above refused); AI-AND-WORKER-ARCHITECTURE section 4 separately requires "heightened policy scrutiny on any tool call proposed while processing" untrusted content. Heightened can only mean MORE refusal, so the informational waiver is WITHDRAWN while untrusted content is in the working context - and in Phase 5 there is no engine to replace it. That makes "zero unauthorized tool executions" STRUCTURAL: it does not depend on detecting the injection at all. Three of the nine corpus entries are written to match no signal, and the test asserts both that the detector finds nothing AND that the call is refused anyway.
- P5-003c design: wrapUntrusted does NOT sanitise (a filter can be evaded; a value that never reaches an instruction position has nothing to evade - and rewriting content would corrupt the evidence a human later reads). detectInjection returns SIGNALS with no `safe`/`clean` field, so the claim the heuristic cannot support is not spellable. The dispatcher takes the context ITEMS and classifies them itself; hasUntrustedContext treats anything unrecognised as untrusted. Corpus asserts on the tool_calls TABLE, not a return value and not a mock, plus a CONTROL test that the same call on the trusted path is authorized - without it every corpus assertion would be satisfied by a dispatcher that refuses everything.
- P5-003c REVIEW PASS 1 FAIL at 6e9b6bd: `context` was OPTIONAL, so a forgotten context defaulted to the trusted path - now REQUIRED, and the compiler found all fifteen call sites. And detectInjection shipped uncalled; it now runs on the live path and records its signals in the audit payload (closed vocabulary, never content; key ABSENT rather than '' when nothing matched, because '' would have to be read as either "none matched" or "not checked").
- P5-003c REVIEW PASS 2 FAIL at 1b20368, AND THIS IS THE ONE THAT MATTERED. I had listed `tool_output` as TRUSTED input alongside `trusted_user_input`. Complete bypass: a web-fetching tool returns a page -> its output enters the context labelled tool_output -> reads as trusted -> waiver not withdrawn -> the injected instructions are back inside the boundary. Every corpus test still passed, because the corpus wraps with wrapUntrusted; the hole was one label away and nothing exercised it. Canon NEVER called tool output trusted - section 4 says "per-tool class", the TOOL decides, and a per-tool mapping needs P5-004's registry. I had turned an unresolved question into a permissive default while writing the module whose subject is that mistake. TRUSTED_INPUT is now exactly ['trusted_user_input']; semi_trusted_generated stays out too (generated documents are "never auto-promoted to fact", and a model's own output is how an injection persists across steps). Proven at contract and real-DB level. Ledger P5-003c-REVIEW.md.
- Recorded as a deliberate deviation (CDR-055 section 5): this module is STRICTER than canon's table - canon attaches heightened scrutiny only to external web content, here every class except trusted_user_input withdraws the waiver. Conscious over-restriction in the safe direction while no per-tool trust mapping exists; costs nothing today because nothing fetches external content yet. P5-004 inherits the question.
- 2026-07-28 (window 12) — ACBP-P5-003c MERGED squash 83477a5 (PR #57); exact-head CI on 6459130 and exact-main on 83477a5 both green zero-skip (2294/2294); branch deleted; migrations end 0037. ACBP-P5-003 COMPLETE (a 5381389 + b c9c4a5e + c 83477a5).
- ACBP-P5-004 worker definitions, branch `p5-004-worker-definitions` from main `83477a5`, draft PR #58, CDR-056, migration 0038. THE POINT: CDR-054 section 1-G3 and CDR-055 both deferred where the dispatcher's tool allowlist comes FROM. It was a caller-supplied parameter, making WORK-005's "server-enforced" true mechanically and not authoritatively. It now comes from a versioned definition, which is what trust-critical #4 says. Proven end to end: resolve the worker, hand the allowlist to dispatchToolCall, watch a tool the definition does not list get refused BY THE DEFINITION.
- P5-004 shape: TWO tables answering to two owners. worker_definitions is GLOBAL platform config, versioned (worker_id, version), SELECT-only for the app role - canon says workers are "versioned configuration over one shared runtime, not independent agent services", so a definition the product could rewrite at runtime is not a control. It carries canon's own eleven fields (AI-AND-WORKER-ARCHITECTURE section 2). company_worker_states is TENANT data because WORK-006 is per company, keyed on worker_id WITHOUT a version - an owner pauses "the research worker", and pinning to a version would un-pause it the moment a new one was registered. worker:control is OWNER-only (canon: "granular emergency control", beside ADMIN-001).
- P5-004 TDD caught a gate inversion before it shipped: requiresApproval passed the threshold straight to isAtLeastAsRestrictiveAs, which RESOLVES it - so an unrecognised threshold became the MOST restrictive class, nothing could meet it, and one typo in a definition would have switched approval off for EVERY class. The P5-003a riskRank inversion in a new place. The threshold is validated BEFORE it is resolved. Lesson worth keeping: resolve-then-compare is safe for the candidate and dangerous for the threshold.
- P5-004 REVIEW PASS 1 FAIL at 04b5b3e. THE HIGH: CDR-056 section 2-G4 said the MVP zero-external-actions boundary was enforced STRUCTURALLY - canon's own word - and I had written isMvpSafeAllowlist, tested it against every class, and then called it from nothing. A definition allowlisting send_email would have resolved cleanly. A guarantee documented and not implemented is WORSE than one never claimed, because the document is what the next person trusts instead of re-checking. It cannot be a CHECK (allowed_tools is a text[], classes live in another table, CHECKs cannot subquery), so it is enforced at RESOLUTION - a violating definition may exist and can never be USED. An allowlist naming an unregistered tool fails it too. Also pass 1: toolRiskClasses returned every version, so a tool re-classified DOWN would have kept being refused by its own history; now distinct on (tool_id) order by version desc.
- P5-004 REVIEW PASS 2 FAIL at 8cb2050. WORK-006's failure clause ("Disable during execution triggers safe-stop per TASK-007") is NOT met and was not said so. It cannot be yet: nothing links a run to a worker, task_runs has no worker_id, and the component that knows which worker is executing is the runtime (P5-005) - a nullable unpopulated worker_id now would be exactly the FK-less hole CDR-049/CDR-052 refused. Sequencing, not oversight, but SILENT, and an unmet failure clause nobody wrote down is indistinguishable from one nobody noticed. Now recorded in CDR-056 section 6 naming the two steps that close it. Also pass 2: WORK-001's acceptance ("Registry lists each worker with capabilities and tool allowlist") was unproven and listActiveDefinitions sat uncalled - same shape as P5-003c's uncalled detector. listWorkers added, member-readable on purpose: hiding a pause from viewers would make the control invisible to exactly the people wondering why nothing ran. Ledger P5-004-REVIEW.md.
- P5-004 IOQ-12 (worker execution budgets, decider "Eng + owner", input "P2-P4 usage telemetry"): that telemetry does not exist - no live provider has ever been called - so there is nothing to derive from. Followed the CDR-008 precedent: interim technical values (500_000 micros per run, 600_000 ms), explicitly revisit-bound, no pricing implied, stored as per-definition COLUMNS so changing one is a data change rather than a deploy. NOT owner-ratified; flagged in CDR-056 section 3. The duration default is asserted in tests against DEFAULT_HEARTBEAT_GRACE_MS so it provably leaves room for several heartbeat windows.
- A capability CHECK drift caught at authoring time: my first draft invented `strategic_option_generation` and `general` and wrote `product_requirements` for `internal_product_requirements`. All four vocabulary CHECKs now assert SET EQUALITY against their contracts in the integration suite, written up front rather than after a review pass, because that one-directional gap has been the finding on three tickets running.
- PowerShell backtick corruption struck a THIRD time this window (a template literal eaten inside a double-quoted replacement in worker-repository.ts). Typecheck caught it; repaired with a structural edit. The rule is now absolute: never put code containing backticks through a PowerShell double-quoted string.
- 2026-07-28 (window 13, start 13:38 +03:00) — NEW 8-hour autonomous window. State validated: main==origin/main==2f83f3c, tree clean, migrations end 0039, disk C 14.3 GB / E 104.9 GB free. Selected ACBP-P5-005 (worker runtime) per the owner's standing directive. Branch p5-005-worker-runtime from 2f83f3c; draft PR #61; CDR-057.
- P5-005 CLOSES the clause CDR-056 §6 recorded as UNMET. WORK-006's "disable during execution triggers safe-stop" was unmet for a STRUCTURAL reason, not an oversight: nothing linked a task run to the worker executing it, so "this worker's running work" was not a question the database could answer. `worker_runs` (migration 0040) is that link, and it is canon's own shape rather than a cheaper pair of columns on `task_runs` — DATA-ARCHITECTURE says a Task run HAS a Worker run and EVENT-CATALOG gives the events a `worker_run_id`. Company-owned, dual-keyed FORCE RLS, TENANT-PINNED composite FK to `task_runs (id, company_id)` because RI checks always bypass RLS, `UNIQUE(task_run_id)` ("one worker executes one attempt"; a retry is a NEW task run so this never blocks one), and a COLUMN-SCOPED UPDATE grant that leaves the stamp and the snapshot bounds immutable — a run can never be re-attributed to another worker nor re-judged against a budget it was not given. No DELETE, no new SECURITY DEFINER (still 3) / role / BYPASSRLS. Table swept into all 40 reset lists by the guard.
- Contracts slice (TDD, RED watched first): `decideStepAdmission` is PURE and TOTAL — the clock is a PARAMETER (`elapsedMs`), never read inside — and the check runs BEFORE the step. That ordering IS NFR-015's "no more than one billing increment": a step is admitted only while headroom remains, so the most a run can overshoot by is the single call that crossed the line. Checking afterwards would bound nothing at all. FAIL-CLOSED: an unreadable bound or counter HALTS rather than reading as "no limit", because a run whose bounds cannot be determined is exactly the runaway NFR-015 exists to stop. 28 unit tests.
- Audit: canon's three names registered (EVENT-CATALOG line 183) with canon's payload; subject = the WORKER RUN. A SAFE-STOP is a fourth outcome `stopped` filed under `worker.completed` carrying `run_outcome: 'stopped'` — NOT `worker.failed`, because the run did what the owner asked and counting it as a malfunction would misreport every deliberate intervention; and not silently a completion either, because the payload names which of the two it was. The audit OUTCOME stays `success` on a failed run, following the `planningRunRecorded` precedent (the audited operation is RECORDING the run; `denied`/`blocked` stay meaningful). Optional keys are OMITTED, never null — the P5-003b lesson, where a null in AuditMetadata threw and killed auditing for exactly one refusal path.
- The safe-stop sweep lives INSIDE `setCompanyWorkerState`'s transaction. A disable that committed while its stops did not would leave an owner looking at a disabled worker that is still working — the exact failure the control exists to prevent. The stop is REQUESTED, never forced: durable on the task run, honoured at the worker's next checkpoint, because killing a call mid-flight is how a half-performed external action happens. `setCompanyWorkerState` now reports `stopsRequested`, so "did the disable actually reach work in flight" is answerable rather than assumed.
- The runtime NEVER executes a tool itself — every call still goes through `dispatchToolCall`. The chokepoint is only a chokepoint if the component doing the work cannot go around it. The step is an INJECTED seam because no worker logic exists yet (P5-006/007/008) and no live provider has ever been called; that seam is precisely what makes the budget halt, the duration halt and disable-during-execution provable against a real database TODAY rather than provable against a fake.
- 2026-08-01/02 — ACBP-P6-009 account usage rollups. TWO SESSIONS ran this ticket on one branch simultaneously, and the coordination cost more attention than any single technical problem in it. The collision surfaced only because an `Edit` failed on a stale match: `git status` then showed three files modified that this session had never touched, while HEAD and `origin` both still matched its own last commit. **Every git-head check passes cleanly while another agent's uncommitted work sits in the shared tree** — head comparison cannot detect that class at all. The next step would have been `git add -A`, which would have swept a live writer's in-progress slice into this session's commit under this session's message. Explicit paths only from then on, `git status` re-read immediately before staging, and any file I did not personally touch treated as a stop.
- "COMMITTED" IS NOT "LANDED". The sibling committed Slice 4 locally with a clean tree while `origin` lagged, which reads exactly like finished work. Its transcript tail showed it was holding the push pending its own reviewers, and it later amended. Waiting for `origin/<branch>` to move is the only reliable handover signal.
- THE BASH TOOL IS UNUSABLE IN THIS ENVIRONMENT and it cost two wasted background watches before I checked: every invocation, including `echo hello`, dies with `/usr/bin/bash: -c: line 81: unexpected EOF while looking for matching '` — a broken quote in the profile the tool sources. I misdiagnosed the resulting exit codes twice (once as a deleted branch, once as my own logic) because I read the exit code instead of the output file. The output said the same thing both times. Use PowerShell for anything scripted here, and read the output before theorising about the exit status.
- THE TICKET'S OWN FAILURE MODE, COMMITTED INSIDE THE GATE WARNING ABOUT IT. CDR-073 §0 is written against "the number is wrong and nothing notices". The test written to prove the SQL and TypeScript period derivations agree issued its **own third copy** of the expression and never called either production method — so deleting `at time zone 'UTC'` from both repository queries left every suite in the repo green, because CI runs UTC and the two spellings are then byte-identical. Two comments and the gate itself claimed an integration test held them together. Found by the third independent review, not by me, not by CI.
- THE PATTERN ACROSS THREE REVIEW PASSES: **the implementation was rarely the defect — the tests and the comments were.** All three HIGH findings were real, correct controls that nothing measured; each was proven by deleting the control and watching exactly one test go red. Mutation is what separated a guard from a guard-shaped comment, every time.
- A MUTATION FOUND A DEFECT IN THE TEST DOING THE MUTATING. Deleting the advisory lock made the new concurrency test fail — and its failing `expect` threw before releasing the holder transaction, so three later suites hung to their timeouts and 46 tests reported as skipped. The real failure was buried under noise pointing elsewhere. Release moved into `finally`. I would have shipped that without running the mutation.
- LOCAL POSTGRESQL IS WORTH STANDING UP AND IS NOT A SUBSTITUTE FOR CI. Started the stopped `acbp-local-dev` WSL distro and built an ISOLATED disposable database rather than touch the shared `acbp_test` (a second session might be using it) or the owner's config (backed up, with a restore script). It gave fast real-PG feedback on every targeted suite. But a full local run cannot pass: `acbp_app` is CLUSTER-WIDE and shared with the owner's other databases, so the drop-to-zero migration test cannot complete. Hosted zero-skip CI on the exact SHA remains the only evidence.
- MIGRATION 0051 IS THE 51ST, AND A TEST HAD `for (i = 0; i < 50; i++)` while claiming to be "robust to any number of migrations". Each iteration reverses ONE migration, so it silently under-reversed by one and failed downstream as `expected 1 to be +0` — a message pointing nowhere near the cause. Raised the bound AND made the loop assert *why* it exited, because "ran out of iterations" and "drained the stack" were previously indistinguishable.
- The MVP zero-external-actions boundary is re-checked at `startWorkerRun`, not only at allowlist resolution — checking only in the read path would leave the enforcement one caller away from being skipped, and this is the moment work actually begins.
- My own no-orphan-events guard caught the three new registrations immediately (every registered event must be produced by exactly one approved operation). Working as designed; three operations added.
- Local gate on `87695f0`: typecheck, lint, check:secrets, check:encoding, check:boundaries, check:reset-lists, test:boundaries all exit 0; 1389 passed / 0 failed / 989 skipped. The 19 new real-PG tests skip locally — hosted CI on the exact SHA is the only evidence, per standing discipline.
- 2026-07-30 (window 17, start ~00:30 +03:00) — ACBP-P6-002 dispatcher enforcement. State validated: main==origin/main==2f51bd7, migrations end 0046, disk C 7.5 GB / E 81.5 GB. Local PostgreSQL live on 127.0.0.1:5432 for the whole window, so every real-PG suite EXECUTED rather than skipped — the first window in a while where "green" means what it says. Note for future windows: `ACBP_TEST_DATABASE_URL` must be set in the SAME shell invocation as vitest; a suite that skips reads identically to one that passes, and I wasted a cycle reporting `3 skipped (3)` as evidence before noticing.
- P6-002 landed in four commits after two earlier attempts were drafted and reverted for running short mid-change. Slicing it (wiring → suite → review fixes → cleanup) is what made it land; redoing the same work three times cost more than the smaller step ever would.
- THE LOOSENING'S HOLE. Making the approval demand conditional on policy (PM ruling, CDR-067 §2-G7) meant a policy answer of `allow` left `facts.untrustedContext` with NO EFFECT WHATSOEVER — the NFR-021 injection boundary went dead and laundered content would have reached tools on a plain `allow`. The boundary had been resting on a mechanism that was not its own: untrusted provenance refused a call by WITHDRAWING the waiver, which only worked because an approval was demanded of every non-waived call. Remove the demand, remove the property, silently. Caught by `injection-corpus.integration.test.ts` (7 failures) during the full sweep — nothing in the loosening's own tests mentioned untrusted content, and review had not caught it. The general lesson, because it will recur: **a security property can rest on a mechanism that is not its own, and removing the mechanism removes the property with no local sign.**
- INV-2 EARNED ITS KEEP ON A CHANGE IT WAS NOT WRITTEN FOR. Closing that hole introduced a second read of `facts.untrustedContext`, and the read-counting test failed with `expected 2 to be 1` — a test that fails on a second read even when that read changes no decision. It was written one commit earlier to protect a property CDR-066 had recorded as untestable, and its first version was VACUOUS until a mutation exposed it.
- THE ADVERSARIAL REVIEW WAS SCOPED TO ONE QUESTION and that is why it worked: *find any path where a call proceeds without an approval that policy demanded* — ten named attack lines, an instruction not to trust the comments (which are long and confident), and permission to answer "nothing found". `decideDispatch` held on all ten. Two gaps came out anyway, BOTH IN CODE THIS TICKET TOUCHED BUT DID NOT CHANGE, neither findable from the diff: (a) `toPolicyGateAnswer` forwarded `result.decision` unvalidated — the only link from stored policy to gate not total over `unknown`, and the landing value for an unreadable decision was `unavailable`, which is the ONE gate value the waiver spares, so the failure mode was not a wrong denial but an informational call proceeding on a decision nobody could read; (b) the idempotency short circuit returns BEFORE the policy evaluation, reported a prior DENIED call as `duplicate` (a caller written `if (denied) abort; else proceed;` reads a laundered refusal as permission), and did not bind the key to the arguments.
- WHAT I DID NOT FIX, AND WHY IT IS RECORDED LOUDLY. The reviewer's headline finding is real: `gates.approval` is caller-injectable, so a caller can satisfy a policy-demanded approval with a lambda, and after this ticket the approval gate is the SOLE enforcement of `require_approval`. It is not a regression (the same lambda satisfied the universal demand before) and not reachable (zero non-test callers of `dispatchToolCall` repo-wide — verified independently rather than taken on the reviewer's word, the only other mention being a comment in `runtime.ts` stating it does not call it). Closing it now would make every `require_approval` an unconditional deny with no approval store to consult, leaving the approve-and-proceed path unexecuted until P6-003/004 — the same unreachable-path shape as the D1 defect, and the exact failure the PM ruling was chosen to avoid. Recorded as a REQUIRED CLOSURE for P6-003/P6-004, plus `gates.stop` for P6-007.
- THE TICKET IS NOT DONE AND IS NOT MARKED DONE. Its acceptance row says "three evaluation points wired"; one of three is. Point 2 cannot be attempted before P6-003/004 exist. Point 1 needs an owner ruling, because the engine's observations are tool-shaped (a plan-accept evaluation would answer about nothing) and deciding what a point-1 refusal DOES changes P4-002's state machine — under the owner-ruled baseline, planning is internal work allowed by default, so a point-1 gate refusing planning would deny work the company's own policy permits. Proceeding past it was safe on a specific argument, not on convenience: points 1 and 2 sit strictly EARLIER than point 3, so their absence cannot let an action through, and canon itself calls point 1 "early honest feedback" and only point 3 "never skippable".
- MUTATION TESTING CAUGHT WHAT REVIEW DID NOT, AGAIN — eight mutations this window, every one restored byte-identical against a recorded sha256. The most valuable was not on a new guard: stopping the evaluation from pinning the LIVE policy version turns the supersession-race test red, which is the only evidence that test reaches the property its title names. Two of my own test defects were also found this way rather than by reading: `gates`/`auditWriter` are OPTIONS and folding them into params silently dropped them (three tests failed with default behaviour and looked like product defects), and the `rows[0]?.field` idiom used elsewhere in the package would have let two id comparisons pass on `undefined === undefined`.
- A DOC CLAIM I HAD TO CORRECT ON MYSELF: CDR-067 said P6-001a/b/c were "all merged on local verification". They are not — `main` is at Phase 5 completion and they sit on this same branch, so one branch carries two tickets. Recorded rather than repaired, because splitting it means rewriting pushed history. Also corrected CDR-066 §0.3's mutation figure (four red → the measured 6/10/6 across separately-mutated disjuncts) and CDR-067 §2-G5, which described a two-evaluation-point flow that was never built.
- A CI-HANG RISK I FIXED WITHOUT BEING TOLD TO: the supersession race holds an owner transaction open across an in-flight dispatch. Without `finally`, a thrown dispatch would leave that transaction holding a row lock on `policies` and the next test's `truncateFixtures` would BLOCK rather than fail — a hung run instead of a red one.

## ACBP-P6-004 — payload binding, expiry, revocation, single-use consumption (CDR-069)

- THE SYSTEM CAUGHT BOTH DESIGN ERRORS, NOT THE REVIEW. (a) CDR-069 §1-G5 specified the gate as a read of the REQUEST; rewritten that way a REJECTED approval authorized, because a reject is `decided` too — as is a not-yet-due `schedule` and an `edit_then_approve`. Seven existing tests across the P6-002 and P6-003 suites went red immediately, which is what those suites are for. Both halves are needed: the request says the approval is live and bound to these bytes, the decision says a human said yes. (b) The same gate specified consumption BEFORE the call was recorded, reasoning that a lost race must never leave an authorized call beside an unspent approval; `consumed_by_call_id` is a real foreign key, so the database refused it outright — and the reasoning was unnecessary anyway, because both statements are in ONE transaction and nothing commits separately.
- I DEBUGGED BY MEASURING RATHER THAN THEORISING, which is the only reason (b) took minutes. Seven tests failed with "denied, expected authorized" and the obvious hypothesis was a hash mismatch. A temporary debug print showed the binding matched and the gate answered `allow` — killing every hashing theory at once and leaving the FK as the only candidate.
- A WRONG ASSUMPTION READ FROM ONE FILE, CORRECTED BY THE REAL SCHEMA. Migration 0048 first tried to add `tool_calls_id_company_uq`, having grepped 0036 and concluded the composite unique was missing. 0045 already created it. The `down()` no longer drops another migration's object either.
- FOUR GUARDS FIRED ON THIS TICKET AND ALL FOUR WERE RIGHT: the exhaustive authz role-map, the exact registered-event-name list, the no-orphan-events completeness check, and the compile-time exhaustive audit factory switch. The catalog adversarial test also caught the widened column-level UPDATE grant — the one that matters, since `payload_hash`, `binding_version` and `expires_at` are deliberately NOT in it.
- ONE MUTATION SURVIVED AND THE GUARD WAS DELETED RATHER THAN DOCUMENTED. A `/^[0-9a-f]{64}$/` shape check on the stored hash could not fire: `===` against a real digest already refuses everything it refused. This repo's own measured rule — a guard that cannot fire is worse than no guard, because the next reader trusts it — applies to guards I write too. The other 15 binding mutations all died.
- THE BOUNDARY CAUGHT A REAL DESIGN ERROR IN SECONDS. `computePayloadBinding` was written into `@acbp/contracts` with `node:crypto`; the typecheck refused, because mine would have been the only Node import in a zero-dep package. Contracts now owns the canonical MATERIAL, core owns the digest — exactly how the dispatcher already splits `canonicalizeToolArguments` from its `createHash` call.
- LIMITS NAMED IN CODE RATHER THAN LEFT FOR A REVIEWER. The dispatcher cannot detect COST drift: it has no cost input, so it recomputes the hash with the request's own stored cost and never sees a second cost to compare — execution-time enforcement is P5-005's. Consumption at authorization BURNS the approval even when the call never runs, which is fail-closed and a real UX cost. `approval.expired` is in EVENT-CATALOG and stays unregistered, because nothing sweeps expiry and one event per refused dispatch would be duplicates describing a single lapse.
- EXPIRY SHIPPED AS A MECHANISM WITH NO VALUES, deliberately. ADR-009 §15 leaves per-risk-class defaults an OPEN OWNER QUESTION (AOQ-14-adjacent). `expires_at` is NOT NULL, caller-supplied, and defaulted nowhere in the stack — a nullable "no expiry" column would have made the ABSENCE of an owner decision read as permission to never expire, which is the failure this phase exists to prevent.
- `approval.consumed` IS NOT IN CANON'S CATALOGUE and was registered anyway, on source priority: the P6-004 backlog row's audit behaviour is "Consumption audited", and the backlog (priority 3) outranks the architecture docs (priority 5). Recorded here so the divergence is a decision rather than a drift.

## ACBP-P6-005 — edit invalidation, launch gate 4 (CDR-070)

- A TESTING TICKET THAT FOUND A REAL GAP, which is the argument for writing proofs rather than assuming them. APPR-007 states the mechanism as *"Edit rebinds hash"*; `decideApproval` accepted `supersededByRequestId` and checked NOTHING about it — any pending request in the company satisfied it, including one bound to a completely different action. The chain a human relies on (*"not those 500 recipients, these 3"* → the old approval dies → the new one carries the 3) held only because callers were expected to behave. An unverified rebinding is not a rebinding. Three tests red before the guard, control green throughout.
- THE PROOF IS A MATRIX, NOT A CASE, and the reason is one ticket old: P6-004's review found the tool-VERSION binding component inert because nothing varied it. A single "changed payload is refused" test would have passed while three of canon's four material-change elements did nothing. So gate 4's evidence is one case per bound element — payload (two shapes), tool, tool version — plus the cost bound asserted at the contract, where it can be.
- EVERY NEGATIVE ASSERTS THE APPROVAL WAS NOT BURNED, and proves it by running the legitimate call afterwards. A refusal that consumed the approval would deny the modified call and the honest one alike, forcing a human to re-approve because somebody else tampered — a denial of service wearing a security control's clothes. That property is not implied by "the modified call was denied", so it is asserted separately.
- THE CONTROL IS MANDATORY AND IS STATED AS SUCH. A negative suite with no positive case cannot distinguish "correctly refuses a modified payload" from "refuses everything", and the second passes every assertion in the block.
- SCOPE HELD: no schema, no new audit events, no new authz action. Everything needed already existed — the binding columns, the append-only decision, `superseded_by_request_id`, and `approval.approved` carrying `edit_then_approve` in its path metadata. A Testing ticket that grows a table is a Testing ticket that has changed scope.
- WHAT IS STILL NOT TRUE, recorded so nobody reads the gate as more than it is: `edited_data` is not APPLIED anywhere. The successor request carries the edited payload because a human raised it that way; the platform now verifies that it does, which is a different and weaker claim than "the platform rebinds the edit". Automatic rebinding needs a decision about who authors the successor, and that decision has not been made.
- ACBP-P6-006 (autonomy levels 1-2, CDR-071) on branch p6-006-autonomy-levels from 7b4cc32; draft PR #66. THE FINDING THAT RESHAPED THE TICKET: `DEFAULT_NEW_COMPANY_POLICY` ALREADY IS LEVEL 2 and it is owner-ruled (2026-07-29) — informational + internal-reversible allowed, anything higher requires approval — which is PRD §12's L2 row behaviour for behaviour. So the ticket NAMES an existing posture and adds the stricter one; the L2 rule set IS that constant, with a test asserting they agree (two definitions of "what executes without asking" is one too many). Canon §12 is explicit for L1/L2 across all four risk classes, so nothing was interpreted; CDR-051 §0.3 stays flagged and provably cannot affect it (external + sensitive both require per-action approval at BOTH levels).
- P6-006 design: the level is a PROFILE that selects rules, never a POLICY_DIMENSION rules test against (a rule about the level would be a loop). It lives on the versioned `policies` row, so a level change IS a new version and `policy.changed` already fires — no new event, no new table, no new authz. `autonomyLevelRules` returns RULES not a RULE SET, deliberately: rules only restrict, so composition under the existing most-restrictive ordering can only TIGHTEN, whereas a rule set carries a baseline and a baseline REPLACES. Two defaults decided oppositely: new company = 2 (owner-ruled; tightening it unilaterally would override an accepted decision under cover of caution), unreadable/out-of-range stored level = 1 (corrupt data is not a configuration anyone chose) — with a test asserting they are NOT the same constant. Migration 0049 add-nullable → backfill 2 → set NOT NULL with NO column default left behind, CHECK 1-5 storable (0047 precedent) while the service admits 1-2 and refuses 3-5 BY NAME, never clamping (a founder who asked for 4 and was quietly given 2 would believe they had 4 — each refusal is followed by a read asserting the level is UNCHANGED).
- P6-006 commits: f504c42 CDR-071; b5b6c25 contracts+0049+repo+provisioning caller; 17bb3ec composition at evaluation; b6e7c79 raw-insert fixes; b063505 set/read service; fe85082 MUTATION PROBE (deliberate, reverted); 311951d revert+docs; b7d52ab review pass 1; 80c9e6a review pass 2; 5774320 audit-assertion fix.
- P6-006 CI history is the substance of this ticket, not a footnote. (1) b5b6c25 RED 43 failures: migration 0049 made `autonomy_level` NOT NULL and three raw inserts write `insert into policies` with NO schema prefix — my search was anchored to `insert into public\.policies`, so the fix AND its "zero remain" check both measured the same wrong set, and the check could not fail. One was a shared `mkPolicy` helper, hence ~40 failures from one omission. The supersession-race "timeout" was the same cause, not flake: the insert threw inside the racing transaction so its barrier never released. (2) b6e7c79 RED 2 failures — and one is the BEST evidence here: `untrustedRuleIds` for a model-sourced risk class grew from ["baseline-risk-approval"] to include "autonomy-l2-approval-above-internal", i.e. the level reached the evaluator and was correctly tainted by untrusted provenance. That is a real observable consequence of composition, which `pnpm run check` could NOT produce locally (composition is a no-op on every existing test because every policy is level 2 and its rule duplicates the stored one — deleting the composition outright left the suite green). (3) fe85082 mutation probe RED 2/3149 as predicted → `Array.isArray` guard is load-bearing; restored byte-identical. (4) 80c9e6a RED 1 failure: my audit assertion selected `metadata` but the column is `payload` — a raw `sql` template is not checked against the schema, so the generic I wrote declared what I EXPECTED and tsc had nothing to disagree with; rewritten through Kysely's typed builder so a wrong column is a compile error. (5) b063505 GREEN 226 files / 3151 tests ZERO SKIPS (run 30645193259).
- P6-006 mutation testing: 5 contract mutations, 0 survivors, sources restored byte-identical (collapse-to-most-restrictive; levels 3-5 never yielding permissive rules; L1 propose-only across every risk class; the two defaults not collapsing; the MVP set not widening to admit 3). The `Array.isArray` guard could NOT be measured locally — `pnpm run check` was exit 0 with it removed, 3151 tests, no failures — so it was measured under hosted CI as a labelled probe with the predicted failure written down BEFORE the run. Prediction right about mechanism, incomplete about shape: a JSON string spreads into CHARACTERS (the silent rescue predicted), a JSON object is not iterable and THROWS (louder, and it hit first). Both kill it; only the first would have been silent in production.
- P6-006 review pass 1 — REAL DEFECT: `runInCompanyScope` runs its callback INSIDE the account transaction, so returning commits. `setCompanyAutonomyLevel` did supersede-then-insert and returned a typed refusal when the insert hit `(company, version)` uniqueness — COMMITTING the supersession with no replacement. Result: company with NO ACTIVE POLICY (fail-closed, evaluation denies) but UNRECOVERABLE, because `initializeCompanyPolicy` would then find no active policy, try version 1, collide with the superseded version 1, and throw its own invariant error. Fixed by throwing so the transaction rolls back; proven against real PostgreSQL by parking a superseded row at the version the change will claim, forcing the conflict, and asserting the call rejects AND the company still holds exactly one active policy at the original version and level. GENERAL CLASS, recorded: inside a transaction-wrapping scope helper, a typed refusal returned after a write is a commit.
- P6-006 review pass 2 — the backlog clause "level changes audited" was met NOMINALLY and unmet in SUBSTANCE. `policy.changed` fired, but a level change carries the SAME baseline and SAME rules, so the event read {version, baseline, rule_count} and was indistinguishable from any other change except by version number — on the single field that decides what the platform does without asking a human. Added `autonomy_level` + `superseded_version` (optional, so events already written stay valid), recorded at INITIALIZATION too so the first entry states the level the company started with rather than leaving it inferred from whatever the default was that day. Pinned by reading `audit_events.payload` through the typed builder. GENERAL CLASS, recorded: a requirement satisfied nominally may still be unmet in substance — check the record answers the question it exists for.
- P6-006 scope held: NO UI. The read model (which levels exist, which are available, each level's plain-language consequence — PRD principle 2 requires the consequence, not the number) is in scope and shipped; the SURFACE is an owner gate and nothing was scaffolded. AOQ-14 untouched (no numeric threshold on any limit dimension). Flagged for the owner: new companies still start at LEVEL 2, so research and internal drafting execute without asking on day one exactly as ruled — if that should be L1 it is a one-line change to `DEFAULT_NEW_COMPANY_AUTONOMY_LEVEL` and CDR-071 §2-G3 is where the decision belongs.

- ACBP-P6-007 (emergency stop) — CI HAD BEEN RED FOR FIVE CONSECUTIVE COMMITS while the local gate stayed green, because every failing suite is real-PostgreSQL and SKIPS locally. Two causes: a SEVENTH casualty of the over-broad reset-list edit, this time INSIDE a SQL string literal (`where table_name = 'approval_decisions', 'emergency_stops', 'held_work'` — a syntax error, not a harmless extra array element), which my earlier revert missed because it swept only the call-argument `(...)` form; and `emergency_stops`/`held_work` present in `TENANT_TABLES` but absent from `EXPECTED_GRANTS`, so P1-014 compared their real INSERT/SELECT against []. The re-sweep this time enumerated ALL 73 occurrences repo-wide and classified each by whether its LINE looks like SQL — a DIFFERENT anchor from the one I fixed against, which is the whole lesson. Fixed at 791bd56; CI 30679478824 GREEN, 227/227 files, 3220/3220 tests, ZERO SKIPS.
- P6-007 — TWO REQUIREMENTS WERE NOMINALLY DONE AND SUBSTANTIVELY MISSING, the P6-006 class again. (1) The refusal did not say WHAT halted it: `denial_reason: emergency_stopped` cannot distinguish "the account is halted" from "one task is", so an account-wide halt and a single stopped task left IDENTICAL evidence. `tool.call_requested` now carries `stop_scopes` — comma-joined scope NAMES from the closed vocabulary, taken from the evaluation that decided the call rather than re-derived, present ONLY on that reason, never carrying a target id into audit metadata. (2) Four of the five enforceable scopes had never been proven THROUGH THE DISPATCHER; ten real-PG cases now prove each twice (halts what it claims / does not halt what it should not), including a cross-account and a sibling-company miss, because an over-broad `company_id is null` branch would turn one company's halt into a platform outage that still reads as a correct stop. (3) And gate 8 was measured for ONE scope while CDR-072 §G4 promised "every scope" — a gap between my own design record and my own test; timing and enforcement are now one case per scope, deliberately not splittable, driven off ENFORCEABLE_STOP_SCOPES with a guard asserting the keys match exactly.
- P6-007 — **THE §0 FAILURE WAS SITTING INSIDE THE TICKET WRITTEN TO PREVENT IT.** The dispatcher resolved `task`/`worker` stop identities with `select task_run_id, worker_id from worker_runs where id = <params.runId>`, and `runId` is a `task_runs.id`. Two independent errors in one line: the join key matched NOTHING ever, and `task_run_id` is not a task id either (a `task` stop names a task — `held_work.task_id` references `tasks`), so it could not have matched even had the join been right. Both scopes were storable, activatable, visible in the read model and HALTED NOTHING — an operator stopping one runaway task would have been told it worked and watched it keep running. The pure `evaluateStops` was correct throughout and its suite was green: the covering relation was never wrong, the DISPATCHER's ability to populate the identity it matches on was. Fixed at b9d303e (`taskRun.task_id`, already in hand; worker from `worker_runs WHERE task_run_id = <runId>`, exactly one by migration 0040's UNIQUE(task_run_id)). GENERAL CLASSES, both recorded in CDR-072 §G2: the covering relation being correct is NOT the same claim as the scope being enforced, so the matrix must run end-to-end; and a fixture helper that THREW instead of returning null is what exposed it — nulls would have compared against nulls, gone green, and certified two dead scopes as enforced, a passing matrix being the most convincing way to ship exactly this bug. Same shape as `not.toContain('work_id')` against a column that does not exist, which is why `expectUpdatableColumnsExactly` resolves every named column against `information_schema.columns` first.
- P6-007 — LAUNCH GATE 8 MEASURED ON REAL POSTGRESQL IN CI (30680683466 on b9d303e, 227/227 files, 3237/3237 tests, ZERO SKIPS): account_wide 6.7 ms, external_actions_only 7.1 ms, worker 7.8 ms, task 7.9 ms, company 8.6 ms — bound 5000 ms. Met for FIVE of seven and stated that way everywhere: `capability` and `integration` never produce `emergency_stopped` (they deny `stop_unavailable`), so there is no halt to time, and a table of seven green rows would be exactly the false assurance §0 warns about. The caller-injectable `stop` port is DELETED, `ToolGates` has no members, and `tools/check-stop-port.mjs` keeps it gone in `check:static`. Ticket Done / PR ready / merge remain OWNER GATES and have not been taken.

- ACBP-P6-007 — AN INDEPENDENT REVIEW (subagent, owner-authorised) FOUND 2 BLOCKERS, 3 HIGHS and 10 Medium/Low that TWO author review passes had missed. That gap IS the argument for the independent pass. BLOCKER 1: `held_work.status` was written by `reviewHeldWork` and read by NOTHING — a cleared stop authorized every held task's next call whether its review said `held`, `confirmed` or `discarded`, so an explicit discard changed one column and nothing else. BLOCKER 2: the stop controller had ZERO tests and ZERO callers; the dispatcher suite used its own raw-insert helper, so every service guard was unproven and `StopRepository.insert`'s `ON CONFLICT` inference against a PARTIAL `NULLS NOT DISTINCT` index had never executed — the `credit_transactions_reservation_key_uq` shape, where a dead write path passes typecheck, lint and every unit test. HIGHS: the hold query ignored scope entirely; `task`/`worker` targets were unvalidated free text; and gate 8's measurement timed a raw superuser INSERT rather than `activateStop` (N+3 round trips), so 6.7–8.6 ms was never evidence for the product path.
- P6-007 — THE WORST FINDING WAS A DOC I WROTE. CDR-072 §1-G6, PROJECT-STATE and a direct report to the owner all said "nothing auto-fires on resume" held "for the company the stop was raised from". It held for NO company. Having found a real gap (sibling companies), I described its blast radius as the smallest reading that was still bad news and recorded that reading as established fact in three places. GENERAL CLASS, recorded: a partial diagnosis stated confidently is worse than an open question, because it CLOSES the question. Wrong documentation is worse than missing documentation. All three now record that the narrower version was stated as fact, rather than being quietly corrected. Four more claims that guaranteed something nothing provided were fixed the same way (§1-G8's "Proven the way P6-006 proved it"; `audit-operations.ts`'s deferral to a suite that did not exist; `stop-repository.ts`'s "check-conflict-targets fails the build if they drift" — it never parses inference predicates; and the halt/un-halt authority separation, where both actions map to `['owner']`). One claim was made TRUE instead of softened: the scope-CHECK/`STOP_SCOPES` set equality now has a real assertion reading `pg_constraint`.
- P6-007 — BLOCKER 1'S FIX WAS A CANON FINDING, NOT A DESIGN CHOICE, and canon was searched BEFORE any mechanism was proposed (the J-13 / autonomy-default pattern). `WORKFLOW-STATE-MACHINES.md` §4 lists `paused` among the task holds and gives `running→paused / paused→running` | system (company pause / emergency stop) | scope stop active | held visibly; resume requires review (ADMIN-002); `queued→running` already requires "stop-state clear"; `diagrams/13` ends "CONFIRMED items resume from checkpoints". `paused` and BOTH transitions were ALREADY in the implemented `LEGAL_TRANSITIONS` (P4-002, verbatim from WORKFLOW §4) — a specified transition simply had no producer. Implemented exactly that: activation pauses the RUNNING tasks it caught; only a CONFIRMED review resumes; a DISCARD leaves the task paused rather than cancelling it ("nothing lost"); reviewing while the stop is still active is refused, because ADMIN-002 says clearing OPENS the review. `paused_count` joins `held_count` so "held 5, paused 2" distinguishes a halted fleet from a queue that never started.
- P6-007 — REMEDIATION EVIDENCE: exact-head CI `30698900097` on `4f82b6c`, **229/229 files, 3285/3285 tests, ZERO SKIPS**, 33 stop-service cases, gate 8 at **4.5 ms measured through `activateStop`**. Three CI-caught defects along the way were ALL in my instruments rather than the code: a `held_at` ordering bug (every held row from one activation shares a transaction timestamp, so `[0]` read whichever row the planner returned), a fixture that never drove a task to `running` (so the entire paused matrix compared 'queued' against 'paused' and could not have detected a real failure), and two pre-existing review tests that predated the new ordering rule. GENERAL CLASS: the guards that assert their OWN preconditions are the ones that caught things — `runIdentities` throwing rather than returning null, `expectUpdatableColumnsExactly` resolving column names against `information_schema`, and the fixture asserting it left the task RUNNING. Every place I omitted that, the test passed while proving nothing. Also the EIGHTH occurrence of a PowerShell bulk `-replace` corrupting source (it ate two `select ... from tasks` clauses): the lesson is not "be careful with regex" but "use the editing tool for edits".

- ACBP-P6-007 — A SECOND INDEPENDENT REVIEW, briefed at the chokepoint write path, found 1 BLOCKER + 5 HIGHS in code written an hour earlier, ALL of which a green CI run had already passed over. BLOCKER: the held row could name a stop that never covered the call — attribution matched covering SCOPE NAMES against `activeStops`, which `listActive` orders by `activated_at, id`, so with two `task` stops up a call from the second task was filed under the first task's stop. `listHeld` for the real stop then returned empty (clearing it reported nothing to review while the task sat paused), and the item could only be released by reviewing a row under a stop that never covered it — which `reviewHeldWork` refuses while that stop is active. A PERMANENTLY PAUSED, UNCOMPLETABLE TASK with the evidence saying all was well. Fixed by asking the covering relation ITSELF, one stop at a time, about the same call facts the decision used. HIGHS: `external_actions_only` (which halts calls, not tasks) was silently pausing whole tasks; a new guard test was VACUOUS in exactly the way the previous review had caught THIRTY LINES ABOVE its own corrected twin; `complete_for_scope` was false for every scope carrying it (deleted, not re-scoped — a label nothing can truthfully carry is worse than no label); and the dispatcher's `running→paused` was unaudited with its row count discarded, twenty lines below a comment insisting row counts must be checked.
- P6-007 — TWO PM RULINGS RECORDED IN CDR-072 §1-G6, both attributed (options + recommendation the engineer's, choice the PM's on the owner's authority). (1) OPTION B + C's LABELLING: the chokepoint holds what it refuses. The objection the engineer raised against their OWN recommendation is recorded as ACCEPTED, not overlooked — B puts a write on the refusal path and gives the chokepoint a task-lifecycle responsibility — together with C as the coherent RETREAT if that responsibility later causes trouble. Three conditions were required and met: counts state what they measure (`held_scope`, `heldQueueCompleteness`, `heldQueueCaveat` — `held_count` is a FLOOR); the "a task that never attempts a call is never held" boundary is stated loudly wherever the queue is described; and idempotency is PROVEN — five consecutive refusals against a real database leave exactly one held row (five rather than two, because two cannot distinguish idempotent from "the second happened to fail"). (2) IN-FLIGHT SAFE-STOP AT ACTIVATION, which was a CANON FINDING rather than a design choice.
- P6-007 — THE CANON FINDING ON IN-FLIGHT WORK. Pausing the TASK does not stop the RUN: nothing on the dispatch path reads `tasks.state`, so a paused task kept executing tools once the stop cleared; the pause only blocked completion. `WORKFLOW-STATE-MACHINES.md` §4 assigns the stop checks to DIFFERENT ACTORS — line 54 gives the WORKER "stop-state clear" at `queued→running`, line 89 gives the DISPATCHER stop-state for the call, and line 15 gives company pause the effect "no new job pickup; IN-FLIGHT SAFE-STOP". For work already running canon's answer is a safe stop, not a fourth chokepoint read — and the mechanism already existed unused from here: `task_runs.stop_requested_at` via `requestStop`, which `decideStepAdmission` checks FIRST because "an owner's request outranks every automatic rule and is not a failure", ending the run `stopped` rather than failed. That is OQ-14's "finish the current tool call, halt before the next" verbatim. PM placed the call at ACTIVATION (not the refusal path): the information is already in hand, it keeps the refusal path to one write, and it reaches an in-flight run THAT MAY NEVER MAKE ANOTHER TOOL CALL — which the refusal-path version structurally cannot. Proven both ways on real PostgreSQL: the flag is set AND fed to the real `decideStepAdmission` (reading the column alone would show a flag and ASSUME the consequence), and a run outside the stop's scope is left un-requested.
- P6-007 — COMMENT AUDIT (PM-required before merge): every guarantee-shaped claim added in this ticket, verified against the code or deleted. THREE FAILED, and all three were in the labelling added FOR the PM condition about not overclaiming — the same reflex reappearing inside the fix for it. "A surface MUST render this", "so the boundary cannot be lost in translation", "held_scope IS REQUIRED, NOT DECORATION": nothing enforces any of them, no surface exists to enforce them in, and a caller may ignore both fields. GENERAL RULE LEFT IN THE CODE, and the most useful thing this ticket produced: a comment may describe what the code does and why, but the moment it claims a guarantee is ENFORCED, the enforcement must be NAMEABLE — a test, a constraint, a checker — or the sentence goes. A comment that lies is worse than no comment, because the next reader stops checking.
- ACBP-P6-007 — MERGED as squash `1f3096d` (PR #67), and the merge evidence was RE-VERIFIED from the CI logs rather than from my own earlier notes, because this ticket's most expensive defects were all self-reported completions. Exact-head `30705908508` on `19f5013`, exact-main `30706308683` on `1f3096d`: **229/229 files, 3294/3294 tests, and NOT ONE `N skipped` line anywhere in the job log** — the distinction that matters here, since every suite that proves this ticket skips locally and a green local gate proved nothing all ticket. Blocker 1's behavioural half was confirmed present in MAIN's source, not just in the branch that was deleted and not just in a log entry claiming it: `stop-service.ts:313` transitions `running→paused` inside the activation transaction and counts only the rows that actually changed, and `reviewHeldWork` resumes ONLY on `confirmed`, leaving a `discarded` item paused rather than cancelling it. Ancestry was not accepted as proof the branch tip landed — a squash merge breaks it — so the tip's tree was diffed against `main` directly. STILL OPEN AND OWNER-GATED, and a Done ticket does not close it: the `account_wide` held-work scoping (CDR-072 §1-G6).
- ACBP-P6-011 — THE LEDGER HALF OF TRUST-CRITICAL #12, and the honest note that it fixes no live bug. `usage_events` had no idempotency key and no uniqueness beyond its primary key, so two deliveries of one model call made two rows — and CDR-073's reconciliation could never have caught it, because it recomputes FROM the ledger: a duplicated row is reproduced faithfully on both sides, the total is wrong, the two sides agree, nothing alerts. Migration 0052 adds a nullable key with a PARTIAL unique index on `(company_id, idempotency_key)`. A natural key over the call's attributes was rejected: two distinct calls with identical provider, model, tokens and cost in the same instant are legitimate, and collapsing them UNDER-counts — the same class of error in the other direction, and the direction nothing downstream ever contradicts. Per COMPANY, not per account, because a unique index is enforced regardless of RLS and an account-wide key space would let one company's key suppress another's billing row. Suppression is a SUCCESS, not a throw: metering is fail-closed, so a duplicate surfaced as a write failure would withhold an output the customer was already charged for — the feature causing the harm it exists to prevent. STATED IN CDR-074 §2 AND IN THE COMMIT: today's write is inline and fail-closed, so a failed write leaves nothing behind and a genuine retry means the model really was called again. This closes a STRUCTURAL hole ahead of message-driven delivery; no double-count was found or measured, and a guard sold as fixing a present bug invites the next reader to assume otherwise.
- ACBP-P6-011 — THE COUNTER, AND THE LIMIT IT DOES NOT REMOVE. Canon calls a suppressed duplicate "invisible (correct)", which is right for the user and treacherous as a verification standard: "nothing happened twice" is also what a system with every guard removed looks like on a day nothing was re-delivered. So the replay suite DELIVERS the duplicate and asserts two independent things — the effect happened exactly once, and the system KNEW it was suppressing. Only the second cannot be faked by a system that simply never duplicated anything. One stable event name across every surface (`idempotency.suppressed`, surface as a field) because per-surface names would require knowing the full list in advance to count them, and a surface that quietly stopped reporting would look like one nobody knew to query. INFO not WARN — warning on a correct outcome trains readers to ignore warnings. Metadata is built from NAMED FIELDS, never a spread, because an idempotency key is caller-authored and unbounded; asserted end-to-end, not only at the unit boundary. AND §5.2 LEADS WITH THE LIMIT RATHER THAN BURYING IT: a count of ZERO is ambiguous — a path that silently broke and a quiet week both report nothing. The counter is evidence suppression FIRED, not that it WOULD have. Only a live canary fixes that, and it is P7-006's, owner-gated.
- ACBP-P6-011 — THE REVIEW PASS FOUND WHAT A GREEN CI HAD NOT, for the third ticket running. `createIdentityWebhookService` — the live production entry point, and the ONLY one of the three instrumented surfaces that suppresses anything in production today, because providers genuinely re-deliver webhooks — never passed a logger to the processor, and `IdentityEventProcessor` had no field that could carry one. Every real re-delivery called `recordSuppression(undefined, …)`, a no-op. The counter was structurally incapable of registering the one duplicate that actually happens: exactly the failure CDR-074 §0 argues against, reproduced inside the fix for it. Nothing was mis-billed — suppression itself worked, the re-delivered webhook was correctly ignored; the visibility was what went missing. WHY THE TESTS MISSED IT is the transferable part: the replay suite called the INTERNAL `processVerifiedIdentityEvent` rather than the entry point, so it proved "the processor records when handed a logger" and said nothing about whether anything ever hands it one. A test aimed one layer below the entry point cannot see a gap that lives AT the entry point, and it reads as thorough while doing it — the same shape as P6-009's HIGH 1, where a test re-implemented the SQL it was checking. In both cases the assertion was real and the SUBJECT was wrong. Both identity cases now drive the real service with only the signature verifier faked; the logger is asserted BY IDENTITY, not by presence, because a different logger would satisfy `toBeDefined`. Mutation-verified: deleting the pass-through fails with "expected undefined to be { debug: [Function], … }". CDR-074 §5.2's "the replay suite calls the real use cases" was the overclaim that hid it — true of the repositories, false of the entry points — corrected by §5.2a with a per-surface table that also records that `job_enqueue` has no production caller at all.
- ACBP-P6-011 — A SECOND-OCCURRENCE DEFECT, now guarded statically. Migration 0052 broke a USER-MAPPING test on an IDEMPOTENCY PR: its migration-drain loop capped at 50, and at 52 migrations it stopped with 0002 still applied, asserting "expected 2 to be +0". P6-009's migration 0051 had already broken the identical pattern in another file, and THAT fix did not sweep for other copies. The cap is silently coupled to the migration count, so it always detonates on unrelated work and always reads like a real defect in the ticket under review. Closed on both axes: the loop now records WHY it ended (`tables_gone` / `stack_empty` / `iteration_cap`, three causes a bare count assertion reports identically), and `tools/check-migration-drain-loops.mjs` fails the build on any `migrateDown` loop whose cap does not clear the migration count — verified by restoring the 50 and watching it name the file, line and both numbers. A sweep confirmed one remaining instance; every other bounded loop in the repo is capped by a domain constant that does not scale.
- ACBP-P6-009 — DONE LINE ADDED LATE, recorded here because the omission is itself the lesson. The ticket merged as squash `c43acf8` (PR #68, exact-main CI `30723999693`, 237/237 files, 3426/3426 tests, zero skips) but PROJECT-STATE.md was never given its DONE line. By that file's own rule — only the topmost ticket without a DONE line above it is genuinely in flight — a fully merged ticket was reading as the active one. Identical to the omission commit `4faac72` fixed for P6-007, which means the finalization checklist misses this step by default rather than by accident. STILL OWNER-GATED and not closed by that line: the drift threshold value (CDR-073 §3.1) and who may trigger a rebuild (§3.2).
- ACBP-P6-010 — THE CDR PHASE FOUND A LANDMINE AIMED BY NAME AT THIS TICKET, and it reordered the work. CDR-067's post-review correction says the dispatcher supplies the policy engine exactly ONE observation (`risk_class`), so a rule on `spending_limit` has nothing to read, is UNEVALUABLE, and by CDR-066 §3-G9 contributes `deny` — "a company whose policy carried a spend cap would have every tool call refused", latent only "because no product path writes rules until P6-010". THIS TICKET IS THAT PRODUCT PATH. So the naive shape — add a rule carrying the cap — takes a tenant offline, and no test of the rule-writing path would show it, because the rule would be stored perfectly correctly. The hard part was never choosing the cap; it was supplying the observation so a cap rule is evaluable at all. `policy-enforcement.integration.test.ts` had predicted the day and left instructions ("it will fail the day an observation for this dimension is supplied"); it was updated on its own instructions, and its original assertions were MOVED to `working_hours` — still genuinely unobserved — rather than deleted, because supplying ONE observation must not silently delete the fail-closed coverage for every other dimension while the suite still looks complete.
- ACBP-P6-010 — TWO OPEN-QUESTION REGISTERS DISAGREED, and the session surfaced it rather than picking. IMPLEMENTATION-OPEN-QUESTIONS marked IOQ-09 "Resolved (CDR-008)" naming P6-010 as the consumer; ARCHITECTURE-OPEN-QUESTIONS still had AOQ-14 open pending alpha data; PROJECT-STATE said the values "remain unruled and unshipped". Reconciled as: INTERIM values are ruled (CDR-008, Accepted, owner-authorized, revisit-bound), FINAL values are not. The owner ruled to ship them. They are labelled interim AT THE DEFINITION rather than only in the CDR, because a number whose provisional status lives three documents away gets read as settled by whoever finds it next.
- ACBP-P6-010 — THE INDEPENDENT REVIEW FOUND THIS TICKET'S OWN GATE UNIMPLEMENTED. CDR-075 §3-G8 says emission is once per (scope, period, threshold) crossing "and the mechanism that makes 'once' true has to be named, not assumed". No mechanism existed: a company at 76% of its daily cap wrote another `usage.limit_reached` row on EVERY model call, into a trail retained for the billing lifetime — the exact noise the gate was written to prevent. THE OBVIOUS FIX IS A TRAP AND IT IS WORTH REMEMBERING: `audit_events.idempotency_key` is unique-when-present, but `writeAuditEvent` inserts with NO ON CONFLICT, so a second write raises 23505 — and a constraint violation ABORTS THE ENCLOSING TRANSACTION. Catching it in JavaScript does not un-abort it; every later statement fails with 25P02 and the commit fails. A soft ALERT would have become a hard OUTAGE, a worse failure than the noise. The mechanism is a read-before-write, named in the code, with its limit stated honestly: once-per-crossing under ordinary traffic, at most a handful under a race, not exactly-once.
- ACBP-P6-010 — AND FIXING IT EXPOSED A FIFTH DEFECT ONLY THE CORRECTED TESTS COULD SEE. The dedupe silently did not work: it filtered on `company_id`, but `usage.limit_reached` is written under an ACCOUNT scope and `writeAuditEvent` documents that an account scope leaves `company_id` NULL. The filter matched nothing and five calls still wrote five rows. Account-scoped is CORRECT and was not the thing to change — the account-spanning read happens there, and after `readSpendTotals` the company GUC points at whichever company the loop visited LAST, so a company-scoped write would stamp an arbitrary one; the row identifies its company through `subject_id`. Caught ONLY because the review had just replaced `toBeGreaterThanOrEqual(1)` with an exact count. That loose assertion would have passed on all five rows — the SECOND time on this ticket that an assertion which could not fail hid the defect directly beneath it, the first being a membership test asserting a company-scoped block that would have stayed green with the account loop entirely broken.
- ACBP-P6-010 — THE CEILING IS REACHABLE AND UNREACHED, recorded in CDR-075 §4.3 rather than implied away. Nothing passes `caps`, because `createModelGateway` has no production composition anywhere — only demo scripts, journey helpers and integration tests construct one. This is the SAME defect §1 diagnoses in `policyPrecheck` (an optional seam defaulting to always-allowed with no production caller), reproduced one layer up by the fix for it, and §1's own wording ("this is the function that fills it") read as though the gap were closed. Deliberately NOT worked around by wiring the demo scripts, which would make the ceiling look enforced in the one place it does not matter and leave the real gap exactly where it is. Same treatment CDR-074 §5.4 gave the usage idempotency key.
- ACBP-P6-010 — A PROPERTY OF CDR-008's NUMBERS, found when a real-PG case failed asserting 'account' and getting 'company'. The account ceiling is 3x the company cap, so splitting it across TWO companies puts each at 1.5x its OWN ceiling and the company cap fires first, correctly, being more restrictive. Three companies each just under their own cap still fall short of 3x — so THE ACCOUNT CEILING CAN ONLY BIND AT FOUR OR MORE COMPANIES. The system was right and the test was wrong. Recorded in §4.2 because it is invisible from the numbers and will read as a broken account cap to whoever next tests it with two; if the intent was for it to bind earlier, the MULTIPLIER is the lever, not the per-company value.
- ACBP-P7-001 — THE TICKET ARGUES WITH ITSELF, AND CANON SETTLES IT. Acceptance asks for BOTH "archive matches in-product data" AND "zero secrets", and when a founder has typed their own key into their own document those conflict. `SECURITY-ARCHITECTURE:19` rules it — archives never contain secret values — so the value is redacted and the SURROUNDING DOCUMENT STAYS: dropping a whole document over one span would lose the founder's actual work, which is the failure export exists to prevent. That forces a third manifest category the design has to carry — not just included and omitted, but INCLUDED WITH REDACTIONS — and `manifestIsFaithful` to answer the stronger question `complete` cannot ("is this everything, EXACTLY as it was?"). A fully complete archive can still differ from the product wherever a secret was removed, and collapsing the two would let a redacted archive describe itself as an untouched copy.
- ACBP-P7-001 — THE CLASSIFICATION IS TOTAL AND DECLARED, because the likeliest way this ticket fails its own acceptance criterion is a collection nobody remembered to export. A bespoke read per entity could never catch that: the code would be correct about everything it mentioned and silent about everything it did not, and the archive would look complete to every test written against it. So every table carrying a `company_id` sits in exactly one of two closed lists — 28 exported, 18 excluded with a ruling from a closed vocabulary — and a real-PostgreSQL guard compares them against `information_schema`, DELIBERATELY A DIFFERENT ANCHOR from the `DatabaseSchema` interface the export itself reads through, so a wrong entry in one cannot excuse itself in the other. A future migration adding a company-scoped table fails that guard until someone rules on it. The reader is GENERIC over that allowlist rather than a mapper per entity, and the trade runs in the safe direction: a mapper that forgets a column UNDER-delivers silently, which is ADR-002's failure; a generic whole-row read can only pick a new column UP, and that lands in the secret guard, which redacts, counts and reports.
- ACBP-P7-001 — A ROW IS NOT A TEXT FIELD, and the omission vocabulary shrank because of what that revealed. `sanitizeExportText` handles one field; a secret pasted into `payload.notes[2]` is exactly as gone as one in a top-level column, so the walk is recursive over VALUES, and a leaf that cannot be represented excludes its WHOLE ROW rather than being dropped from an otherwise-included one — a row silently missing a field is a lie about that row, an enumerated omission is a complaint. A secret in a KEY excludes too, because redacting keys would collide and one would silently overwrite the other: data loss wearing redaction's clothes. THEN `unsupported_format` HAD TO GO. It shipped in the manifest slice and never acquired a producer — artifact BYTES are not copied by this ticket, so no stored format is ever rejected — and a reason nothing can produce is a case a reader will wrongly believe can occur. Removed, `truncated` added in its place, and the rule made explicit: every member of the vocabulary has a named producer. Same "catalogued, reaches nothing" failure CDR-074 §5.4 and CDR-075 §4.3 both had to disclose, caught this time before it shipped rather than after.
- ACBP-P7-001 — THE INDEPENDENT REVIEW FOUND A DISCLOSURE DEFECT THAT LOOKED LIKE DILIGENCE. The ownership check returned the failing rows' IDENTITIES, and the export wrote them into the manifest as `ownership_unverified` omissions — so a row that leaked past RLS would have had its id handed to the WRONG FOUNDER, confirming that another tenant's record exists and naming it. CDR-078 §3-G8 forbids exactly that. Enumerating what went wrong is the right instinct everywhere else in that file; it is wrong for the one class of row whose identifier is not ours to enumerate, which is what made it invisible on the way in. FIXED AT THE SOURCE, NOT THE CALL SITE: `partitionRowsByOwnership` now returns a COUNT, because a return value that must never be used is an invitation. The founder still learns which collection and how many rows. Found by READING rather than running — nothing could have run it, since the branch is unreachable while RLS holds, which is also precisely why it is a pure function.
- ACBP-P7-001 — MUTATION TESTING, 17 GUARDS, AND TWO OF THE FOUR FINDINGS WERE TESTS THAT COULD NOT FAIL. (1) The `expected !== ''` check in the ownership comparison was UNMEASURED: two tests existed — a blank scope with a real row, a blank row with a real scope — and NEITHER CROSSED THE TWO BLANKS, which is the only branch where `'' === ''` matches and the last ownership check becomes a rubber stamp. (2) BOTH case-insensitivity tests asserted NOTHING AT ALL: the fixture uuid was `11111111-1111-4111-8111-111111111111`, all digits and hyphens, so `toUpperCase()` on it is a NO-OP and they were comparing a string to itself — they would have passed against a case-sensitive comparison, a case-insensitive one, or no comparison whatsoever. A fixture that cannot express the difference cannot test for it, which is ACBP-P6-007's fixture-guard lesson arriving through a different door. (3) A FAKE CONTROL: the repository tested both `isExportCollectionTable` and `exportOrderBy(...) === undefined`, which are equivalent because both derive from the same constant, so deleting either left every test green; a condition no test can distinguish reads as defence-in-depth and is not. (4) And the harness itself — an aborted first run left a FAIL-OPEN COVERAGE MUTATION IN THE WORKING TREE, caught only because the re-run reported that mutation's anchor as missing. Had the script not been re-run it would have been committed.
- ACBP-P7-001 — HOSTED CI FOUND THE LAST ONE, AND IT WAS THE BOUNDARY WORKING CORRECTLY. The no-hollow-success test asserted the message `/did not land/` and got "database error (unclassified database error)": the throw happens INSIDE the company scope, so the transaction wrapper sanitises it into a bounded `PlatformError` before any caller sees it. A test demanding the raw text was asking the boundary to leak. The assertion moved to the OUTCOME, which was the substantive claim anyway — the export refuses, and NO audit row exists to vouch for an archive that is not there — and both halves still fail if the read-back verification is removed. The raw reason survives in the cause chain, which the CI log confirms verbatim, and which is where an operator needs it and a caller does not.
- ACBP-P7-001 — WHAT IS NOT CLAIMED, stated in the DONE line and the PR rather than only in the CDR. There is no S3-compatible adapter and no dependency for one, so NOTHING HERE PROVES AN ARCHIVE LANDS DURABLY. Everything that IS proven — ownership verification, manifest correctness, secret exclusion, cross-tenant denial, partial enumeration, the coverage guard — is provable against the in-memory adapter because none of those properties are about S3. There is also deliberately NO `export_jobs` TABLE, though the BACKLOG data-objects cell and EVENT-CATALOG `:279` both name one: a job row exists to be POLLED and §4 ruled out the surface that would poll it, so it would have had one writer, no reader and no observable status — "designed, catalogued, reaches nothing" built on purpose. And §6.8 records the cost nobody had named: the whole export runs inside ONE database transaction, which against a real provider means holding it open across ~56 network round trips. It belongs to the ticket that brings the adapter, recorded so whoever writes it inherits the problem rather than discovering it.
- ACBP-P7-001 — AND PROJECT-STATE's FORWARD POINTER WAS STALE AGAIN. "Next executable action" still read "Phase 6 is code-complete pending two owner merges" after BOTH had merged (`1f4acaa`, `b36a079`), and neither had been given the DONE line the file's own convention requires — so by that convention P6-012's working block was reading as the ticket in flight. This is the SECOND correction to that section in two days, and the third time a merged ticket has gone without its DONE line (P6-007, P6-009, now P6-008 and P6-012), which means the finalization checklist misses the step by default rather than by accident. Both DONE lines added, the section rewritten against `git log origin/main`, and the instruction made explicit in the text: trust git and an `Import-Csv` of the backlog over any prose in that file, including the paragraph making the claim.

- ACBP-P7-002 — THE GATE THE TICKET WAS MEANT TO EXTEND DID NOT EXIST. Gate 14 says "deactivation blocks new autonomous work", so the expected work was two new states in a CHECK constraint. **Nothing in production read a company's lifecycle status before doing autonomous work**: pausing a company was a LABEL, NOT A CONTROL, and had been since ACBP-P1-010. GENERAL CLASS, and this is the reusable part: **FIVE independent artefacts made the control look like it existed**, which is why six phases passed over it. `canPickUpAutonomousWork`'s docstring called itself "the single truth a scheduler/worker consults" and had ZERO production callers. A green integration test named `'pause blocks new autonomous-work pickup (invariant 16 groundwork)'` called that pure predicate on a returned value and exercised no pickup path — note its author's own hedge, "groundwork", which was honest and was still read as coverage. `EVENT-CATALOG:40` listed a `company.paused` consumer that does not exist, and has done since the Phase-0 initial commit — it predates P1-010. `REQUIREMENT-TRACEABILITY.csv` marked COMP-006 `Covered (MVP)`, verified by "Pause-then-schedule negative tests" that did not exist — **the worst of the five, because a traceability matrix is what a reader consults to ask "is this requirement covered", which is the exact question it answered wrongly.** And `stop-service.ts:513` had already WRITTEN DOWN the underlying fact — "nothing stops a task being created, planned, queued and started while the stop stands" — about the very machinery deactivation was to reuse; that one is not a false claim but a recorded admission nobody joined up. A docstring, a test name, a catalog row, a coverage cell and a comment agreeing with each other is not five pieces of evidence; it is one unverified belief with five copies. The only test that separates a control from a control-shaped comment is neutralising it and watching something go red. **The fifth artefact was found LAST, during the documentation pass, after the code had shipped and been reviewed twice** — so the count in a ticket's own record is itself a thing that goes stale.

- ACBP-P7-002 — THE FIRST ENFORCEMENT PLAN WAS KILLED BEFORE ANY CODE, AND SO WAS ITS PREFERRED REMEDY. A nine-agent design investigation (four read-only lenses, synthesis, three refutation lenses instructed to default to REFUTED, completeness critic) found that the obvious five-point plan did not cover the three shipped worker bodies, and that its own favoured fix — the `policyPrecheck` seam — would have PERMANENTLY PRECLUDED NFR-015's spend caps, because `createModelGateway` throws when both are supplied. A remedy that closes today's gap by making a committed requirement unbuildable is not a remedy. Four of the five points shipped. **The fifth is `runWorkerStep`**, which waits on the durable-stop sweep §9.5 gates — and the three worker BODIES are a separate, larger gap: they pass through none of the five points, which is the reason the plan was killed in the first place. Both are disclosed open (§9.3) rather than papered over, and conflating them would understate the residual.

- ACBP-P7-002 — THIS TICKET SILENTLY DISABLED A MERGED TRUST-CRITICAL CONTROL, AND NO TEST SAW IT. The new lifecycle gate outranks the stop gate in `decideDispatch`. P6-007's held-work capture was keyed on WHICH REFUSAL WAS REPORTED (`finalReason === 'emergency_stopped'`), so for a company BOTH non-active AND covered by a live stop — **a combination NO fixture in this repository produces, which is precisely why nothing caught it** — the capture stopped running: no `held_work` row, no `running`→`paused`, and ADMIN-002's confirm-or-discard review lost that task entirely. The audit event carried the identical bug, so the permanent record also lost the trace. The contract-level precedence test lists "allowlist, policy and approval" and conspicuously omits `stop`. GENERAL CLASS, now CDR-079 §9.14: **a new gate that outranks an existing one inherits responsibility for every side effect the old one carried.** Nothing in this repository enforces that rule, and the failure is invisible because both gates independently return a correct-looking refusal.

- ACBP-P7-002 — AND THE FIX FOR IT WAS TOO BROAD; CI CAUGHT THAT. Keying the capture on `stopEvaluation.kind` alone made ANY denial pollute the review queue, and `'a refusal for a DIFFERENT reason holds nothing — WITH a covering stop in force'` failed immediately. That test's own comment names this exact mutation as the one it exists to catch, and it had itself been hardened by an EARLIER independent review after shipping vacuous. Two lessons in one failure: a guard written to catch a specific mutation caught it, and the widen-the-guard reflex from the previous phase is not free — widening past a guard's purpose is its own defect. The surviving line distinguishes a call the stop INTERRUPTED from one that was never going to happen: `not_registered`/`no_allowlist`/`not_allowlisted` describe requests invalid on their own terms; `company_not_active` describes a well-formed call that both controls independently refused.

- ACBP-P7-002 — BOTH HIGH FINDINGS WERE IN MY OWN CLAIMS, NOT IN THE CODE, WHICH IS NOW THE PROGRAMME'S DOMINANT PATTERN. (1) `coordinator.ts` asserted the gate made "no NEW autonomous work" true "since every body takes a `runId`" — false, and its stated reason INVERTED: a `runId` is provenance metadata, its own docstring says so, and `runResearch` validates it against nothing, reaching the network and the METERED GATEWAY before its first database statement. A fabricated uuid spends money identically; only the persist fails, afterwards. (2) CDR-079 still read "the enforcement plan the investigation KILLED" and "Enforcement — blocked" while the branch shipped four of those five points, so the only place the ruling was argued was a code comment. **That is the second stale-record HIGH in one week** (ACBP-P7-001's PROJECT-STATE forward pointer was the first): the record goes stale BEFORE the code does, because code is verified by CI every push and prose is verified by nobody.

- ACBP-P7-002 — THE MUTATION PROBE'S FIRST RUN WAS A FALSE CONFIRMATION, AND THE PROBE THEN FOUND A DEFECT IN MY OWN SUITE. A disposable branch neutralised the gate without touching a test; the intended evidence was "tests go red". The first attempt went red at LINT (an unused constant) and NEVER REACHED THE TESTS — accepting "CI is red" would have recorded a verification that verified nothing, the same shape as the aborted-harness incident on P7-001. Rebuilt to pass static checks, it turned **8 of the suite's then-17 Gate-14 cases red** through production paths against real PostgreSQL. **THAT FIGURE CANNOT BE RE-DERIVED BY ANYONE, AND THAT IS THIS TICKET'S FAULT.** The probe branch was disposable and was NOT preserved, and no CI run id was cited — where ACBP-P6-006 did the recoverable half properly: it recorded BOTH the probe SHA (`fe85082`) AND its CI run id (`30646208952`, CDR-071:184). The SHA is reachable from no ref today — that branch was squash-merged and deleted — so the RUN ID is the only part that survived, and the part worth copying. So the word "mutation-proven", which the backlog row and the PR both lean on, rests on prose: exactly the weakness the artefacts above illustrate, reproduced by the ticket that was documenting it. The suite is also 18 cases now and the probe was not re-run, and the enumeration is probably short by one, since `'RESUMING restores the ability to work'` asserts the same refusal and cannot have stayed green. RULE: **a probe that proves a suite measures something is evidence, and evidence gets committed.** It also exposed a weak assertion of mine: `expect(refused.status).not.toBe('task_not_startable')` PASSED WITH THE GATE OFF, because a successful `'ok'` is also not `task_not_startable`. **A negative-only assertion cannot fail in the direction it is named after.** And the same review found the suite's fixture seeded no policy, so an ACTIVE company in it is refused `policy_denied` — the two dispatcher cases were passing against a fixture whose every call was refused for an unrelated reason, which `dispatcher.integration.test.ts` had already warned about in writing.

- ACBP-P7-002 — THE TICKET IS NOT MARKED DONE, DELIBERATELY, AND THE BACKLOG SAYS WHY IN PROSE. Four of five enforcement points ship and are mutation-proven, but the deactivate TRANSITIONS are not built — so **nothing can reach `deactivating` in production; the two new states are reachable only by a direct database write** — the durable-stop sweep is absent, the account half is deferred by owner ruling, and the worker bodies remain ungated. Every one of those is an owner decision (§9.2, §9.3, §9.5, §9.7, §9.8, §9.10, and §10 slice 5), not an engineering gap I may close alone. Following P6-002's precedent, the Status cell carries the unmet clause rather than `Done`. A ticket that launched a real control and honestly says what it did not launch is worth more than one whose row says Done and whose gate is half-built.

- ACBP-P7-002 — THE DOCUMENTATION NEEDED ITS OWN ADVERSARIAL PASS, AND IT RETURNED **36 CONFIRMED DEFECTS, 7 HIGH, EVERY ONE IN PROSE**. After the code review closed, six lenses plus a completeness critic were pointed at nothing but the sentences this ticket had written — every number, every quoted string, every section cross-reference, every claim of absence. WHY IT WAS RUN AT ALL: both HIGH findings of the CODE review had been in this ticket's own claims rather than in its code, so the prose was the demonstrated weak point, and nothing else checks it — **CI verifies code on every push; a paragraph is verified by whoever happens to reread it.** WHAT IT FOUND, in descending order of embarrassment. (1) THREE PERMANENT SURFACES RECORDED THE TICKET AS **MERGED** — the review ledger, PROJECT-STATE and the backlog Status cell — while PR #74 sat an open draft ahead of `origin/main`. **Merging is an owner gate. I wrote down that a gate had been taken.** (2) The CDR's own §9.14 still documented the FIRST version of its fix — the one CI REJECTED — so the section teaching "do not break the control below you" would have taught the broken version. (3) The corrected EVENT-CATALOG note, written to strike a phantom `company.paused` consumer, certified the `company.deactivated` row's `export` consumer as "real and independent" ONE COLUMN OVER — and there is no event-delivery machinery in this repository at all, no outbox, no subscriber, no LISTEN. The true fact is narrower and is not consumption: export keeps working on a deactivated company because nothing gates it. (4) The same note credited the reads to `mayStartAutonomousWork`, a PURE PREDICATE whose docstring says "no clock, no I/O" — miscrediting a pure function with production reads, in the note documenting a docstring that miscredited a pure function with production reads. (5) A **fifth** false artefact surfaced only here: `REQUIREMENT-TRACEABILITY.csv` marked COMP-006 `Covered (MVP)`. (6) FOUR documents sent readers to §9.5 for the deactivate transitions — including the review ledger itself; §9.5 asks a different question, and §9.8 and §9.10 were cited nowhere in the repository, while §10 slice 5 was named only in the ledger and the CDR's status line, never where a reader was sent to §9.5. GENERAL CLASS, and it is the one to carry forward: **a documentation pass is a change like any other and needs the same adversarial treatment as code — more, because nothing else will catch it.** The corrections are made in place and visibly, not silently, because a record that quietly repairs itself teaches nothing.
- ACBP-P7-007 — THE LIST THIS TICKET WAS JUDGED AGAINST WAS ITSELF UNRELIABLE, AND ALL TWENTY ITEMS ALREADY HAD SOMETHING GREEN BESIDE THEIR NAMES. The acceptance criterion is "all suites green"; the honest way to read it is to open the twenty trust-critical negatives and read the test **bodies**, not the checkmarks. As of `main` at `2c4f0f5`: **eleven** genuinely assert database state or a recorded row through the production entry point; **four** rest on a returned value or weaker (7, 10, 14, 19); **three** never execute the claim as worded (3, 16, 18); **two** are not covered at all (8, 15). 11+4+3+2 = 20. **Seven of the twenty attribution lines were wrong or incomplete**, plus a misgrouped `#9` in a second canon file and **four coverage cells across the two traceability matrices** naming tests, rigs and reviews that do not exist. The lesson is not "the list was sloppy". It is that **a checkmark and a test title are the same artefact to a reader and completely different artefacts to a machine**, and only one of them can be verified — which is why the deliverable became a checker rather than a correction. (That count read "twelve" and totalled twenty-one across five documents until a review pass added it up. A classification of exactly twenty things that does not sum to twenty is the cheapest self-check available, and nobody ran it.)
- ACBP-P7-007 — TWO OF THE TWENTY CAN NEVER GO GREEN AS WORDED, AND SAYING SO IS THE DELIVERABLE. **#8** asserts that a revoked integration denies a tool call; **no integrations entity exists anywhere in this repository** — no table, no migration, no service, no contract, no integration value in `TOOL_DENIAL_REASONS` — and CDR-067, the decision record of the ticket credited with building the rig, never mentions integrations. **#15** names a provider API key that does not exist in the runtime. An acceptance criterion that cannot be met on its literal wording is a **finding about the criterion**, and the two honest moves are to prove the narrower property that will actually carry the claim and to write down, in the row itself, that the narrower property is what was proven. The dishonest move — the one every green checkmark above was an instance of — is to point the criterion at whatever happens to be green nearby.
- ACBP-P7-007 — THE RULING: A NEGATIVE IS GREEN ONLY WITH A RECORDED MUTATION **RUN ID**, NOT A PROBE SHA. P6-006 recorded probe commit `fe85082`, which is reachable from **no ref today** because the branch was squash-merged and deleted; only run id `30646208952` survived. P7-002 recorded no run at all and its probe figure ("8 of 17 red") is therefore unre-derivable prose. So this ticket enforced the rule in a tool — `tools/trust-critical-index.mjs` plus `check-trust-critical-index.mjs`, now in `check:static` — where each of the 20 rows carries a file, a **verbatim** test title, an entry point, a closed-vocabulary anchor class (`database_state | recorded_row | return_value_only | pure_helper_only | none`), who really built it, the exact mutation, its run id, and **what it does not prove**. Statement drift, a missing file, a title no longer attached to a live `test(`, a `measured` row without a run id, or a run id on an unmeasured row all fail the build. **An attribution with no test is now a red build.** The probe then ran — run `31113087854`, three deliberately type-safe and lint-clean mutations, **3747 passed / 5 failed of 3752** — and the branch was deleted **on purpose**, to demonstrate that the run id resolves afterwards and the SHA does not.
- ACBP-P7-007 — I RECORDED THREE MEASURED ROWS AND THE RUN HAD BOUGHT TWO. This is the ticket's own thesis, committed by the ticket. Row 19 named the test *"a MATERIAL decision does NOT silently fall over"* — which asserts only outcome, fallbackUsed, callCount and validatedOutput, **none of which the recorded mutation can touch**. That test was green in run `31113087854`. What went red was a *different* test in the same file, and it went red through that test's **leak** assertion, making the evidence about egress rather than about silent fallback. Two independent reviews caught it; one pulled the run log and enumerated the failing set. **The checker could not catch it, and that is a design fact rather than a bug: nothing cross-checks a row's `mutation` against its `testTitle`.** So the rule "the run in which THE NAMED TEST actually went red" was, in practice, enforced by a human reading a log — and the human read it wrong. Row 19 is back to `unmeasured`, the ceiling is 18, and both limits (a run id is shape-checked and never resolved; no mutation↔title cross-check exists) are now printed by the tool and stated in its header rather than left for a reader to assume away. **A rule enforced only by its author's attention is not enforced.**
- ACBP-P7-007 — THE FIFTH FAILURE WAS NOT PLANNED, AND IT IS RECORDED RATHER THAN TRIMMED. The probe's fifth red test — `model-gateway.test.ts > success returns validated output and writes exactly one ok usage event` — went red because appending a marker to `model` broke an unrelated **success-path** assertion. **The mutation was not surgical**, and a mutation with collateral damage makes a red run harder to read: a reviewer must separate intended failures from side effects, which is exactly the work the run id was supposed to remove. Two durable rules. **A partial local run is not a preview of CI** — locally only the three touched files were run, showing four failures; the fifth existed only in the full suite. And **the count of red tests belongs in the row**, so nobody re-runs the probe expecting four and quietly "fixes" the fifth.
- ACBP-P7-007 — ONE REAL PRODUCTION BUG, FOUND BY WRITING THE TEST THAT WAS MISSING; THEN THE FIX'S OWN COMMENT WAS FOUND WRONG. `logger.ts` passed `metadata` and `error` through `redact()` and emitted `message` **verbatim** — so any caller interpolating a secret into a log message published it, while the file read as if redaction were handled. Red-then-green. This is the argument for the whole ticket in one line: the hole was not in code nobody had tested, it was in code whose test **covered the two fields that were already safe**. But the fix's comment then offered *"a connection string or `password=…`"* as interchangeable examples, and a review measured them: `password=` was covered and **a connection string was emitted verbatim**, as were a JWT, an AWS key id, a Slack token and the `Basic` scheme. So the measured mutation proved only that `message` is PIPED THROUGH the redactor — the case it reddened plants the one shape already handled. `redactString` now composes `redactSecrets` (the contracts `SECRET_PATTERNS`, a strict superset, and the **same detector `containsSecret` uses**, so the audit sweep and the log redactor finally agree on what a secret is), `event` is redacted too because it is `string`-typed and the dotted-name convention is not a control, and there is one case per shape plus two non-erasure controls.
- ACBP-P7-007 — SEVEN TIMES A TOOL BUILT BY THIS TICKET CAUGHT ITS OWN AUTHOR, WHICH IS THE ONLY EVIDENCE THAT ANY OF IT WORKS. (1) The index checker rejected my own citation **one minute after it existed** — item 14's title had drifted from the source. (2) The ratchet **failed the build for ADDING COVERAGE**: it counted unmeasured rows alone, so a row moving `not_covered → unmeasured` made the number rise; replaced with a count of every not-yet-proven row. (3) A mutation that proved nothing was caught before being recorded — my first #19 edit touched a path a provider failure never reaches, so all nine tests passed, and **that outcome is indistinguishable from the finding it was supposed to disprove**. (4) The secret scanner was **invisible to itself**: a literal NUL byte in its own binary-file guard made it skip its own source. (5) The index checker blocked a canon edit mid-flight because my first correction rewrote a *statement* instead of its attribution. (6) ESLint died of an out-of-memory after reporting 30,288 errors from inside a sibling agent worktree — the local gate could not finish at all. (7) The new live-test check failed on the real index within a minute of existing, because item 14's title contains an apostrophe and the first version unescaped the source **before** scanning for literal boundaries. Two of the seven are the same mistake in different clothes: a guard written this week, wrong on its first contact with real data.
- ACBP-P7-007 — THE CORRECTION RE-CREATED THE DEFECT IT DOCUMENTED, FOR THE THIRD TIME IN THIS REPOSITORY, AND THIS TIME IN A LITERAL BYTE. The scanner had been blind to itself because of a raw NUL in its binary-file guard. The fix worked — and then the COMMENT explaining the fix was written with a raw NUL where the six characters `\\u0000` were meant, so `tools/check-secrets.mjs` still contained a NUL, still skipped its own file, and **three separate documents said the blindness had been fixed**. `CDR-080` carried the same corrupted byte. A review found it by reading bytes rather than rendered text. The scanner now reports **734** files where it reported 733: the extra one is itself. Standing rule: **a sentence describing a byte-level defect is the most likely place to re-commit it**, and rendered text cannot show you that.
- ACBP-P7-007 — A SHIFTED CSV ROW HAS NOW ANSWERED A COVERAGE QUESTION WRONGLY TWICE, SO IT IS A GATE NOW RATHER THAN A LESSON. This ticket wrote `Partially covered - boundary only, no quarantine (MVP)` **unquoted** into the `Coverage status` column of BOTH traceability matrices. A real parser then read `Partially covered - boundary only` as the coverage, ` no quarantine (MVP)` as the `Gap or question`, and pushed the entire explanatory note into a phantom twelfth column no header names. The diff looked fine; the row was wrong. That is the same class as ACBP-P6-011's backlog row landing one column left. `tools/check-csv-shape.mjs` now parses every **tracked** CSV with RFC-4180 rules and fails the build when any data row's width differs from its header, with its own negative self-test and 11 regression cases. It found two more rows malformed since the Phase 0 initial commit; both were pure quoting errors and both are fixed, so the checker ships with **no exclusion list** — an exclusion list being the thing that rots.
- ACBP-P7-007 — WHAT IS DELIBERATELY NOT DELIVERED, EACH FOR A NAMED REASON. *"Isolation re-proven on staging"* — **no staging exists in any form**, there is no deployment configuration of any kind in this repository. The pen test is an external engagement that `RELEASE-GATES.md` places at the General MVP gate, not the closed-beta gate this feeds, and there is no findings source to close against. NFR-018's log-pipeline scanner cannot exist because **no log pipeline is deployed** — "zero findings on secrets" is evidence about the repository, not about the product, and the CI step is now named so that distinction is visible rather than buried in an aggregate gate. NFR-010's ASVS items — **CSRF, HTTP rate limiting, and security headers/CSP absent from `apps/web` as of `main` at `2c4f0f5`** — are recorded as findings with three tickets spawned, now in flight on unpushed local branches. And **18 of 20 index rows are unproven**: the remaining strengthening work is real-PostgreSQL, local PG is unreachable here, and they are **left unmeasured rather than written unverified**, which is the entire point of having a word for it.
- ACBP-P7-007 — MERGED AS SQUASH `1bb4751` (PR #76), AND THE LEDGER'S OWN OPENING LINE WENT STALE THE INSTANT IT DID. That line read *"THE TICKET IS NOT DONE AND IS NOT MERGED"* — half of which the merge falsified — and it is **the same defect ACBP-P7-002 produced, recorded as a lesson in this very ledger, and then committed again by the author who wrote the lesson**: a status WORD goes stale, a SHA does not. Corrected in place rather than quietly, because a merged ticket whose review ledger opens by denying the merge is worth more as a demonstration than as a clean page. **Knowing the rule is not the same as being protected by it** — the only thing that would have caught this is a check, and there isn't one. The rule stands and now applies to itself: *name the commit, not the state.*
- ACBP-P7-007 — THE MERGE WAS VERIFIED BY TREE HASH, NOT BY ANCESTRY, BECAUSE A SQUASH DESTROYS ANCESTRY. `git merge-base --is-ancestor` is meaningless after a squash merge, so the check that actually answers "did main get what CI tested" is comparing the TREE object: branch `381601a` and the merged `1bb4751` are both `c236ea1`, byte-identical. Exact-head CI `31123686961` on `381601a` was **265 files, 3782 passed, ZERO skips** against 1603 skipped locally — the real-PostgreSQL suites, which is the entire gap the hosted run exists to close.
- ACBP-P7-007 — EXACT-MAIN CI NEVER RAN, AND THAT IS AN UNMET STEP RATHER THAN A WAIVED ONE. GitHub created **no workflow run anywhere in this repository** between 17:45 and at least 19:11 UTC; the merge at 18:19 fell inside that window and produced nothing, and `ci.yml` has no `workflow_dispatch` trigger, so there is no way to force one. The tree-identity argument above is genuinely strong — a squash of an identical tree onto a fast-forwardable base introduces no content the green run did not execute — but it is an ARGUMENT, and the completion standard asks for a RUN. Recording the distinction because collapsing it is the exact move this ticket spent its whole length hunting. **Standing gap this exposed: the workflow has no manual trigger, so an Actions outage is unrecoverable rather than merely slow.**
- ACBP-P7-007 — THE ONE THING THE MERGE ITSELF PROVED. Before it, the ratchet's no-rise rule could not run: it compares `MAX_UNPROVEN` against `origin/main`, and `tools/trust-critical-index.mjs` did not exist there, so the tool honestly printed *"Ceiling baseline UNREADABLE — the no-rise rule was NOT enforced on this run."* Post-merge it resolves (`Ceiling 18 ≤ baseline 18 (origin/main)`), and raising the value to 19 exits **1** with the message naming why. **That closes the review finding that the word "ratchet" named no enforcer** — and it was verified by mutation, on main, rather than by assuming the merge would fix it.
- 2026-08-07 â€” ACBP-P7-008 failure-injection pass, slices 5-7a on branch `p7-008-failure-injection-pass` (draft PR #81), head `668198f`. Slice 5: launch **gate 8** was measured from a raw `INSERT INTO emergency_stops` helper NAMED `activateStop`, so it timed TRANSACTION VISIBILITY and excluded the production activation path entirely â€” exactly what ACBP-P7-007 recorded as trust-critical #10 defect. Added a test driving the real `activateStop` use case (owner scope resolution + `stop:activate` + audit + held-work capture inside the measured window), asserting a control first and asserting the activation actually returned `ok` so a `forbidden` result cannot leave the timing assertion measuring nothing. The import is aliased to `activateStopUseCase`: two things named `activateStop` in one file is the trap slice 2 spent its length removing.
- ACBP-P7-008 slice 6 â€” THE PROBE COULD NOT START, BECAUSE THE COLUMN IT RUNS FROM WAS PROSE. Reading the `mutation` field of both evidence indexes as what it claims to be (the exact edit that reddens the cited test), **18 of the 33 rows carrying one could not be applied by anyone who had not written them**: "widen the heartbeat grace to infinity", "skip the stop check at the step boundary", "remove the idempotency read-back". `checkMutationNamesRealCode` now requires a mutation to name a camelCase/snake_case/SCREAMING_SNAKE symbol or a source filename that EXISTS in non-test source; both checkers exit 2 rather than 0 if the source walk finds zero files. All 18 rewritten against the code. Three were not merely vague but WRONG: scenario 2 aimed at the ineligible SIBLING test; scenario 3 would have hung the suite so the only red was a timeout; trust-critical 1 was an EQUIVALENT MUTATION (RLS still returns no row, so foreign and unknown stay byte-identical and the cited test never notices) â€” a green probe there would have been read as proof. The tool also caught trust-critical #10, which I had rewritten an hour earlier.
- ACBP-P7-008 slice 6 â€” THE RULE STATES ITS OWN LIMIT, AND THE LIMIT ALREADY BIT. It cannot tell a right symbol from a wrong-but-real one: scenario row 16 named `startRun` while its cited test drives `enqueueJob`, both real functions in the same file. A human reading the test body caught it; no tool did. Corrected by hand and pinned as a test case so the limit cannot quietly stop being true. A first version of the rule also FAILED HONEST ROWS â€” the tokeniser did not admit hyphens, so `enqueue-job.ts` became `job.ts` â€” found reviewing my own diff before writing the docs describing it, and fixed at `668198f`. That direction is the worse one: a rule that fails good rows teaches people the tool is wrong.
- ACBP-P7-008 slice 7a â€” `docs/implementation/P7-008-SCENARIO-EVIDENCE.md` is GENERATED from the index by `tools/render-scenario-evidence.mjs`, and `check:scenario-evidence` (now in `check:static`) fails the build if the two disagree. Verified by mutation: appending one line exits 1, re-rendering exits 0. A hand-written summary of a machine-checked table is a second copy of the truth nothing keeps honest â€” P7-002 shipped artefacts asserting a control nobody built, and P7-007 ledger opened with "THE TICKET IS NOT DONE AND IS NOT MERGED", true when written and false at merge. Backlog `Requirement IDs` for the ticket is now `NFR-005;NFR-019` (NFR-020 removed per CDR-084 SS5 + owner ruling), verified by re-parsing the CSV and diffing every cell of all 101 rows: exactly one changed.
- ACBP-P7-008 â€” **THE PROBE ITSELF IS STILL OWED, AND IS BLOCKED BY AN ACTIONS OUTAGE.** GitHub Actions entered a major outage at 15:22 UTC on 2026-08-06 with webhooks throttled to ~15%, and **no workflow run has ever been created for this branch** â€” not for `f90566b`, not for `9c34123`, not since. So 0 of 16 scenario rows are `measured`, both ceilings are unchanged (16 and 18), and the slice-4 and slice-5 tests are written, typechecked and index-cited but **UNVERIFIED**: they are `describe.skipIf(!hasTestDatabase)` and skip locally, 51 collected and 51 skipped. Nothing in this ticket claims they pass. This is the SECOND ticket in two days stopped by the standing gap P7-007 named: `ci.yml` has no `workflow_dispatch` trigger, so an outage is unrecoverable rather than merely slow.
- 2026-08-07 - ACBP-P7-008 THE PROBE RAN, and the Actions outage that blocked it was broken by a route I first documented as impossible. `ci.yml` had no manual trigger - a gap ACBP-P7-007 named in its own log after it could not run exact-main CI. Adding `workflow_dispatch` I asserted in the file that GitHub only honours it on the DEFAULT branch, so it could not help the branch adding it. TESTED INSTEAD OF ASSUMED: `gh workflow run ci.yml --ref p7-008-failure-injection-pass` was accepted and created run 31128906516 on 2314ef7 - the first run this branch ever had, DURING the outage. The comment is corrected in place at c2649aa. Writing a false sentence and shipping it took ten minutes; this ticket exists to remove that artefact class.
- ACBP-P7-008 - SLICES 4 AND 5 ARE NOW VERIFIED, not merely written. Run 31128906516 and 31129035777 are both GREEN with ZERO SKIPS (268 files, 3874 passed, no "skipped" token anywhere in either log), so the expired-approval pair and the gate-8 production-path test ran against real PostgreSQL. The gate-8 test printed its measurement: `first refusal 13.8ms after the owner acted (bound 5000ms)` - launch gate 8 measured through the REAL activateStop use case, with owner scope resolution, `stop:activate` authorization, the audit write and held-work capture INSIDE the window. ACBP-P7-007 recorded that exclusion as trust-critical #10 defect and could not fix it there.
- ACBP-P7-008 slice 6 - FIVE MUTATIONS, FIVE RUN IDS, FOUR ROWS MEASURED. Disposable branch p7-008-mutation-probe, ONE mutation live at a time (each reverted by the commit introducing the next), every one type-safe and lint-clean so it reached the tests rather than dying at the static gate. M1 31129056434 approval expiry BOTH halves (2 red of 3874) measured scenario row 9 AND trust-critical #7; M2 31129196873 account_wide covers nothing (9 red) measured row 15; M3 31139103437 company scope matches nothing (4 red) measured trust-critical #10; M4 31140011057 paused company enqueues (1 red) measured row 16; M5 31140772210 audit rejection swallowed (1 red) measured row 14. Every failed test name READ OUT OF THE RUN LOG, never inferred from what the edit was meant to do.
- ACBP-P7-008 - THE GREEN HALF IS THE EVIDENCE, and it is why these runs mean anything. A red test alone is consistent with a build that refuses everything. M1 paired CONTROL ("the SAME approval, unexpired, authorizes") stayed GREEN, so the refusal is about EXPIRY. M2 and M3 each cut one arm of `evaluateStops` and left the others intact - four sibling scopes and every MISSES case green in both. M4 replay sibling stayed green, so the lifecycle gate is scoped to NEW work and does not break replay (CDR-079 SS6-G5). AND M3 WAS NECESSARY BECAUSE M2 LEFT #10 TEST GREEN: two rows citing tests in the same FILE still need two runs when they exercise different arms of the same function.
- ACBP-P7-008 - THE PROBE PROVED A CORRECTION NO TOOL COULD MAKE. Row 16 named `startRun` as its entry point while its cited test drives `enqueueJob`; both are real functions in the same file, so the new mutation-names-real-code rule PASSED it and a human reading the test body caught it. M4 then demonstrated the correction: mutating `enqueueJob` reddens the test. Run against the row as originally written, the probe would have edited `startRun`, seen a red somewhere, and recorded a run id proving nothing about row 16 - the ACBP-P7-007 row-19 defect, reproduced by the ticket built to prevent it.
- ACBP-P7-008 - WHAT IS STILL NOT MEASURED, stated because four green rows read as more than they are. TWELVE of sixteen scenario rows and SIXTEEN of twenty trust-critical rows remain `unmeasured`: they have live tests that pass and nothing has tried to break them. Two scenario rows have no injectable subject. The acceptance criterion "16-scenario matrix green" is STILL NOT MET (CDR-084 SS0.1 explains why it cannot be on its literal wording). Ceilings moved down for the first time - failure-scenario 16 to 12, trust-critical 18 to 16, ratchet reporting "Ceiling 16 <= baseline 18 (origin/main)". A run id is still shape-checked and never resolved (CDR-080 SS7.10) and nothing cross-checks a mutation against its test title (SS7.11).
- 2026-08-07 - ACBP-P7-008 - THE ENTRY ABOVE IS SUPERSEDED BY SQUASH 442456d, and both of its open items are closed. FOURTEEN of sixteen scenario rows are now measured - every row that HAS a subject - so the failure-scenario ceiling went 12 to 2, which is its FLOOR: rows 5 and 10 cannot be measured without first building the thing being measured. The SS7.11 gap named at the end of that entry was then closed too, by ca2a901 (`pnpm run verify:mutation-runs`), which resolves every recorded run id and confirms the row's OWN cited test is among that run's failures - all 18 measured rows across both indexes CONFIRMED. SS7.10 IS NOT CLOSED AND SAYING SO WOULD BE THE OVERCLAIM THIS TICKET EXISTS TO REMOVE: `verify:mutation-runs` is deliberately OPT-IN and outside `check:static`, because the checkers never contact GitHub so they run offline and unauthenticated. A run id sitting in either index is still shape-checked and never resolved BY THE BUILD; it is resolved only when someone runs the tool. The trust-critical half was untouched at 4 of 20 when this was written; squash 5b7ad92 took it to 12 of 20 the same day.
- 2026-08-08 - TRUST-CRITICAL INDEX, 4 MEASURED TO 12 OF 20 (squash 5b7ad92, PR #90, ceiling 16 to 8, no production code change). THE AUDIT CAME BEFORE THE PROBE AND IS THE RESULT: reading the cited test body of all fifteen unmeasured rows, BEFORE spending a CI cycle on any of them, found FIVE whose recorded text cannot prove what the row claims - the same rate ACBP-P7-008 hit, which makes it a property of these tables rather than one bad afternoon. Row 4 cites a test for a DIFFERENT CONTROL (it claims the worker allowlist denies unregistered tools; the cited test compares key sets of the emergency-STOP scope map and dispatches nothing). Row 6 cites the CONTROL rather than the property. Row 9 named a mutation that ADDS an entry point, which cannot redden a test calling startRun. Row 11 carried the same inert mutation the scenario index did - one test, two indexes, one wrong sentence in both. Row 19 HAD BEEN CORRECTED ONCE AND THE CORRECTION WAS ALSO WRONG. Rows 4 and 6 stay unmeasured rather than not_covered, because whether a correct test exists has not been searched and claiming absence without looking is the error these indexes exist to prevent.
- 2026-08-11 - THE TWO ROWS THAT COULD NOT BE PROBED AT ALL ARE MEASURED (squash 584c32b, PR #92, ceiling 8 to 6, 14 of 20). NEITHER NEEDED A BETTER MUTATION - BOTH NEEDED A CORRECT CITATION, and both correct tests already existed. Row 4 cited a stop-scope key-set comparison; its real test was ABOUT A HUNDRED LINES AWAY IN THE SAME FILE and asserts denial_reason read back from the database, matching the recorded_row anchor the row already claimed. Row 6 cited THE CONTROL - a test whose whole assertion is that an UNCHANGED payload is authorized, so it stays green under any binding mutation by construction. TEN red for row 4, all about the allowlist; TWELVE for row 6, all about payload binding. THE LESSON IS ABOUT THE EARLIER DECISION: holding these at unmeasured rather than declaring them not_covered was right, because a correct test was sitting in each suite the whole time.
- 2026-08-11 - A LOCAL FLAKE IS RECORDED AND NOT CLOSED. The first pnpm run check on the rows 4/6 work reported 1 failed / 2429 passed; two later local runs were clean and THREE HOSTED RUNS were 4056/4056 zero-skip. THE FAILING TEST WAS NEVER IDENTIFIED, because the output was filtered before the name was read. A green CI run does not retroactively explain a red local one. Capture full test output to a FILE before filtering, so a one-off failure stays nameable.
- 2026-08-11 - THE NUL DEFECT HAD RECURRED TWICE MORE, AND check:encoding COULD NOT SEE IT. tools/check-secrets.mjs skips any file containing a NUL (its own line 170), which is how it once went blind to itself. A repo-wide byte sweep found raw NULs in TWO tracked text files - docs/agent/EXECUTION-LOG.md (inside the very entry describing this defect, where the six characters backslash-u-0-0-0-0 were meant) and packages/contracts/src/tools/untrusted.test.ts - so BOTH were being skipped by the secret scanner. The encoding checker passed them because its rules cover BOM, raw TAB, lone CR and PowerShell escape products, but NOT NUL. Guard coverage was the defect again: the rule existed, the byte was outside it.
- ACBP - A RED CITED TEST IS NOT AUTOMATICALLY EVIDENCE, and the SECTION 7.11 CROSS-CHECK CANNOT SEE THE DIFFERENCE. Trust-critical row 12's first mutation dropped UNIQUE from the usage idempotency index. That does not remove deduplication - it removes what ON CONFLICT needs in order to resolve, so every usage write raised SQLSTATE 42P10 and NINETY-FOUR tests went red across planning, steering and authz, because metering sits on every model call. The cited test was among them, so verify:mutation-runs reported CONFIRMED - and the run proves nothing, because the test failed on the FIRST insert throwing and the question it asks was never reached. What caught it was the stack trace and the blast radius: 94 against 3. BOTH RUNS ARE RECORDED IN THE ROW so the contrast can be re-derived. The tool I built the same morning has a stated limit now, written where the next reader will meet it.
- ACBP - A CONTROL CAN HAVE AN ENFORCEMENT LAYER ABOVE ITS TEST, and then the row cannot be measured honestly. Trust-critical row 13 says corrections never edit the original usage row; its recorded mutation is to make recordUsageCorrection UPDATE instead of insert. That CANNOT BE WRITTEN: every usage_events column is typed ColumnType<T, T, never> and the third parameter is the UPDATE type, so the probe died at TS2322. Measuring the row would need a TWO-FILE mutation - weaken the schema type AND edit the service - which proves LESS than a one-file one, because a reader cannot tell which of the two edits the test noticed. Left unmeasured deliberately, with the reason in the row.
- ACBP - FOUR SOUND ROWS WERE LEFT UNPROBED ON PURPOSE (trust-critical 1, 14, 17, 20). Their mutations need call paths that were not traced - runInAccountScope resolves role through resolveAccountContext into a database bootstrap helper; the rollup aggregate sits inside a per-company loop; row 1's obvious edit echoes the caller's own input rather than leaking tenancy. Stopping is the cheaper error: three of the five defects found this session are mutations that LOOK right and prove nothing, and a guessed anchor is how a fourth would have been created.
- ACBP-P7-008 - THE SLICE-6 RULE PASSED THREE ROWS THAT WERE WRONG, and that is the finding rather than the count. It verifies a mutation names REAL SYMBOLS IN REAL FILES; it cannot tell whether the edit ACHIEVES the effect the row claims. Row 11's recorded mutation was INERT - skipping `enqueueJob`'s `findByIdempotencyKey` read-back cannot create a second job, because dedupe is the partial unique index plus ON CONFLICT DO NOTHING, so the probe would have gone green and been read as proof. Row 12's named the WRONG ONE OF TWO call sites: `listCheckpoints` is called in `runJobStep` and in `getResumeState`, the cited test never asks `runJobStep` to re-run anything, and mutating it reddened three NEIGHBOURS while leaving the cited test GREEN (run 31212362663) - the ACBP-P7-007 row-19 shape, caught this time from the other side. Row 2's first attempt died at lint and never ran. Each was caught by a human reading a test body or a run log. The rule's limit was already documented; this is the first time it cost three rows in one pass.
- ACBP-P7-008 - A MUTATION THAT REDDENS 175 TESTS IS WEAKER EVIDENCE THAN ONE THAT REDDENS 1, and the row now says so instead of the tally hiding it. Row 6 mutates `withTransaction`, which wraps every database write in the platform, so there is no narrower edit - the row's claim IS about the wrapper. It is recorded as measured with the limit written in: the run proves the rollback is load-bearing across the suite, but with 175 failures a cascade explains any single one of them, so it does NOT isolate row 6's own assertion the way row 8's SINGLE-TEST run does. ACBP-P7-007 recorded that a non-surgical mutation makes a red run harder to read; this is the clearest instance so far, and collateral is now recorded PER ROW rather than averaged into a total.
- 2026-08-07 - A DEPENDENCY ADVISORY CAN REDDEN A BRANCH THAT DID NOT TOUCH DEPENDENCIES, and diagnosing it as "my change" would have been the expensive mistake. GHSA-2v37-7h3g-55p8 (`nanoid <3.3.17`) was published between main's last green run at ca2a901 and the next push, so the High+ audit gate failed on a docs-only branch. The same red was waiting on main and every open branch. THE FIX HAD A TRAP: the three existing overrides in `pnpm-workspace.yaml` all use `>=`, and `nanoid: >=3.3.17` resolves to the 6.x line, which is ESM-only, while postcss asks for `^3.3.11` and requires it from CommonJS - the audit would have gone GREEN while the web build broke. `^3.3.17` instead. Confirmed by building `apps/web`, which hosted CI does NOT do: postcss is the Next.js CSS pipeline, so no gate in this repository would have caught it.
