// @acbp/web — local dev entry (ACBP-P0-021).
//
// The web app is a scaffold: no HTTP server, routes, or UI exist yet (they arrive in Phase 1+).
// Rather than fake a running server, this prints an honest notice and exits 0. When apps/web gains a
// real entry point, this command will start it. Do NOT add product behavior here.
console.log('[@acbp/web] scaffold entry — no web server implemented yet (Phase 0). Nothing to serve.');
console.log('This dev command will start the real API/web server once apps/web is implemented in a later ticket.');
