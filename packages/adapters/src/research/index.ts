// adapters/research — the `web_research` port (ACBP-P5-006). The CONCRETE fetcher reaches the public internet and is
// an owner gate (CDR-061 §3); what lives here is the in-memory implementation every test runs against.
export { InMemoryResearchFetcher } from './in-memory-fetcher.js';
