# Secret-scan allowlist (ACBP-P0-013).
# Reviewed false positives only. One entry per line: <repo-relative-path>|<rule-id>
# Example:  packages/config/src/example.ts|generic-credential-assignment
# Adding an entry is a reviewed decision — never silence a real credential.
# (empty — no exceptions)

# ACBP-P2-007 (CDR-032): the context-assembly secret-blocklist test file contains SYNTHETIC, pattern-shaped
# secrets by design (to prove redactSecrets removes each shape). Reviewed false positives — no real credentials.
packages/contracts/src/context/context.test.ts|pem-private-key
packages/contracts/src/context/context.test.ts|bearer-token
packages/contracts/src/context/context.test.ts|openai-key
packages/contracts/src/context/context.test.ts|clerk-secret-key
packages/contracts/src/context/context.test.ts|aws-access-key-id
packages/contracts/src/context/context.test.ts|github-token
packages/contracts/src/context/context.test.ts|slack-token
# ACBP-P7-007 ADDED THREE ENTRIES HERE AND THEN REMOVED THEM; the removal is the lesson, so it is recorded
# rather than erased. They covered `metadata-secrets.test.ts` (bearer-token, aws-access-key-id) and
# `audit-secret-sweep.test.ts` (generic-credential-assignment), justified in writing by the claim that "there is
# no shape that both matches `containsSecret` and evades this scanner". THAT CLAIM WAS FALSE, and an independent
# review produced the counterexamples: assembling a literal from parts (`Bear${'er'} …`) leaves the runtime
# value identical while no source line matches, and the third entry existed only because a constant was NAMED
# `SECRET`. Both files now do that instead, and the scanner sees them again.
#
# The general rule this leaves behind: an entry silences a rule for the WHOLE FILE, FOREVER — a real
# `password = "…"` added to an allowlisted file later is invisible. Reach for it only when the value must be
# spelled out verbatim to be meaningful. That is why the seven P2-007 entries above stay: `context.test.ts`
# asserts that `redactSecrets` REWRITES each literal shape, so assembling them from parts would change what is
# under test rather than merely how it is spelled.
